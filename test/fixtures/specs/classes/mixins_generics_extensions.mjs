function __dartStr(value) {
  if (value == null) return "null";
  if (Array.isArray(value)) {
    return "[" + value.map(__dartStr).join(", ") + "]";
  }
  if (value instanceof Set) {
    return "{" + Array.from(value).map(__dartStr).join(", ") + "}";
  }
  if (value instanceof Map) {
    return "{" + Array.from(value, ([key, entryValue]) => __dartStr(key) + ": " + __dartStr(entryValue)).join(", ") + "}";
  }
  if (typeof value === "object") {
    const toString = value.toString;
    if (typeof toString === "function" && toString !== Object.prototype.toString) {
      return String(toString.call(value));
    }
  }
  return String(value);
}
function __dartPrint(value) {
  console.log(__dartStr(value));
}

// Generated by dart2esm.

const $Named_interface = Symbol("Named");

export class Box {
  constructor(value) {
    this.value = value;
  }
  read() {
    return this.value;
  }
}

export class Named {
  constructor() {
    Object.defineProperty(this, $Named_interface, { value: true });
  }
  get name() {
    throw new TypeError("Abstract member Named.name");
  }
  set name(value) {
    Object.defineProperty(this, "name", { value, writable: true, configurable: true, enumerable: true });
  }
  label() {
    return "name:" + __dartStr(this.name);
  }
}
Object.defineProperty(Named, Symbol.hasInstance, { value(value) { return value != null && value[$Named_interface] === true; } });

class _User_Object_Named {
  constructor() {
    Object.defineProperty(this, $Named_interface, { value: true });
  }
  get name() {
    throw new TypeError("Abstract member _User&Object&Named.name");
  }
  set name(value) {
    Object.defineProperty(this, "name", { value, writable: true, configurable: true, enumerable: true });
  }
  label() {
    return "name:" + __dartStr(this.name);
  }
}

export class User extends _User_Object_Named {
  constructor(name) {
    super();
    this.name = name;
    Object.defineProperty(this, $Named_interface, { value: true });
  }
}

export function identity(value) {
  return value;
}

export function StringTools_twice(_this) {
  return (_this + _this);
}

export function StringTools_get_twice(_this) {
  return function() { return StringTools_twice(_this); };
}

export function main() {
  const box = new Box(3);
  __dartPrint(__dartStr(box.read()) + " " + __dartStr(identity("x")) + " " + __dartStr(StringTools_twice("a")) + " " + __dartStr(new User("u").label()));
}

main();
