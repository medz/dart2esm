// Generated by dart2esm.

function __dartPrint(value) {
  console.log(value);
}

const $NamedThing_interface = Symbol("NamedThing");
export class NamedThing {
  get name() {
    throw new TypeError("Abstract member NamedThing.name");
  }
  describe() {
    throw new TypeError("Abstract member NamedThing.describe");
  }
}

Object.defineProperty(NamedThing, Symbol.hasInstance, { value: function(value) { return value != null && value[$NamedThing_interface] === true; } });
export class Person {
  constructor(name) {
    this.name = name;
    Object.defineProperty(this, $NamedThing_interface, { value: true });
  }
  describe() {
    return `person:${this.name}`;
  }
}

export function hide(value) {
  return value;
}

export function main() {
  let thing = new Person("ada");
  const maybeThing = hide(thing);
  __dartPrint(`${thing.name} ${thing.describe()} ${maybeThing instanceof NamedThing}`);
}

main();
