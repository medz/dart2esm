// Generated by dart2esm.

function __dartExtensionTypeRep(value, field) {
  if (value != null && typeof value === "object" && Object.prototype.hasOwnProperty.call(value, field)) return value[field];
  return value;
}

function __dartFormatException(message) {
  const error = new Error(String(message));
  error.name = "FormatException";
  return error;
}
function __dartIntTryParse(source, radix = null) {
  const text = String(source).trim();
  let base = radix == null ? null : Number(radix);
  if (base != null && (!Number.isInteger(base) || base < 2 || base > 36)) return null;
  if (base == null && /^[+-]?0x[0-9a-f]+$/i.test(text)) return Number.parseInt(text, 16);
  base ??= 10;
  const sign = /^[+-]/.test(text) ? text[0] : "";
  const digits = sign === "" ? text : text.slice(1);
  if (digits.length === 0) return null;
  for (const char of digits.toLowerCase()) {
    const code = char.charCodeAt(0);
    const value = code >= 48 && code <= 57 ? code - 48 : code >= 97 && code <= 122 ? code - 87 : -1;
    if (value < 0 || value >= base) return null;
  }
  return Number.parseInt(text, base);
}
function __dartIntParse(source, radix = null) {
  const value = __dartIntTryParse(source, radix);
  if (value == null) throw __dartFormatException("Invalid integer literal");
  return value;
}

function __dartPrint(value) {
  console.log(__dartStr(value));
}

function __dartStr(value) {
  if (value == null) return "null";
  if (Array.isArray(value)) {
    return "[" + value.map(__dartStr).join(", ") + "]";
  }
  if (value instanceof Set) {
    return "{" + Array.from(value).map(__dartStr).join(", ") + "}";
  }
  if (value instanceof Map) {
    return "{" + Array.from(value, ([key, entryValue]) => __dartStr(key) + ": " + __dartStr(entryValue)).join(", ") + "}";
  }
  if (typeof value === "object") {
    const toString = value.toString;
    if (typeof toString === "function" && toString !== Object.prototype.toString) {
      return String(toString.call(value));
    }
  }
  return String(value);
}

function __dartAs(value, test, typeName) {
  if (test(value)) {
    return value;
  }
  throw new TypeError("Type cast failed: expected " + typeName);
}

export class UserId {
  constructor(value_1) {
    this.value = $UserId_new(value_1);
  }
  static get defaultValue() {
    return $UserId_defaultValue;
  }
  static get current() {
    return new UserId(__dartExtensionTypeRep($UserId_current, "value"));
  }
  static set current(value) {
    return $UserId_current = __dartExtensionTypeRep(value, "value");
  }
  static parse(text) {
    return new UserId(__dartExtensionTypeRep($UserId_parse(text), "value"));
  }
  static get zeroValue() {
    return $UserId_zeroValue();
  }
  get doubled() {
    return $UserId_doubled(__dartExtensionTypeRep(this, "value"));
  }
  set observed(amount) {
    return $UserId_observed(__dartExtensionTypeRep(this, "value"), amount);
  }
  plus(amount) {
    return $UserId_plus(__dartExtensionTypeRep(this, "value"), amount);
  }
  "+"(amount) {
    return new UserId(__dartExtensionTypeRep($UserId__(__dartExtensionTypeRep(this, "value"), amount), "value"));
  }
  static zero() {
    return new UserId(__dartExtensionTypeRep($UserId_zero(), "value"));
  }
}

const $UserId_defaultValue = 12;
let $UserId_current = $UserId_new(1);
function $UserId_parse(text) {
  return $UserId_new(__dartIntParse(text));
}

function $UserId_zeroValue() {
  return 0;
}

function $UserId_doubled(_this) {
  return __dartAs(_this, (value) => typeof value === "number", "int") * 2;
}

function $UserId_observed(_this, amount) {
  __dartAs(_this, (value) => typeof value === "number", "int") + amount;
}

function $UserId_plus(_this, amount) {
  return __dartAs(_this, (value) => typeof value === "number", "int") + amount;
}

function $UserId__(_this, amount) {
  return $UserId_new(__dartAs(_this, (value) => typeof value === "number", "int") + amount);
}

function $UserId_new(value_1) {
  const _this = value_1;
  return _this;
}

function $UserId_zero() {
  return $UserId_new(0);
}

export function hide(value_1) {
  return value_1;
}

export function main() {
  const id = $UserId_new(5);
  const maybeId = hide(id);
  __dartPrint(`${__dartStr(__dartAs(id, (value) => typeof value === "number", "int"))} ${__dartStr($UserId_plus(id, 2))} ${__dartStr(typeof __dartExtensionTypeRep(maybeId, "value") === "number")}`);
  __dartPrint(`${__dartStr(__dartAs($UserId_parse("9"), (value) => typeof value === "number", "int"))} ${__dartStr($UserId_zeroValue())} ${__dartStr(__dartAs($UserId__(id, 3), (value) => typeof value === "number", "int"))} ${__dartStr(__dartAs($UserId_zero(), (value) => typeof value === "number", "int"))} ${__dartStr(12)} ${__dartStr(__dartAs($UserId_current, (value) => typeof value === "number", "int"))}`);
  const make = $UserId_new;
  const parse = $UserId_parse;
  const zero = $UserId_zero;
  const plus = function(amount) {
    return $UserId_plus(__dartExtensionTypeRep(id, "value"), amount);
  };
  $UserId_observed(id, 1);
  $UserId_current = $UserId__(id, 4);
  __dartPrint(`${__dartStr($UserId_doubled(id))} ${__dartStr(__dartAs(make(4), (value) => typeof value === "number", "int"))} ${__dartStr(__dartAs(parse("10"), (value) => typeof value === "number", "int"))} ${__dartStr(__dartAs(zero(), (value) => typeof value === "number", "int"))} ${__dartStr(plus(6))} ${__dartStr(__dartAs($UserId_current, (value) => typeof value === "number", "int"))}`);
}

main();
