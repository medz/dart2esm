function __dartStr(value) {
  if (value == null) return "null";
  if (Array.isArray(value)) {
    return "[" + value.map(__dartStr).join(", ") + "]";
  }
  if (value instanceof Set) {
    return "{" + Array.from(value).map(__dartStr).join(", ") + "}";
  }
  if (value instanceof Map) {
    return "{" + Array.from(value, ([key, entryValue]) => __dartStr(key) + ": " + __dartStr(entryValue)).join(", ") + "}";
  }
  if (typeof value === "object") {
    const toString = value.toString;
    if (typeof toString === "function" && toString !== Object.prototype.toString) {
      return String(toString.call(value));
    }
  }
  return String(value);
}
function __dartPrint(value) {
  console.log(__dartStr(value));
}
function __dartFormatException(message) {
  const error = new Error(String(message));
  error.name = "FormatException";
  return error;
}
function __dartIntTryParse(source, radix = null) {
  const text = String(source).trim();
  let base = radix == null ? null : Number(radix);
  if (base != null && (!Number.isInteger(base) || base < 2 || base > 36)) return null;
  if (base == null && /^[+-]?0x[0-9a-f]+$/i.test(text)) return Number.parseInt(text, 16);
  base ??= 10;
  const sign = /^[+-]/.test(text) ? text[0] : "";
  const digits = sign === "" ? text : text.slice(1);
  if (digits.length === 0) return null;
  for (const char of digits.toLowerCase()) {
    const code = char.charCodeAt(0);
    const value = code >= 48 && code <= 57 ? code - 48 : code >= 97 && code <= 122 ? code - 87 : -1;
    if (value < 0 || value >= base) return null;
  }
  return Number.parseInt(text, base);
}
function __dartIntParse(source, radix = null) {
  const value = __dartIntTryParse(source, radix);
  if (value == null) throw __dartFormatException("Invalid integer literal");
  return value;
}
function __dartDoubleTryParse(source) {
  const text = String(source).trim();
  if (text === "") return null;
  const value = Number(text);
  return Number.isNaN(value) ? null : value;
}
function __dartDoubleParse(source) {
  const value = __dartDoubleTryParse(source);
  if (value == null) throw __dartFormatException("Invalid double literal");
  return value;
}
function __dartNumTryParse(source) {
  return __dartDoubleTryParse(source);
}
function __dartNumParse(source) {
  const value = __dartNumTryParse(source);
  if (value == null) throw __dartFormatException("Invalid number literal");
  return value;
}
function __dartAs(value, test, typeName) {
  if (test(value)) return value;
  throw new TypeError("Type cast failed: expected " + typeName);
}
function __dartExtensionTypeRep(value, field) {
  if (value != null && typeof value === "object" && Object.prototype.hasOwnProperty.call(value, field)) return value[field];
  return value;
}

// Generated by dart2esm.

export class UserId {
  static get defaultValue() {
    return UserId_defaultValue;
  }
  static get current() {
    return new UserId(__dartExtensionTypeRep(UserId_current, "value"));
  }
  static set current(value) {
    return UserId_current = __dartExtensionTypeRep(value, "value");
  }
  static parse(text) {
    return new UserId(__dartExtensionTypeRep(UserId_parse(text), "value"));
  }
  static get zeroValue() {
    return UserId_zeroValue();
  }
  get doubled() {
    return UserId_get_doubled(__dartExtensionTypeRep(this, "value"));
  }
  set observed(amount) {
    return UserId_set_observed(__dartExtensionTypeRep(this, "value"), amount);
  }
  plus(amount) {
    return UserId_plus(__dartExtensionTypeRep(this, "value"), amount);
  }
  "+"(amount) {
    return new UserId(__dartExtensionTypeRep(UserId__(__dartExtensionTypeRep(this, "value"), amount), "value"));
  }
  constructor(value) {
    this.value = UserId_constructor_(value);
  }
  static zero() {
    return new UserId(__dartExtensionTypeRep(UserId_constructor_zero(), "value"));
  }
}

const UserId_defaultValue = 12;

let UserId_current = UserId_constructor_(1);

function UserId_parse(text) {
  return UserId_constructor_(__dartIntParse(text, null));
}

function UserId_zeroValue() {
  return 0;
}

function UserId_get_doubled(_this) {
  return (__dartAs(__dartExtensionTypeRep(_this, "value"), value => typeof value === "number", "int") * 2);
}

function UserId_set_observed(_this, amount) {
  (__dartAs(__dartExtensionTypeRep(_this, "value"), value => typeof value === "number", "int") + amount);
}

function UserId_plus(_this, amount) {
  return (__dartAs(__dartExtensionTypeRep(_this, "value"), value => typeof value === "number", "int") + amount);
}

function UserId_get_plus(_this) {
  return function(amount) { return UserId_plus(_this, amount); };
}

function UserId__(_this, amount) {
  return UserId_constructor_((__dartAs(__dartExtensionTypeRep(_this, "value"), value => typeof value === "number", "int") + amount));
}

function UserId_constructor_(value) {
  const _this = value;
  return _this;
}

function UserId_constructor___new_tearOff(value) {
  return UserId_constructor_(value);
}

function UserId_constructor_zero() {
  return UserId_constructor_(0);
}

function UserId_constructor___zero_tearOff() {
  return UserId_constructor_zero();
}

export function hide(value) {
  return value;
}

export function main() {
  const id = UserId_constructor_(5);
  const maybeId = hide(id);
  __dartPrint(__dartStr(__dartAs(__dartExtensionTypeRep(id, "value"), value => typeof value === "number", "int")) + " " + __dartStr(UserId_plus(id, 2)) + " " + __dartStr(typeof __dartExtensionTypeRep(maybeId, "value") === "number"));
  __dartPrint(__dartStr(__dartAs(__dartExtensionTypeRep(UserId_parse("9"), "value"), value => typeof value === "number", "int")) + " " + __dartStr(UserId_zeroValue()) + " " + __dartStr(__dartAs(__dartExtensionTypeRep(UserId__(id, 3), "value"), value => typeof value === "number", "int")) + " " + __dartStr(__dartAs(__dartExtensionTypeRep(UserId_constructor_zero(), "value"), value => typeof value === "number", "int")) + " " + __dartStr(12) + " " + __dartStr(__dartAs(UserId_current, value => typeof value === "number", "int")));
  const make = $UserId__tearoff;
  const parse = UserId_parse;
  const zero = UserId_constructor___zero_tearOff;
  const plus = UserId_get_plus(id);
  UserId_set_observed(id, 1);
  UserId_current = UserId__(id, 4);
  __dartPrint(__dartStr(UserId_get_doubled(id)) + " " + __dartStr(__dartAs((make)(4), value => typeof value === "number", "int")) + " " + __dartStr(__dartAs((parse)("10"), value => typeof value === "number", "int")) + " " + __dartStr(__dartAs((zero)(), value => typeof value === "number", "int")) + " " + __dartStr((plus)(6)) + " " + __dartStr(__dartAs(UserId_current, value => typeof value === "number", "int")));
}


function $UserId__tearoff(value) {
  return UserId_constructor___new_tearOff(value);
}
main();
