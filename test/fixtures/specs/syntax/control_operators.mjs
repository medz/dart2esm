// Generated by dart2esm.

function __dartPrint(value) {
  console.log(value);
}

export class Box {
  constructor(value) {
    this.value = value;
  }
  bump(amount) {
    this.value = this.value + amount;
    return this.value;
  }
  describe() {
    return `box:${this.value}`;
  }
}

export function describe(maybe) {
  const first = (maybe?.value ?? -1);
  const second = (maybe?.bump(2) ?? -2);
  let assigned = maybe;
  (assigned === null ? assigned = new Box(7) : null);
  const cascaded = (function() { const v = new Box(1); return (function() { v.value = 3; v.bump(4); return v; })(); })();
  const bits = ~first;
  const truth = !(second < 0);
  return `${first} ${second} ${assigned.describe()} ${cascaded.describe()} ${bits} ${truth}`;
}

export function main() {
  __dartPrint(describe(new Box(5)));
  __dartPrint(describe(null));
}

main();
