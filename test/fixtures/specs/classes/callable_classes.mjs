// Generated by dart2esm.

const __dartConstValues = new Map();
function __dartConst(key, create) {
  if (!__dartConstValues.has(key)) {
    __dartConstValues.set(key, create());
  }
  return __dartConstValues.get(key);
}

function __dartDynamicCall(receiver, positionalArguments, namedArguments = null) {
  const args = namedArguments == null ? Array.from(positionalArguments) : [...positionalArguments, namedArguments];
  if (typeof receiver === "function") return receiver(...args);
  const call = receiver?.call;
  if (typeof call === "function") return call.apply(receiver, args);
  throw new TypeError("Object is not callable");
}

function __dartPrint(value) {
  console.log(value);
}

export class Adder {
  constructor(base) {
    this.base = base;
  }
  call(value) {
    return this.base + value;
  }
}

export const add2 = __dartConst("[\"InstanceConstant\",\"InstanceConstant(const Adder{Adder.base: 2})\"]", () => Object.freeze(Object.assign(Object.create(Adder.prototype), { base: 2 })));
export function apply(fn, value) {
  return fn(value);
}

export function main() {
  const add3 = new Adder(3);
  let dynamicAdder = add3;
  let dynamicFunction = function(value) { return value * 2; };
  __dartPrint(`${__dartConst("[\"InstanceConstant\",\"InstanceConstant(const Adder{Adder.base: 2})\"]", () => Object.freeze(Object.assign(Object.create(Adder.prototype), { base: 2 }))).call(5)} ${add3.call(5)} ${apply(function() { const $receiver = __dartConst("[\"InstanceConstant\",\"InstanceConstant(const Adder{Adder.base: 2})\"]", () => Object.freeze(Object.assign(Object.create(Adder.prototype), { base: 2 }))); return function(value) { return $receiver.call(value); }; }(), 6)}`);
  __dartPrint(`${__dartDynamicCall(dynamicAdder, [4], null)} ${__dartDynamicCall(dynamicFunction, [4], null)}`);
}

main();
