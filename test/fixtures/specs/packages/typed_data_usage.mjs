// Generated by dart2esm.

function __dartCompare(left, right, compare = null) {
  if (typeof compare === "function") return Number(compare(left, right));
  const compareTo = left == null ? null : left.compareTo;
  if (typeof compareTo === "function") return Number(compareTo.call(left, right));
  return left < right ? -1 : left > right ? 1 : 0;
}

function __dartCoreError(typeName, message) {
  const text = message == null ? "" : String(message);
  const display = text === "" ? typeName : typeName + ": " + text;
  const error = new Error(text);
  error.name = typeName;
  Object.defineProperty(error, "__dartCoreErrorType", { value: typeName });
  Object.defineProperty(error, "toString", { value() { return display; } });
  return error;
}
function __dartIsCoreError(value, typeName) {
  const actual = value == null ? null : value.__dartCoreErrorType;
  if (actual != null) {
    if (actual === typeName) return true;
    if (typeName === "Exception" && actual === "FormatException") return true;
    if (typeName === "RangeError" && actual === "IndexError") return true;
    if (typeName === "ArgumentError" && (actual === "RangeError" || actual === "IndexError")) return true;
    return typeName === "Error" && actual !== "Exception" && actual !== "FormatException";
  }
  if (typeName === "TypeError" && value instanceof TypeError) return true;
  return typeName === "Error" && value instanceof Error;
}

function __dartEquals(left, right) {
  if (left === right) return true;
  if (left == null || right == null) return false;
  if ((typeof left === "number" || left.__dartType === "double") && (typeof right === "number" || right.__dartType === "double")) return Number(left) === Number(right);
  if (__dartIsRecord(left) && __dartIsRecord(right)) {
    const leftShape = left[__dartRecordShape];
    const rightShape = right[__dartRecordShape];
    if (leftShape.length !== rightShape.length) return false;
    for (let i = 0; i < leftShape.length; i++) {
      const name = leftShape[i];
      if (name !== rightShape[i]) return false;
      if (!__dartEquals(left[name], right[name])) return false;
    }
    return true;
  }
  const equals = left["=="];
  return typeof equals === "function" ? equals.call(left, right) : false;
}

function __dartIterableToArray(iterable) {
  if (Array.isArray(iterable)) return Array.from(iterable);
  if (iterable != null && typeof iterable["[]"] === "function" && typeof iterable.length === "number") {
    return Array.from({ length: Number(iterable.length) }, (_, index) => iterable["[]"](index));
  }
  return Array.from(iterable);
}

function __dartIterableTakeWhile(iterable, test) {
  const values = [];
  for (const value of __dartIterableToArray(iterable)) {
    if (!test(value)) break;
    values.push(value);
  }
  return values;
}
function __dartIterableSkipWhile(iterable, test) {
  const values = [];
  let skipping = true;
  for (const value of __dartIterableToArray(iterable)) {
    if (skipping && test(value)) continue;
    skipping = false;
    values.push(value);
  }
  return values;
}

function __dartIterator(iterable) {
  const values = __dartIterableToArray(iterable);
  let index = -1;
  return {
    current: undefined,
    moveNext() {
      index++;
      if (index < values.length) {
        this.current = values[index];
        return true;
      }
      this.current = undefined;
      return false;
    },
  };
}

function __dartLazyField(name, initialize, writable, publish = null) {
  let state = 0;
  let value;
  function get() {
    if (state === 2) return value;
    if (state === 1) {
      throw new Error("Cyclic initialization of field " + name);
    }
    if (initialize == null) {
      throw new Error("Late field " + name + " has not been initialized");
    }
    state = 1;
    try {
      value = initialize();
      if (publish) publish(value);
      state = 2;
      return value;
    } catch (error) {
      state = 0;
      throw error;
    }
  }
  function set(next) {
    if (writable === false || (writable === "once" && state === 2)) {
      throw new TypeError("Cannot assign to final field " + name);
    }
    value = next;
    if (publish) publish(value);
    state = 2;
    return next;
  }
  return { get, set };
}

function __dartListAdd(list, value) {
  if (Array.isArray(list)) {
    list.push(value);
  } else if (list != null && typeof list.add === "function") {
    list.add(value);
  } else {
    const index = list.length;
    list.length = index + 1;
    __dartListLikeSet(list, index, value);
  }
  return null;
}

function __dartListAddAll(list, values) {
  for (const value of __dartIterableToArray(values)) {
    __dartListAdd(list, value);
  }
  return null;
}

function __dartFixedList(values) {
  const list = __dartIterableToArray(values);
  Object.preventExtensions(list);
  return list;
}
function __dartListOf(values, growable = true) {
  const list = __dartIterableToArray(values);
  return growable ? list : __dartFixedList(list);
}
function __dartListFilled(length, fill, growable = false) {
  const list = Array(Number(length)).fill(fill);
  return growable ? list : __dartFixedList(list);
}
function __dartListGenerate(length, generator, growable = true) {
  const list = Array.from({ length: Number(length) }, (_, index) => generator(index));
  return growable ? list : __dartFixedList(list);
}
function __dartUnmodifiableList(values) {
  return Object.freeze(__dartIterableToArray(values));
}

function __dartListLikeGet(list, index) {
  if (Array.isArray(list) || ArrayBuffer.isView(list) || typeof list === "string") return list[index];
  const op = list == null ? null : list["[]"];
  return typeof op === "function" ? op.call(list, index) : list[index];
}
function __dartListLikeSet(list, index, value) {
  if (Array.isArray(list) || ArrayBuffer.isView(list)) {
    list[index] = value;
    return value;
  }
  const op = list == null ? null : list["[]="];
  if (typeof op === "function") return op.call(list, index, value);
  list[index] = value;
  return value;
}
function __dartListMixinFirst(list) {
  return __dartListLikeGet(list, 0);
}
function __dartListMixinLast(list) {
  return __dartListLikeGet(list, list.length - 1);
}
function __dartListMixinSingle(list) {
  if (list.length !== 1) throw __dartCoreError("StateError", "Too many elements");
  return __dartListLikeGet(list, 0);
}
function __dartListMixinInsert(list, index, value) {
  index = Number(index);
  const length = Number(list.length);
  list.length = length + 1;
  for (let i = length; i > index; i--) {
    __dartListLikeSet(list, i, __dartListLikeGet(list, i - 1));
  }
  __dartListLikeSet(list, index, value);
  return null;
}

function __dartListMutationRead(list, index) {
  return list != null && typeof list["[]"] === "function" ? list["[]"](index) : list[index];
}
function __dartListMutationWrite(list, index, value) {
  if (list != null && typeof list["[]="] === "function") {
    list["[]="](index, value);
  } else {
    list[index] = value;
  }
}
function __dartListMutationValues(values) {
  return __dartIterableToArray(values);
}
function __dartListShuffle(list, random = null) {
  for (let index = list.length - 1; index > 0; index--) {
    const nextInt = random == null ? Math.floor(Math.random() * (index + 1)) : Number(random.nextInt(index + 1));
    const current = __dartListMutationRead(list, index);
    __dartListMutationWrite(list, index, __dartListMutationRead(list, nextInt));
    __dartListMutationWrite(list, nextInt, current);
  }
  return null;
}
function __dartListRemoveAt(list, index) {
  index = Number(index);
  const value = __dartListMutationRead(list, index);
  __dartListRemoveRange(list, index, index + 1);
  return value;
}
function __dartListInsert(list, index, value) {
  index = Number(index);
  if (Array.isArray(list)) {
    list.splice(index, 0, value);
    return null;
  }
  const length = Number(list.length);
  list.length = length + 1;
  for (let cursor = length; cursor > index; cursor--) {
    __dartListMutationWrite(list, cursor, __dartListMutationRead(list, cursor - 1));
  }
  __dartListMutationWrite(list, index, value);
  return null;
}
function __dartListRemove(list, value) {
  for (let index = 0; index < list.length; index++) {
    if (__dartEquals(__dartListMutationRead(list, index), value)) {
      __dartListRemoveRange(list, index, index + 1);
      return true;
    }
  }
  return false;
}
function __dartListRemoveLast(list) {
  return __dartListRemoveAt(list, list.length - 1);
}
function __dartListInsertAll(list, index, values) {
  index = Number(index);
  const inserted = __dartListMutationValues(values);
  if (Array.isArray(list)) {
    list.splice(index, 0, ...inserted);
    return null;
  }
  const length = Number(list.length);
  list.length = length + inserted.length;
  for (let cursor = length - 1; cursor >= index; cursor--) {
    __dartListMutationWrite(list, cursor + inserted.length, __dartListMutationRead(list, cursor));
  }
  for (let cursor = 0; cursor < inserted.length; cursor++) {
    __dartListMutationWrite(list, index + cursor, inserted[cursor]);
  }
  return null;
}
function __dartListSetAll(list, index, values) {
  let cursor = Number(index);
  for (const value of __dartListMutationValues(values)) {
    __dartListMutationWrite(list, cursor++, value);
  }
  return null;
}
function __dartListFillRange(list, start, end, fill = null) {
  for (let index = Number(start); index < Number(end); index++) {
    __dartListMutationWrite(list, index, fill);
  }
  return null;
}
function __dartListReplaceRange(list, start, end, values) {
  start = Number(start);
  end = Number(end);
  const replacement = __dartListMutationValues(values);
  __dartListRemoveRange(list, start, end);
  __dartListInsertAll(list, start, replacement);
  return null;
}
function __dartListRemoveRange(list, start, end) {
  start = Number(start);
  end = Number(end);
  if (Array.isArray(list)) {
    list.splice(start, end - start);
    return null;
  }
  const length = Number(list.length);
  const count = end - start;
  for (let cursor = end; cursor < length; cursor++) {
    __dartListMutationWrite(list, cursor - count, __dartListMutationRead(list, cursor));
  }
  list.length = length - count;
  return null;
}
function __dartListRemoveWhere(list, test) {
  for (let index = list.length - 1; index >= 0; index--) {
    if (test(__dartListMutationRead(list, index))) {
      __dartListRemoveRange(list, index, index + 1);
    }
  }
  return null;
}
function __dartListRetainWhere(list, test) {
  for (let index = list.length - 1; index >= 0; index--) {
    if (!test(__dartListMutationRead(list, index))) {
      __dartListRemoveRange(list, index, index + 1);
    }
  }
  return null;
}
function __dartListAsMap(list) {
  const map = new Map();
  for (let index = 0; index < list.length; index++) map.set(index, __dartListMutationRead(list, index));
  return map;
}

function __dartListRangeRead(list, index) {
  return list != null && typeof list["[]"] === "function" ? list["[]"](index) : list[index];
}
function __dartListRangeWrite(list, index, value) {
  if (list != null && typeof list["[]="] === "function") {
    list["[]="](index, value);
  } else {
    list[index] = value;
  }
}
function __dartListRangeValues(source, start = 0, end = null) {
  const from = Number(start);
  return __dartIterableToArray(source).slice(from, end == null ? undefined : Number(end));
}
function __dartListCopyRange(target, at, source, start = 0, end = null) {
  const values = __dartListRangeValues(source, start, end);
  let index = Number(at);
  for (const value of values) __dartListRangeWrite(target, index++, value);
  return null;
}
function __dartListWriteIterable(target, at, source) {
  let index = Number(at);
  for (const value of __dartListRangeValues(source)) {
    __dartListRangeWrite(target, index++, value);
  }
  return null;
}
function __dartListSetRange(target, start, end, source, skipCount = 0) {
  const from = Number(skipCount);
  const count = Number(end) - Number(start);
  const values = __dartListRangeValues(source, from, from + count);
  let index = Number(start);
  for (const value of values) __dartListRangeWrite(target, index++, value);
  return null;
}

function __dartRandom(seed = null, secure = false) {
  let state = seed == null ? 0 : Number(seed) >>> 0;
  function nextUint32() {
    if (secure) {
      const crypto = globalThis.crypto || globalThis.msCrypto;
      if (crypto && typeof crypto.getRandomValues === "function") {
        const values = new Uint32Array(1);
        crypto.getRandomValues(values);
        return values[0] >>> 0;
      }
    }
    if (seed == null) {
      return Math.floor(Math.random() * 0x100000000) >>> 0;
    }
    state = (Math.imul(state, 1664525) + 1013904223) >>> 0;
    return state;
  }
  return {
    nextInt(max) {
      if (!Number.isInteger(max) || max <= 0) throw new RangeError("max must be positive");
      return nextUint32() % max;
    },
    nextDouble() { return nextUint32() / 0x100000000; },
    nextBool() { return (nextUint32() & 1) === 1; },
  };
}

function __dartNullCheck(value) {
  if (value == null) throw new TypeError("Null check operator used on a null value");
  return value;
}

function __dartPrint(value) {
  console.log(__dartStr(value));
}

function __dartCheckValueInInterval(value, minValue, maxValue, name = null, message = null) {
  if (value < minValue || value > maxValue) throw __dartCoreError("RangeError", message ?? (String(name ?? "value") + " out of range"));
  return value;
}
function __dartCheckValidIndex(index, indexable, name = null, length = null, message = null) {
  length ??= indexable.length;
  if (index < 0 || index >= length) throw __dartCoreError("RangeError", message ?? (String(name ?? "index") + " out of range"));
  return index;
}
function __dartCheckValidRange(start, end, length, startName = null, endName = null, message = null) {
  if (start < 0 || start > length) throw __dartCoreError("RangeError", message ?? (String(startName ?? "start") + " out of range"));
  if (end == null) return length;
  if (end < start || end > length) throw __dartCoreError("RangeError", message ?? (String(endName ?? "end") + " out of range"));
  return end;
}
function __dartCheckNotNegative(value, name = null, message = null) {
  if (value < 0) throw __dartCoreError("RangeError", message ?? (String(name ?? "index") + " must not be negative"));
  return value;
}

const __dartRecordShape = Symbol("dart.recordShape");

function __dartIsRecord(value) {
  return value != null && typeof value === "object" && Array.isArray(value[__dartRecordShape]);
}

function __dartSetContains(set, needle) {
  if (typeof set.__dartSplayIsValidKey === "function" && !set.__dartSplayIsValidKey(needle)) return false;
  if (set.__dartSplayCompare !== undefined) {
    for (const value of set) {
      if (__dartCompare(value, needle, set.__dartSplayCompare) === 0) return true;
    }
    return false;
  }
  if (!set.__dartEqualitySet) return set.has(needle);
  for (const value of set) {
    if (__dartEquals(value, needle)) return true;
  }
  return false;
}
function __dartSetAdd(set, value) {
  if (__dartSetContains(set, value)) return false;
  set.add(value);
  if (set.__dartSplayCompare !== undefined) __dartSplaySortSet(set);
  return true;
}
function __dartSetFrom(values) {
  const set = new Set();
  Object.defineProperty(set, "__dartEqualitySet", { value: true });
  for (const value of values) __dartSetAdd(set, value);
  return set;
}
function __dartSetAddAll(set, values) {
  for (const value of values) __dartSetAdd(set, value);
  return null;
}

function __dartStringBuffer(initial = "") {
  let value = initial == null ? "" : __dartStr(initial);
  return {
    write(next) {
      value += __dartStr(next);
      return null;
    },
    writeln(next = "") {
      value += __dartStr(next) + "\n";
      return null;
    },
    writeAll(values, separator = "") {
      const parts = __dartIterableToArray(values).map((item) => __dartStr(item));
      value += parts.join(__dartStr(separator));
      return null;
    },
    writeCharCode(charCode) {
      value += String.fromCodePoint(Number(charCode));
      return null;
    },
    clear() {
      value = "";
      return null;
    },
    toString() {
      return value;
    },
    get length() {
      return value.length;
    },
    get isEmpty() {
      return value.length === 0;
    },
    get isNotEmpty() {
      return value.length !== 0;
    },
  };
}

function __dartStr(value) {
  if (value == null) return "null";
  if (Array.isArray(value)) {
    return "[" + value.map(__dartStr).join(", ") + "]";
  }
  if (value instanceof Set) {
    return "{" + Array.from(value).map(__dartStr).join(", ") + "}";
  }
  if (value instanceof Map) {
    return "{" + Array.from(value, ([key, entryValue]) => __dartStr(key) + ": " + __dartStr(entryValue)).join(", ") + "}";
  }
  if (typeof value === "object") {
    const toString = value.toString;
    if (typeof toString === "function" && toString !== Object.prototype.toString) {
      return String(toString.call(value));
    }
  }
  return String(value);
}

const __dartTypeCache = new Map();
function __dartType(name) {
  if (__dartTypeCache.has(name)) return __dartTypeCache.get(name);
  const value = Object.freeze({
    name,
    toString() { return name; },
  });
  __dartTypeCache.set(name, value);
  return value;
}

function __dartAs(value, test, typeName) {
  if (test(value)) {
    return value;
  }
  throw new TypeError("Type cast failed: expected " + typeName);
}

const $QueueList_interface = Symbol("QueueList");
class QueueList {
  constructor(initialCapacity = null) {
    return QueueList._init_package_collection_src_queue_list_dart.call(new.target, QueueList._computeInitialCapacity_package_collection_src_queue_list_dart(initialCapacity));
  }
  static _init_package_collection_src_queue_list_dart(initialCapacity) {
    const $self = Object.create(this.prototype);
    Object.defineProperty($self, "_table_package_collection_src_queue_list_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_head_package_collection_src_queue_list_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_tail_package_collection_src_queue_list_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_table_package_collection_src_queue_list_dart", { value: Array(initialCapacity).fill(null), writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_head_package_collection_src_queue_list_dart", { value: 0, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_tail_package_collection_src_queue_list_dart", { value: 0, writable: true, enumerable: true, configurable: true });
    return $self;
  }
  static __package_collection_src_queue_list_dart(_head, _tail, _table) {
    const $self = Object.create(this.prototype);
    Object.defineProperty($self, "_table_package_collection_src_queue_list_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_head_package_collection_src_queue_list_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_tail_package_collection_src_queue_list_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_head_package_collection_src_queue_list_dart", { value: _head, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_tail_package_collection_src_queue_list_dart", { value: _tail, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_table_package_collection_src_queue_list_dart", { value: _table, writable: true, enumerable: true, configurable: true });
    return $self;
  }
  static _castFrom_package_collection_src_queue_list_dart(source) {
    return new _CastQueueList(source);
  }
  static from(source) {
    if ((Array.isArray(source) || (ArrayBuffer.isView(source) && !(source instanceof DataView)))) {
      let length = __dartIterableToArray(source).length;
      let queue = new QueueList(length + 1);
      let sourceList = source;
      __dartListSetRange(queue._table_package_collection_src_queue_list_dart, 0, length, sourceList, 0);
      queue._tail_package_collection_src_queue_list_dart = length;
      return queue;
    } else {
      return (() => {
        const v = new QueueList();
        return (() => {
          v.addAll_1(source);
          return v;
        })();
      })();
    }
  }
  static _computeInitialCapacity_package_collection_src_queue_list_dart(initialCapacity) {
    if ((initialCapacity === null || initialCapacity < 8)) {
      return 8;
    }
    initialCapacity = initialCapacity + 1;
    if (QueueList._isPowerOf2_package_collection_src_queue_list_dart(initialCapacity)) {
      return initialCapacity;
    }
    return QueueList._nextPowerOf2_package_collection_src_queue_list_dart(initialCapacity);
  }
  static _isPowerOf2_package_collection_src_queue_list_dart(number) {
    return __dartEquals(number & number - 1, 0);
  }
  static _nextPowerOf2_package_collection_src_queue_list_dart(number) {
    number = (number << 1) - 1;
    for (; ; ) {
      let nextNumber = number & number - 1;
      if (__dartEquals(nextNumber, 0)) {
        return number;
      }
      number = nextNumber;
    }
  }
  add(element) {
    __dartListLikeSet(this, (() => {
      const v = this.length;
      return (() => {
        const v_1 = this.length = v + 1;
        return v;
      })();
    })(), element);
  }
  addAll(iterable) {
    let i = this.length;
    let _sync_for_iterator = __dartIterator(iterable);
    for (; _sync_for_iterator.moveNext(); ) {
      let element = _sync_for_iterator.current;
      __dartListAdd(this, element);
      i = i + 1;
    }
  }
  cast() {
    return this;
  }
  toString() {
    return `[${__dartIterableToArray(this).map((value) => __dartStr(value)).join(", ")}]`;
  }
  removeLast() {
    if (__dartEquals(this.length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    let result = __dartListLikeGet(this, this.length - 1);
    this.length = this.length - 1;
    return result;
  }
  get first() {
    if (__dartEquals(this.length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    return __dartListLikeGet(this, 0);
  }
  set first(value) {
    if (__dartEquals(this.length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    __dartListLikeSet(this, 0, value);
  }
  get last() {
    if (__dartEquals(this.length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    return __dartListLikeGet(this, this.length - 1);
  }
  set last(value) {
    if (__dartEquals(this.length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    __dartListLikeSet(this, this.length - 1, value);
  }
  get iterator() {
    return __dartIterator(this);
  }
  elementAt(index) {
    return __dartListLikeGet(this, index);
  }
  followedBy(other) {
    return __dartIterableToArray(this).concat(__dartIterableToArray(other));
  }
  forEach(action) {
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      action(__dartListLikeGet(this, i));
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
  }
  get isEmpty() {
    return __dartEquals(this.length, 0);
  }
  get isNotEmpty() {
    return !(this.length === 0);
  }
  get single() {
    if (__dartEquals(this.length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    if (this.length > 1) {
      throw __dartCoreError("StateError", "Too many elements");
    }
    return __dartListLikeGet(this, 0);
  }
  contains(element) {
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      if (__dartEquals(__dartListLikeGet(this, i), element)) {
        return true;
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    return false;
  }
  every(test) {
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      if (!(test(__dartListLikeGet(this, i)))) {
        return false;
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    return true;
  }
  any(test) {
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      if (test(__dartListLikeGet(this, i))) {
        return true;
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    return false;
  }
  firstWhere(test, { orElse = null } = {}) {
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      let element = __dartListLikeGet(this, i);
      if (test(element)) {
        return element;
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    if (!(orElse === null)) {
      return orElse();
    }
    throw __dartCoreError("StateError", "No element");
  }
  lastWhere(test, { orElse = null } = {}) {
    let length = this.length;
    for (let i = length - 1; i >= 0; i = i - 1) {
      let element = __dartListLikeGet(this, i);
      if (test(element)) {
        return element;
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    if (!(orElse === null)) {
      return orElse();
    }
    throw __dartCoreError("StateError", "No element");
  }
  singleWhere(test, { orElse = null } = {}) {
    let length = this.length;
    const match = __dartLazyField("match", null, true);
    let matchFound = false;
    for (let i = 0; i < length; i = i + 1) {
      let element = __dartListLikeGet(this, i);
      if (test(element)) {
        if (matchFound) {
          throw __dartCoreError("StateError", "Too many elements");
        }
        matchFound = true;
        match.set(element);
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    if (matchFound) {
      return match.get();
    }
    if (!(orElse === null)) {
      return orElse();
    }
    throw __dartCoreError("StateError", "No element");
  }
  join(separator = "") {
    if (__dartEquals(this.length, 0)) {
      return "";
    }
    let buffer = (() => {
      const v = __dartStringBuffer("");
      return (() => {
        v.writeAll(this, separator);
        return v;
      })();
    })();
    return buffer.toString();
  }
  where(test) {
    return __dartIterableToArray(this).filter(test);
  }
  whereType() {
    return __dartIterableToArray(this).filter((value) => true);
  }
  map(f) {
    return Array.from(this, f);
  }
  expand(f) {
    return __dartIterableToArray(this).flatMap((value) => __dartIterableToArray((f)(value)));
  }
  reduce(combine) {
    let length = this.length;
    if (__dartEquals(length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    let value = __dartListLikeGet(this, 0);
    for (let i = 1; i < length; i = i + 1) {
      value = combine(value, __dartListLikeGet(this, i));
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    return value;
  }
  fold(initialValue, combine) {
    let value = initialValue;
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      value = combine(value, __dartListLikeGet(this, i));
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    return value;
  }
  skip(count) {
    return __dartIterableToArray(this).slice(count, null);
  }
  skipWhile(test) {
    return __dartIterableSkipWhile(this, test);
  }
  take(count) {
    return __dartIterableToArray(this).slice(0, __dartNullCheck(count));
  }
  takeWhile(test) {
    return __dartIterableTakeWhile(this, test);
  }
  toList({ growable = true } = {}) {
    if (this.length === 0) {
      return __dartListOf([], growable);
    }
    let first = __dartListLikeGet(this, 0);
    let result = __dartListFilled(this.length, first, growable);
    for (let i = 1; i < this.length; i = i + 1) {
      __dartListLikeSet(result, i, __dartListLikeGet(this, i));
    }
    return result;
  }
  toSet() {
    let result = __dartSetFrom([]);
    for (let i = 0; i < this.length; i = i + 1) {
      __dartSetAdd(result, __dartListLikeGet(this, i));
    }
    return result;
  }
  remove(element) {
    for (let i = 0; i < this.length; i = i + 1) {
      if (__dartEquals(__dartListLikeGet(this, i), element)) {
        __dartListRemoveRange(this, i, i + 1);
        return true;
      }
    }
    return false;
  }
  _closeGap_dart_collection(start, end) {
    let length = this.length;
    let size = end - start;
    for (let i = end; i < length; i = i + 1) {
      __dartListLikeSet(this, i - size, __dartListLikeGet(this, i));
    }
    this.length = length - size;
  }
  removeWhere(test) {
    __dartListRemoveWhere(this, test);
  }
  retainWhere(test) {
    __dartListRetainWhere(this, test);
  }
  _filter_dart_collection(test, retainMatching) {
    let retained = Array(0).fill(null);
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      let element = __dartListLikeGet(this, i);
      if (__dartEquals(test(element), retainMatching)) {
        __dartListAdd(retained, element);
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    if (!(__dartEquals(retained.length, this.length))) {
      __dartListSetRange(this, 0, retained.length, retained);
      this.length = retained.length;
    }
  }
  clear() {
    this.length = 0;
  }
  sort(compare = null) {
    this.sort((left, right) => __dartCompare(left, right, (compare ?? ((left, right) => __dartCompare(left, right)))));
  }
  shuffle(random = null) {
    (random === null ? random = __dartRandom(null, false) : null);
    let length = this.length;
    while (length > 1) {
      let pos = random.nextInt(length);
      length = length - 1;
      let tmp = __dartListLikeGet(this, length);
      __dartListLikeSet(this, length, __dartListLikeGet(this, pos));
      __dartListLikeSet(this, pos, tmp);
    }
  }
  asMap() {
    return __dartListAsMap(this);
  }
  "+"(other) {
    return (() => {
      const v = __dartListOf(this);
      __dartListAddAll(v, other);
      return v;
    })();
  }
  sublist(start, end = null) {
    let listLength = this.length;
    (end === null ? end = listLength : null);
    __dartCheckValidRange(start, end, listLength, null, null, null);
    return __dartListOf(__dartIterableToArray(this).slice(start, end));
  }
  getRange(start, end) {
    __dartCheckValidRange(start, end, this.length, null, null, null);
    return __dartIterableToArray(this).slice(start, end);
  }
  removeRange(start, end) {
    __dartCheckValidRange(start, end, this.length, null, null, null);
    if (end > start) {
      __dartListRemoveRange(this, start, end);
    }
  }
  fillRange(start, end, fill = null) {
    let value = (() => {
      let v = fill;
      return (v === null ? (() => {
        let v_1 = v;
        return (v_1 === null ? __dartAs(v_1, (value) => true, "TypeParameterType(_QueueList&Object&ListMixin.E%)") : v_1);
      })() : v);
    })();
    __dartCheckValidRange(start, end, this.length, null, null, null);
    for (let i = start; i < end; i = i + 1) {
      __dartListLikeSet(this, i, value);
    }
  }
  setRange(start, end, iterable, skipCount = 0) {
    __dartCheckValidRange(start, end, this.length, null, null, null);
    let length = end - start;
    if (__dartEquals(length, 0)) {
      return;
    }
    __dartCheckNotNegative(skipCount, "skipCount", null);
    let otherList = null;
    let otherStart = null;
    if ((Array.isArray(iterable) || (ArrayBuffer.isView(iterable) && !(iterable instanceof DataView)))) {
      otherList = iterable;
      otherStart = skipCount;
    } else {
      otherList = __dartListOf(__dartIterableToArray(iterable).slice(skipCount), false);
      otherStart = 0;
    }
    if (otherStart + length > otherList.length) {
      throw __dartCoreError("StateError", "Too few elements");
    }
    if (otherStart < start) {
      for (let i = length - 1; i >= 0; i = i - 1) {
        __dartListLikeSet(this, start + i, __dartListLikeGet(otherList, otherStart + i));
      }
    } else {
      for (let i_1 = 0; i_1 < length; i_1 = i_1 + 1) {
        __dartListLikeSet(this, start + i_1, __dartListLikeGet(otherList, otherStart + i_1));
      }
    }
  }
  replaceRange(start, end, newContents) {
    __dartCheckValidRange(start, end, this.length, null, null, null);
    if (__dartEquals(start, this.length)) {
      __dartListAddAll(this, newContents);
      return;
    }
    if (!(((newContents != null && typeof newContents !== "string") && typeof newContents.length === "number"))) {
      newContents = __dartListOf(newContents, true);
    }
    let removeLength = end - start;
    let insertLength = __dartIterableToArray(newContents).length;
    if (removeLength >= insertLength) {
      let insertEnd = start + insertLength;
      __dartListSetRange(this, start, insertEnd, newContents);
      if (removeLength > insertLength) {
        __dartListRemoveRange(this, insertEnd, end);
      }
    } else {
      if (__dartEquals(end, this.length)) {
        let i = start;
        let _sync_for_iterator = __dartIterator(newContents);
        for (; _sync_for_iterator.moveNext(); ) {
          let element = _sync_for_iterator.current;
          if (i < end) {
            __dartListLikeSet(this, i, element);
          } else {
            __dartListAdd(this, element);
          }
          i = i + 1;
        }
      } else {
        let delta = insertLength - removeLength;
        let oldLength = this.length;
        let insertEnd_1 = start + insertLength;
        for (let i_1 = oldLength - delta; i_1 < oldLength; i_1 = i_1 + 1) {
          __dartListAdd(this, __dartListLikeGet(this, (i_1 > 0 ? i_1 : 0)));
        }
        if (insertEnd_1 < oldLength) {
          __dartListSetRange(this, insertEnd_1, oldLength, this, end);
        }
        __dartListSetRange(this, start, insertEnd_1, newContents);
      }
    }
  }
  indexOf(element, start = 0) {
    if (start < 0) {
      start = 0;
    }
    for (let i = start; i < this.length; i = i + 1) {
      if (__dartEquals(__dartListLikeGet(this, i), element)) {
        return i;
      }
    }
    return -1;
  }
  indexWhere(test, start = 0) {
    if (start < 0) {
      start = 0;
    }
    for (let i = start; i < this.length; i = i + 1) {
      if (test(__dartListLikeGet(this, i))) {
        return i;
      }
    }
    return -1;
  }
  lastIndexOf(element, start = null) {
    if ((start === null || start >= this.length)) {
      start = this.length - 1;
    }
    for (let i = start; i >= 0; i = i - 1) {
      if (__dartEquals(__dartListLikeGet(this, i), element)) {
        return i;
      }
    }
    return -1;
  }
  lastIndexWhere(test, start = null) {
    if ((start === null || start >= this.length)) {
      start = this.length - 1;
    }
    for (let i = start; i >= 0; i = i - 1) {
      if (test(__dartListLikeGet(this, i))) {
        return i;
      }
    }
    return -1;
  }
  insert(index, element) {
    __dartNullCheck(index);
    let length = this.length;
    __dartCheckValueInInterval(index, 0, length, "index", null);
    __dartListAdd(this, element);
    if (!(__dartEquals(index, length))) {
      __dartListSetRange(this, index + 1, length + 1, this, index);
      __dartListLikeSet(this, index, element);
    }
  }
  removeAt(index) {
    let result = __dartListLikeGet(this, index);
    __dartListRemoveRange(this, index, index + 1);
    return result;
  }
  insertAll(index, iterable) {
    __dartCheckValueInInterval(index, 0, this.length, "index", null);
    if (__dartEquals(index, this.length)) {
      __dartListAddAll(this, iterable);
      return;
    }
    if ((!(((iterable != null && typeof iterable !== "string") && typeof iterable.length === "number")) || Object.is(iterable, this))) {
      iterable = __dartListOf(iterable, true);
    }
    let insertionLength = __dartIterableToArray(iterable).length;
    if (__dartEquals(insertionLength, 0)) {
      return;
    }
    let oldLength = this.length;
    for (let i = oldLength - insertionLength; i < oldLength; i = i + 1) {
      __dartListAdd(this, __dartListLikeGet(this, (i > 0 ? i : 0)));
    }
    if (!(__dartEquals(__dartIterableToArray(iterable).length, insertionLength))) {
      this.length = this.length - insertionLength;
      throw __dartCoreError("ConcurrentModificationError", iterable);
    }
    let oldCopyStart = index + insertionLength;
    if (oldCopyStart < oldLength) {
      __dartListSetRange(this, oldCopyStart, oldLength, this, index);
    }
    __dartListSetAll(this, index, iterable);
  }
  setAll(index, iterable) {
    if ((Array.isArray(iterable) || (ArrayBuffer.isView(iterable) && !(iterable instanceof DataView)))) {
      __dartListSetRange(this, index, index + __dartIterableToArray(iterable).length, iterable);
    } else {
      let _sync_for_iterator = __dartIterator(iterable);
      for (; _sync_for_iterator.moveNext(); ) {
        let element = _sync_for_iterator.current;
        __dartListLikeSet(this, (() => {
          const v = index;
          return (() => {
            const v_1 = index = v + 1;
            return v;
          })();
        })(), element);
      }
    }
  }
  get reversed() {
    return __dartIterableToArray(this).reverse();
  }
  add_1(element) {
    this._add_package_collection_src_queue_list_dart(element);
  }
  addAll_1(iterable) {
    if ((Array.isArray(iterable) || (ArrayBuffer.isView(iterable) && !(iterable instanceof DataView)))) {
      let list = iterable;
      let addCount = __dartIterableToArray(list).length;
      let length = this.length;
      if (length + addCount >= this._table_package_collection_src_queue_list_dart.length) {
        this._preGrow_package_collection_src_queue_list_dart(length + addCount);
        __dartListSetRange(this._table_package_collection_src_queue_list_dart, length, length + addCount, list, 0);
        this._tail_package_collection_src_queue_list_dart = this._tail_package_collection_src_queue_list_dart + addCount;
      } else {
        let endSpace = this._table_package_collection_src_queue_list_dart.length - this._tail_package_collection_src_queue_list_dart;
        if (addCount < endSpace) {
          __dartListSetRange(this._table_package_collection_src_queue_list_dart, this._tail_package_collection_src_queue_list_dart, this._tail_package_collection_src_queue_list_dart + addCount, list, 0);
          this._tail_package_collection_src_queue_list_dart = this._tail_package_collection_src_queue_list_dart + addCount;
        } else {
          let preSpace = addCount - endSpace;
          __dartListSetRange(this._table_package_collection_src_queue_list_dart, this._tail_package_collection_src_queue_list_dart, this._tail_package_collection_src_queue_list_dart + endSpace, list, 0);
          __dartListSetRange(this._table_package_collection_src_queue_list_dart, 0, preSpace, list, endSpace);
          this._tail_package_collection_src_queue_list_dart = preSpace;
        }
      }
    } else {
      let _sync_for_iterator = __dartIterator(iterable);
      for (; _sync_for_iterator.moveNext(); ) {
        let element = _sync_for_iterator.current;
        this._add_package_collection_src_queue_list_dart(element);
      }
    }
  }
  cast_1() {
    return QueueList._castFrom_package_collection_src_queue_list_dart(this);
  }
  retype() {
    return this.cast_1();
  }
  toString_1() {
    return `{${__dartIterableToArray(this).map((value) => __dartStr(value)).join(", ")}}`;
  }
  addLast(element) {
    this._add_package_collection_src_queue_list_dart(element);
  }
  addFirst(element) {
    this._head_package_collection_src_queue_list_dart = this._head_package_collection_src_queue_list_dart - 1 & this._table_package_collection_src_queue_list_dart.length - 1;
    __dartListLikeSet(this._table_package_collection_src_queue_list_dart, this._head_package_collection_src_queue_list_dart, element);
    if (__dartEquals(this._head_package_collection_src_queue_list_dart, this._tail_package_collection_src_queue_list_dart)) {
      this._grow_package_collection_src_queue_list_dart();
    }
  }
  removeFirst() {
    if (__dartEquals(this._head_package_collection_src_queue_list_dart, this._tail_package_collection_src_queue_list_dart)) {
      throw __dartCoreError("StateError", "No element");
    }
    let result = (() => {
      let v = __dartListLikeGet(this._table_package_collection_src_queue_list_dart, this._head_package_collection_src_queue_list_dart);
      return (v === null ? __dartAs(v, (value) => true, "TypeParameterType(QueueList.E%)") : v);
    })();
    __dartListLikeSet(this._table_package_collection_src_queue_list_dart, this._head_package_collection_src_queue_list_dart, null);
    this._head_package_collection_src_queue_list_dart = this._head_package_collection_src_queue_list_dart + 1 & this._table_package_collection_src_queue_list_dart.length - 1;
    return result;
  }
  removeLast_1() {
    if (__dartEquals(this._head_package_collection_src_queue_list_dart, this._tail_package_collection_src_queue_list_dart)) {
      throw __dartCoreError("StateError", "No element");
    }
    this._tail_package_collection_src_queue_list_dart = this._tail_package_collection_src_queue_list_dart - 1 & this._table_package_collection_src_queue_list_dart.length - 1;
    let result = (() => {
      let v = __dartListLikeGet(this._table_package_collection_src_queue_list_dart, this._tail_package_collection_src_queue_list_dart);
      return (v === null ? __dartAs(v, (value) => true, "TypeParameterType(QueueList.E%)") : v);
    })();
    __dartListLikeSet(this._table_package_collection_src_queue_list_dart, this._tail_package_collection_src_queue_list_dart, null);
    return result;
  }
  get length() {
    return this._tail_package_collection_src_queue_list_dart - this._head_package_collection_src_queue_list_dart & this._table_package_collection_src_queue_list_dart.length - 1;
  }
  set length(value) {
    if (value < 0) {
      throw __dartCoreError("RangeError", `Length ${__dartStr(value)} may not be negative.`);
    }
    if ((value > this.length && !(true))) {
      throw __dartCoreError("UnsupportedError", `The length can only be increased when the element type is nullable, but the current element type is \`${__dartStr(__dartType("E"))}\`.`);
    }
    let delta = value - this.length;
    if (delta >= 0) {
      if (this._table_package_collection_src_queue_list_dart.length <= value) {
        this._preGrow_package_collection_src_queue_list_dart(value);
      }
      this._tail_package_collection_src_queue_list_dart = this._tail_package_collection_src_queue_list_dart + delta & this._table_package_collection_src_queue_list_dart.length - 1;
      return;
    }
    let newTail = this._tail_package_collection_src_queue_list_dart + delta;
    if (newTail >= 0) {
      __dartListFillRange(this._table_package_collection_src_queue_list_dart, newTail, this._tail_package_collection_src_queue_list_dart, null);
    } else {
      newTail = newTail + this._table_package_collection_src_queue_list_dart.length;
      __dartListFillRange(this._table_package_collection_src_queue_list_dart, 0, this._tail_package_collection_src_queue_list_dart, null);
      __dartListFillRange(this._table_package_collection_src_queue_list_dart, newTail, this._table_package_collection_src_queue_list_dart.length, null);
    }
    this._tail_package_collection_src_queue_list_dart = newTail;
  }
  "[]"(index) {
    if ((index < 0 || index >= this.length)) {
      throw __dartCoreError("RangeError", `Index ${__dartStr(index)} must be in the range [0..${__dartStr(this.length)}).`);
    }
    return (() => {
      let v = __dartListLikeGet(this._table_package_collection_src_queue_list_dart, this._head_package_collection_src_queue_list_dart + index & this._table_package_collection_src_queue_list_dart.length - 1);
      return (v === null ? __dartAs(v, (value) => true, "TypeParameterType(QueueList.E%)") : v);
    })();
  }
  "[]="(index, value) {
    if ((index < 0 || index >= this.length)) {
      throw __dartCoreError("RangeError", `Index ${__dartStr(index)} must be in the range [0..${__dartStr(this.length)}).`);
    }
    __dartListLikeSet(this._table_package_collection_src_queue_list_dart, this._head_package_collection_src_queue_list_dart + index & this._table_package_collection_src_queue_list_dart.length - 1, value);
  }
  _add_package_collection_src_queue_list_dart(element) {
    __dartListLikeSet(this._table_package_collection_src_queue_list_dart, this._tail_package_collection_src_queue_list_dart, element);
    this._tail_package_collection_src_queue_list_dart = this._tail_package_collection_src_queue_list_dart + 1 & this._table_package_collection_src_queue_list_dart.length - 1;
    if (__dartEquals(this._head_package_collection_src_queue_list_dart, this._tail_package_collection_src_queue_list_dart)) {
      this._grow_package_collection_src_queue_list_dart();
    }
  }
  _grow_package_collection_src_queue_list_dart() {
    let newTable = Array(this._table_package_collection_src_queue_list_dart.length * 2).fill(null);
    let split = this._table_package_collection_src_queue_list_dart.length - this._head_package_collection_src_queue_list_dart;
    __dartListSetRange(newTable, 0, split, this._table_package_collection_src_queue_list_dart, this._head_package_collection_src_queue_list_dart);
    __dartListSetRange(newTable, split, split + this._head_package_collection_src_queue_list_dart, this._table_package_collection_src_queue_list_dart, 0);
    this._head_package_collection_src_queue_list_dart = 0;
    this._tail_package_collection_src_queue_list_dart = this._table_package_collection_src_queue_list_dart.length;
    this._table_package_collection_src_queue_list_dart = newTable;
  }
  _writeToList_package_collection_src_queue_list_dart(target) {
    if (this._head_package_collection_src_queue_list_dart <= this._tail_package_collection_src_queue_list_dart) {
      let length = this._tail_package_collection_src_queue_list_dart - this._head_package_collection_src_queue_list_dart;
      __dartListSetRange(target, 0, length, this._table_package_collection_src_queue_list_dart, this._head_package_collection_src_queue_list_dart);
      return length;
    } else {
      let firstPartSize = this._table_package_collection_src_queue_list_dart.length - this._head_package_collection_src_queue_list_dart;
      __dartListSetRange(target, 0, firstPartSize, this._table_package_collection_src_queue_list_dart, this._head_package_collection_src_queue_list_dart);
      __dartListSetRange(target, firstPartSize, firstPartSize + this._tail_package_collection_src_queue_list_dart, this._table_package_collection_src_queue_list_dart, 0);
      return this._tail_package_collection_src_queue_list_dart + firstPartSize;
    }
  }
  _preGrow_package_collection_src_queue_list_dart(newElementCount) {
    newElementCount = newElementCount + (newElementCount >> 1);
    let newCapacity = QueueList._nextPowerOf2_package_collection_src_queue_list_dart(newElementCount);
    let newTable = Array(newCapacity).fill(null);
    this._tail_package_collection_src_queue_list_dart = this._writeToList_package_collection_src_queue_list_dart(newTable);
    this._table_package_collection_src_queue_list_dart = newTable;
    this._head_package_collection_src_queue_list_dart = 0;
  }
}

const $QueueList__initialCapacity = __dartLazyField("QueueList._initialCapacity", () => 8, false);
Object.defineProperty(QueueList, "_initialCapacity_package_collection_src_queue_list_dart", { get: function() {
  return $QueueList__initialCapacity.get();
}, set: function(value) {
  $QueueList__initialCapacity.set(value);
}, enumerable: true });
Object.defineProperty(QueueList, Symbol.hasInstance, { value: function(value) {
  return value != null && (QueueList.prototype.isPrototypeOf(value) || value[$QueueList_interface] === true);
} });
class _CastQueueList extends QueueList {
  constructor(_delegate) {
    const $self = QueueList.__package_collection_src_queue_list_dart.call(new.target, -1, -1, _delegate._table_package_collection_src_queue_list_dart);
    Object.defineProperty($self, "_delegate_package_collection_src_queue_list_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_delegate_package_collection_src_queue_list_dart", { value: _delegate, writable: true, enumerable: true, configurable: true });
    return $self;
  }
  get _head_package_collection_src_queue_list_dart() {
    return this._delegate_package_collection_src_queue_list_dart._head_package_collection_src_queue_list_dart;
  }
  set _head_package_collection_src_queue_list_dart(value) {
    return this._delegate_package_collection_src_queue_list_dart._head_package_collection_src_queue_list_dart = value;
  }
  get _tail_package_collection_src_queue_list_dart() {
    return this._delegate_package_collection_src_queue_list_dart._tail_package_collection_src_queue_list_dart;
  }
  set _tail_package_collection_src_queue_list_dart(value) {
    return this._delegate_package_collection_src_queue_list_dart._tail_package_collection_src_queue_list_dart = value;
  }
}

const $TypedDataBuffer_interface = Symbol("TypedDataBuffer");
class TypedDataBuffer {
  constructor(buffer) {
    Object.defineProperty(this, "_buffer_package_typed_data_src_typed_buffer_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_length_package_typed_data_src_typed_buffer_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_buffer_package_typed_data_src_typed_buffer_dart", { value: buffer, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_length_package_typed_data_src_typed_buffer_dart", { value: buffer.length, writable: true, enumerable: true, configurable: true });
  }
  static _reverse_package_typed_data_src_typed_buffer_dart(buffer, start, end) {
    end = end - 1;
    while (start < end) {
      let first = __dartListLikeGet(buffer, start);
      let last = __dartListLikeGet(buffer, end);
      __dartListLikeSet(buffer, end, first);
      __dartListLikeSet(buffer, start, last);
      start = start + 1;
      end = end - 1;
    }
  }
  get length() {
    return this._length_package_typed_data_src_typed_buffer_dart;
  }
  "[]"(index) {
    if (index >= this.length) {
      throw __dartCoreError("IndexError", index);
    }
    return __dartListLikeGet(this._buffer_package_typed_data_src_typed_buffer_dart, index);
  }
  "[]="(index, value) {
    if (index >= this.length) {
      throw __dartCoreError("IndexError", index);
    }
    __dartListLikeSet(this._buffer_package_typed_data_src_typed_buffer_dart, index, value);
  }
  set length(newLength) {
    if (newLength < this._length_package_typed_data_src_typed_buffer_dart) {
      let defaultValue = this._defaultValue_package_typed_data_src_typed_buffer_dart;
      for (let i = newLength; i < this._length_package_typed_data_src_typed_buffer_dart; i = i + 1) {
        __dartListLikeSet(this._buffer_package_typed_data_src_typed_buffer_dart, i, defaultValue);
      }
    } else {
      if (newLength > this._buffer_package_typed_data_src_typed_buffer_dart.length) {
        let newBuffer = null;
        if (__dartIterableToArray(this._buffer_package_typed_data_src_typed_buffer_dart).length === 0) {
          newBuffer = this._createBuffer_package_typed_data_src_typed_buffer_dart(newLength);
        } else {
          newBuffer = this._createBiggerBuffer_package_typed_data_src_typed_buffer_dart(newLength);
        }
        __dartListSetRange(newBuffer, 0, this._length_package_typed_data_src_typed_buffer_dart, this._buffer_package_typed_data_src_typed_buffer_dart);
        this._buffer_package_typed_data_src_typed_buffer_dart = newBuffer;
      }
    }
    this._length_package_typed_data_src_typed_buffer_dart = newLength;
  }
  _add_package_typed_data_src_typed_buffer_dart(value) {
    if (__dartEquals(this._length_package_typed_data_src_typed_buffer_dart, this._buffer_package_typed_data_src_typed_buffer_dart.length)) {
      this._grow_package_typed_data_src_typed_buffer_dart(this._length_package_typed_data_src_typed_buffer_dart);
    }
    __dartListLikeSet(this._buffer_package_typed_data_src_typed_buffer_dart, (() => {
      const v = this._length_package_typed_data_src_typed_buffer_dart;
      return (() => {
        const v_1 = this._length_package_typed_data_src_typed_buffer_dart = v + 1;
        return v;
      })();
    })(), value);
  }
  add(element) {
    this._add_package_typed_data_src_typed_buffer_dart(element);
  }
  addAll(values, start = 0, end = null) {
    __dartCheckNotNegative(start, "start", null);
    if ((!(end === null) && start > end)) {
      throw __dartCoreError("RangeError", end);
    }
    this._addAll_package_typed_data_src_typed_buffer_dart(values, start, end);
  }
  insertAll(index, values, start = 0, end = null) {
    __dartCheckValidIndex(index, this, "index", this._length_package_typed_data_src_typed_buffer_dart + 1, null);
    __dartCheckNotNegative(start, "start", null);
    if (!(end === null)) {
      if (start > end) {
        throw __dartCoreError("RangeError", end);
      }
      if (__dartEquals(start, end)) {
        return;
      }
    }
    if (__dartEquals(index, this._length_package_typed_data_src_typed_buffer_dart)) {
      this._addAll_package_typed_data_src_typed_buffer_dart(values, start, end);
      return;
    }
    if ((end === null && (Array.isArray(values) || (ArrayBuffer.isView(values) && !(values instanceof DataView))))) {
      end = __dartIterableToArray(values).length;
    }
    if (!(end === null)) {
      this._insertKnownLength_package_typed_data_src_typed_buffer_dart(index, values, start, end);
      return;
    }
    let writeIndex = this._length_package_typed_data_src_typed_buffer_dart;
    let skipCount = start;
    let _sync_for_iterator = __dartIterator(values);
    for (; _sync_for_iterator.moveNext(); ) {
      let value = _sync_for_iterator.current;
      label: {
        if (skipCount > 0) {
          skipCount = skipCount - 1;
          break label;
        }
        if (__dartEquals(writeIndex, this._buffer_package_typed_data_src_typed_buffer_dart.length)) {
          this._grow_package_typed_data_src_typed_buffer_dart(writeIndex);
        }
        __dartListLikeSet(this._buffer_package_typed_data_src_typed_buffer_dart, (() => {
          const v = writeIndex;
          return (() => {
            const v_1 = writeIndex = v + 1;
            return v;
          })();
        })(), value);
      }
    }
    if (skipCount > 0) {
      throw __dartCoreError("StateError", "Too few elements");
    }
    if ((!(end === null) && writeIndex < end)) {
      throw __dartCoreError("RangeError", end);
    }
    TypedDataBuffer._reverse_package_typed_data_src_typed_buffer_dart(this._buffer_package_typed_data_src_typed_buffer_dart, index, this._length_package_typed_data_src_typed_buffer_dart);
    TypedDataBuffer._reverse_package_typed_data_src_typed_buffer_dart(this._buffer_package_typed_data_src_typed_buffer_dart, this._length_package_typed_data_src_typed_buffer_dart, writeIndex);
    TypedDataBuffer._reverse_package_typed_data_src_typed_buffer_dart(this._buffer_package_typed_data_src_typed_buffer_dart, index, writeIndex);
    this._length_package_typed_data_src_typed_buffer_dart = writeIndex;
    return;
  }
  _addAll_package_typed_data_src_typed_buffer_dart(values, start = 0, end = null) {
    if ((Array.isArray(values) || (ArrayBuffer.isView(values) && !(values instanceof DataView)))) {
      (end === null ? end = __dartIterableToArray(values).length : null);
    }
    if (!(end === null)) {
      this._insertKnownLength_package_typed_data_src_typed_buffer_dart(this._length_package_typed_data_src_typed_buffer_dart, values, start, end);
      return;
    }
    let i = 0;
    let _sync_for_iterator = __dartIterator(values);
    for (; _sync_for_iterator.moveNext(); ) {
      let value = _sync_for_iterator.current;
      if (i >= start) {
        this.add(value);
      }
      i = i + 1;
    }
    if (i < start) {
      throw __dartCoreError("StateError", "Too few elements");
    }
  }
  _insertKnownLength_package_typed_data_src_typed_buffer_dart(index, values, start, end) {
    if ((Array.isArray(values) || (ArrayBuffer.isView(values) && !(values instanceof DataView)))) {
      if ((start > __dartIterableToArray(values).length || end > __dartIterableToArray(values).length)) {
        throw __dartCoreError("StateError", "Too few elements");
      }
    }
    let valuesLength = end - start;
    let newLength = this._length_package_typed_data_src_typed_buffer_dart + valuesLength;
    this._ensureCapacity_package_typed_data_src_typed_buffer_dart(newLength);
    __dartListSetRange(this._buffer_package_typed_data_src_typed_buffer_dart, index + valuesLength, this._length_package_typed_data_src_typed_buffer_dart + valuesLength, this._buffer_package_typed_data_src_typed_buffer_dart, index);
    __dartListSetRange(this._buffer_package_typed_data_src_typed_buffer_dart, index, index + valuesLength, values, start);
    this._length_package_typed_data_src_typed_buffer_dart = newLength;
  }
  insert(index, element) {
    if ((index < 0 || index > this._length_package_typed_data_src_typed_buffer_dart)) {
      throw __dartCoreError("RangeError", index);
    }
    if (this._length_package_typed_data_src_typed_buffer_dart < this._buffer_package_typed_data_src_typed_buffer_dart.length) {
      __dartListSetRange(this._buffer_package_typed_data_src_typed_buffer_dart, index + 1, this._length_package_typed_data_src_typed_buffer_dart + 1, this._buffer_package_typed_data_src_typed_buffer_dart, index);
      __dartListLikeSet(this._buffer_package_typed_data_src_typed_buffer_dart, index, element);
      this._length_package_typed_data_src_typed_buffer_dart = this._length_package_typed_data_src_typed_buffer_dart + 1;
      return;
    }
    let newBuffer = this._createBiggerBuffer_package_typed_data_src_typed_buffer_dart(null);
    __dartListSetRange(newBuffer, 0, index, this._buffer_package_typed_data_src_typed_buffer_dart);
    __dartListSetRange(newBuffer, index + 1, this._length_package_typed_data_src_typed_buffer_dart + 1, this._buffer_package_typed_data_src_typed_buffer_dart, index);
    __dartListLikeSet(newBuffer, index, element);
    this._length_package_typed_data_src_typed_buffer_dart = this._length_package_typed_data_src_typed_buffer_dart + 1;
    this._buffer_package_typed_data_src_typed_buffer_dart = newBuffer;
  }
  _ensureCapacity_package_typed_data_src_typed_buffer_dart(requiredCapacity) {
    if (requiredCapacity <= this._buffer_package_typed_data_src_typed_buffer_dart.length) {
      return;
    }
    let newBuffer = this._createBiggerBuffer_package_typed_data_src_typed_buffer_dart(requiredCapacity);
    __dartListSetRange(newBuffer, 0, this._length_package_typed_data_src_typed_buffer_dart, this._buffer_package_typed_data_src_typed_buffer_dart);
    this._buffer_package_typed_data_src_typed_buffer_dart = newBuffer;
  }
  _createBiggerBuffer_package_typed_data_src_typed_buffer_dart(requiredCapacity) {
    let newLength = this._buffer_package_typed_data_src_typed_buffer_dart.length * 2;
    if ((!(requiredCapacity === null) && newLength < requiredCapacity)) {
      newLength = requiredCapacity;
    } else {
      if (newLength < 8) {
        newLength = 8;
      }
    }
    return this._createBuffer_package_typed_data_src_typed_buffer_dart(newLength);
  }
  _grow_package_typed_data_src_typed_buffer_dart(length) {
    this._buffer_package_typed_data_src_typed_buffer_dart = (() => {
      const v = this._createBiggerBuffer_package_typed_data_src_typed_buffer_dart(null);
      return (() => {
        __dartListSetRange(v, 0, length, this._buffer_package_typed_data_src_typed_buffer_dart);
        return v;
      })();
    })();
  }
  setRange(start, end, iterable, skipCount = 0) {
    if (end > this._length_package_typed_data_src_typed_buffer_dart) {
      throw __dartCoreError("RangeError", end);
    }
    this._setRange_package_typed_data_src_typed_buffer_dart(start, end, iterable, skipCount);
  }
  _setRange_package_typed_data_src_typed_buffer_dart(start, end, source, skipCount) {
    if (source instanceof TypedDataBuffer) {
      __dartListSetRange(this._buffer_package_typed_data_src_typed_buffer_dart, start, end, source._buffer_package_typed_data_src_typed_buffer_dart, skipCount);
    } else {
      __dartListSetRange(this._buffer_package_typed_data_src_typed_buffer_dart, start, end, source, skipCount);
    }
  }
  get elementSizeInBytes() {
    return (this._buffer_package_typed_data_src_typed_buffer_dart instanceof DataView ? 1 : this._buffer_package_typed_data_src_typed_buffer_dart.BYTES_PER_ELEMENT);
  }
  get lengthInBytes() {
    return this._length_package_typed_data_src_typed_buffer_dart * (this._buffer_package_typed_data_src_typed_buffer_dart instanceof DataView ? 1 : this._buffer_package_typed_data_src_typed_buffer_dart.BYTES_PER_ELEMENT);
  }
  get offsetInBytes() {
    return this._buffer_package_typed_data_src_typed_buffer_dart.byteOffset;
  }
  get buffer() {
    return this._buffer_package_typed_data_src_typed_buffer_dart.buffer;
  }
  get _defaultValue_package_typed_data_src_typed_buffer_dart() {
    throw new TypeError("Abstract member TypedDataBuffer._defaultValue");
  }
  _createBuffer_package_typed_data_src_typed_buffer_dart(size) {
    throw new TypeError("Abstract member TypedDataBuffer._createBuffer");
  }
}

const $TypedDataBuffer__initialLength = __dartLazyField("TypedDataBuffer._initialLength", () => 8, false);
Object.defineProperty(TypedDataBuffer, "_initialLength_package_typed_data_src_typed_buffer_dart", { get: function() {
  return $TypedDataBuffer__initialLength.get();
}, set: function(value) {
  $TypedDataBuffer__initialLength.set(value);
}, enumerable: true });
Object.defineProperty(TypedDataBuffer, Symbol.hasInstance, { value: function(value) {
  return value != null && (TypedDataBuffer.prototype.isPrototypeOf(value) || value[$TypedDataBuffer_interface] === true);
} });
const $_IntBuffer_interface = Symbol("_IntBuffer");
class _IntBuffer extends TypedDataBuffer {
  constructor(buffer) {
    super(buffer);
  }
  get _defaultValue_package_typed_data_src_typed_buffer_dart() {
    return 0;
  }
}

Object.defineProperty(_IntBuffer, Symbol.hasInstance, { value: function(value) {
  return value != null && (_IntBuffer.prototype.isPrototypeOf(value) || value[$_IntBuffer_interface] === true);
} });
const $_FloatBuffer_interface = Symbol("_FloatBuffer");
class _FloatBuffer extends TypedDataBuffer {
  constructor(buffer) {
    super(buffer);
  }
  get _defaultValue_package_typed_data_src_typed_buffer_dart() {
    return 0.0;
  }
}

Object.defineProperty(_FloatBuffer, Symbol.hasInstance, { value: function(value) {
  return value != null && (_FloatBuffer.prototype.isPrototypeOf(value) || value[$_FloatBuffer_interface] === true);
} });
class Uint8Buffer extends _IntBuffer {
  constructor(initialLength = 0) {
    super(new Uint8Array(initialLength));
  }
  _createBuffer_package_typed_data_src_typed_buffer_dart(size) {
    return new Uint8Array(size);
  }
}

class Float64Buffer extends _FloatBuffer {
  constructor(initialLength = 0) {
    super(new Float64Array(initialLength));
  }
  _createBuffer_package_typed_data_src_typed_buffer_dart(size) {
    return new Float64Array(size);
  }
}

const $_TypedQueue_interface = Symbol("_TypedQueue");
class _TypedQueue {
  constructor(_table) {
    Object.defineProperty(this, "_table_package_typed_data_src_typed_queue_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_head_package_typed_data_src_typed_queue_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_tail_package_typed_data_src_typed_queue_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_table_package_typed_data_src_typed_queue_dart", { value: _table, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_head_package_typed_data_src_typed_queue_dart", { value: 0, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_tail_package_typed_data_src_typed_queue_dart", { value: 0, writable: true, enumerable: true, configurable: true });
  }
  toList({ growable = true } = {}) {
    if (this.length === 0) {
      return __dartListOf([], growable);
    }
    let first = __dartListLikeGet(this, 0);
    let result = __dartListFilled(this.length, first, growable);
    for (let i = 1; i < this.length; i = i + 1) {
      __dartListLikeSet(result, i, __dartListLikeGet(this, i));
    }
    return result;
  }
  cast() {
    return this;
  }
  removeLast() {
    if (__dartEquals(this.length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    let result = __dartListLikeGet(this, this.length - 1);
    this.length = this.length - 1;
    return result;
  }
  add(element) {
    __dartListLikeSet(this, (() => {
      const v = this.length;
      return (() => {
        const v_1 = this.length = v + 1;
        return v;
      })();
    })(), element);
  }
  removeRange(start, end) {
    __dartCheckValidRange(start, end, this.length, null, null, null);
    if (end > start) {
      __dartListRemoveRange(this, start, end);
    }
  }
  setRange(start, end, iterable, skipCount = 0) {
    __dartCheckValidRange(start, end, this.length, null, null, null);
    let length = end - start;
    if (__dartEquals(length, 0)) {
      return;
    }
    __dartCheckNotNegative(skipCount, "skipCount", null);
    let otherList = null;
    let otherStart = null;
    if ((Array.isArray(iterable) || (ArrayBuffer.isView(iterable) && !(iterable instanceof DataView)))) {
      otherList = iterable;
      otherStart = skipCount;
    } else {
      otherList = __dartListOf(__dartIterableToArray(iterable).slice(skipCount), false);
      otherStart = 0;
    }
    if (otherStart + length > otherList.length) {
      throw __dartCoreError("StateError", "Too few elements");
    }
    if (otherStart < start) {
      for (let i = length - 1; i >= 0; i = i - 1) {
        __dartListLikeSet(this, start + i, __dartListLikeGet(otherList, otherStart + i));
      }
    } else {
      for (let i_1 = 0; i_1 < length; i_1 = i_1 + 1) {
        __dartListLikeSet(this, start + i_1, __dartListLikeGet(otherList, otherStart + i_1));
      }
    }
  }
  fillRange(start, end, fill = null) {
    let value = (() => {
      let v = fill;
      return (v === null ? (() => {
        let v_1 = v;
        return (v_1 === null ? __dartAs(v_1, (value) => true, "TypeParameterType(__TypedQueue&Object&ListMixin.E%)") : v_1);
      })() : v);
    })();
    __dartCheckValidRange(start, end, this.length, null, null, null);
    for (let i = start; i < end; i = i + 1) {
      __dartListLikeSet(this, i, value);
    }
  }
  sublist(start, end = null) {
    let listLength = this.length;
    (end === null ? end = listLength : null);
    __dartCheckValidRange(start, end, listLength, null, null, null);
    return __dartListOf(__dartIterableToArray(this).slice(start, end));
  }
  get first() {
    if (__dartEquals(this.length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    return __dartListLikeGet(this, 0);
  }
  set first(value) {
    if (__dartEquals(this.length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    __dartListLikeSet(this, 0, value);
  }
  get last() {
    if (__dartEquals(this.length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    return __dartListLikeGet(this, this.length - 1);
  }
  set last(value) {
    if (__dartEquals(this.length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    __dartListLikeSet(this, this.length - 1, value);
  }
  get iterator() {
    return __dartIterator(this);
  }
  elementAt(index) {
    return __dartListLikeGet(this, index);
  }
  followedBy(other) {
    return __dartIterableToArray(this).concat(__dartIterableToArray(other));
  }
  forEach(action) {
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      action(__dartListLikeGet(this, i));
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
  }
  get isEmpty() {
    return __dartEquals(this.length, 0);
  }
  get isNotEmpty() {
    return !(this.length === 0);
  }
  get single() {
    if (__dartEquals(this.length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    if (this.length > 1) {
      throw __dartCoreError("StateError", "Too many elements");
    }
    return __dartListLikeGet(this, 0);
  }
  contains(element) {
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      if (__dartEquals(__dartListLikeGet(this, i), element)) {
        return true;
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    return false;
  }
  every(test) {
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      if (!(test(__dartListLikeGet(this, i)))) {
        return false;
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    return true;
  }
  any(test) {
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      if (test(__dartListLikeGet(this, i))) {
        return true;
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    return false;
  }
  firstWhere(test, { orElse = null } = {}) {
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      let element = __dartListLikeGet(this, i);
      if (test(element)) {
        return element;
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    if (!(orElse === null)) {
      return orElse();
    }
    throw __dartCoreError("StateError", "No element");
  }
  lastWhere(test, { orElse = null } = {}) {
    let length = this.length;
    for (let i = length - 1; i >= 0; i = i - 1) {
      let element = __dartListLikeGet(this, i);
      if (test(element)) {
        return element;
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    if (!(orElse === null)) {
      return orElse();
    }
    throw __dartCoreError("StateError", "No element");
  }
  singleWhere(test, { orElse = null } = {}) {
    let length = this.length;
    const match = __dartLazyField("match", null, true);
    let matchFound = false;
    for (let i = 0; i < length; i = i + 1) {
      let element = __dartListLikeGet(this, i);
      if (test(element)) {
        if (matchFound) {
          throw __dartCoreError("StateError", "Too many elements");
        }
        matchFound = true;
        match.set(element);
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    if (matchFound) {
      return match.get();
    }
    if (!(orElse === null)) {
      return orElse();
    }
    throw __dartCoreError("StateError", "No element");
  }
  join(separator = "") {
    if (__dartEquals(this.length, 0)) {
      return "";
    }
    let buffer = (() => {
      const v = __dartStringBuffer("");
      return (() => {
        v.writeAll(this, separator);
        return v;
      })();
    })();
    return buffer.toString();
  }
  where(test) {
    return __dartIterableToArray(this).filter(test);
  }
  whereType() {
    return __dartIterableToArray(this).filter((value) => true);
  }
  map(f) {
    return Array.from(this, f);
  }
  expand(f) {
    return __dartIterableToArray(this).flatMap((value) => __dartIterableToArray((f)(value)));
  }
  reduce(combine) {
    let length = this.length;
    if (__dartEquals(length, 0)) {
      throw __dartCoreError("StateError", "No element");
    }
    let value = __dartListLikeGet(this, 0);
    for (let i = 1; i < length; i = i + 1) {
      value = combine(value, __dartListLikeGet(this, i));
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    return value;
  }
  fold(initialValue, combine) {
    let value = initialValue;
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      value = combine(value, __dartListLikeGet(this, i));
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    return value;
  }
  skip(count) {
    return __dartIterableToArray(this).slice(count, null);
  }
  skipWhile(test) {
    return __dartIterableSkipWhile(this, test);
  }
  take(count) {
    return __dartIterableToArray(this).slice(0, __dartNullCheck(count));
  }
  takeWhile(test) {
    return __dartIterableTakeWhile(this, test);
  }
  toSet() {
    let result = __dartSetFrom([]);
    for (let i = 0; i < this.length; i = i + 1) {
      __dartSetAdd(result, __dartListLikeGet(this, i));
    }
    return result;
  }
  addAll(iterable) {
    let i = this.length;
    let _sync_for_iterator = __dartIterator(iterable);
    for (; _sync_for_iterator.moveNext(); ) {
      let element = _sync_for_iterator.current;
      __dartListAdd(this, element);
      i = i + 1;
    }
  }
  remove(element) {
    for (let i = 0; i < this.length; i = i + 1) {
      if (__dartEquals(__dartListLikeGet(this, i), element)) {
        __dartListRemoveRange(this, i, i + 1);
        return true;
      }
    }
    return false;
  }
  _closeGap_dart_collection(start, end) {
    let length = this.length;
    let size = end - start;
    for (let i = end; i < length; i = i + 1) {
      __dartListLikeSet(this, i - size, __dartListLikeGet(this, i));
    }
    this.length = length - size;
  }
  removeWhere(test) {
    __dartListRemoveWhere(this, test);
  }
  retainWhere(test) {
    __dartListRetainWhere(this, test);
  }
  _filter_dart_collection(test, retainMatching) {
    let retained = Array(0).fill(null);
    let length = this.length;
    for (let i = 0; i < length; i = i + 1) {
      let element = __dartListLikeGet(this, i);
      if (__dartEquals(test(element), retainMatching)) {
        __dartListAdd(retained, element);
      }
      if (!(__dartEquals(length, this.length))) {
        throw __dartCoreError("ConcurrentModificationError", this);
      }
    }
    if (!(__dartEquals(retained.length, this.length))) {
      __dartListSetRange(this, 0, retained.length, retained);
      this.length = retained.length;
    }
  }
  clear() {
    this.length = 0;
  }
  sort(compare = null) {
    this.sort((left, right) => __dartCompare(left, right, (compare ?? ((left, right) => __dartCompare(left, right)))));
  }
  shuffle(random = null) {
    (random === null ? random = __dartRandom(null, false) : null);
    let length = this.length;
    while (length > 1) {
      let pos = random.nextInt(length);
      length = length - 1;
      let tmp = __dartListLikeGet(this, length);
      __dartListLikeSet(this, length, __dartListLikeGet(this, pos));
      __dartListLikeSet(this, pos, tmp);
    }
  }
  asMap() {
    return __dartListAsMap(this);
  }
  "+"(other) {
    return (() => {
      const v = __dartListOf(this);
      __dartListAddAll(v, other);
      return v;
    })();
  }
  getRange(start, end) {
    __dartCheckValidRange(start, end, this.length, null, null, null);
    return __dartIterableToArray(this).slice(start, end);
  }
  replaceRange(start, end, newContents) {
    __dartCheckValidRange(start, end, this.length, null, null, null);
    if (__dartEquals(start, this.length)) {
      __dartListAddAll(this, newContents);
      return;
    }
    if (!(((newContents != null && typeof newContents !== "string") && typeof newContents.length === "number"))) {
      newContents = __dartListOf(newContents, true);
    }
    let removeLength = end - start;
    let insertLength = __dartIterableToArray(newContents).length;
    if (removeLength >= insertLength) {
      let insertEnd = start + insertLength;
      __dartListSetRange(this, start, insertEnd, newContents);
      if (removeLength > insertLength) {
        __dartListRemoveRange(this, insertEnd, end);
      }
    } else {
      if (__dartEquals(end, this.length)) {
        let i = start;
        let _sync_for_iterator = __dartIterator(newContents);
        for (; _sync_for_iterator.moveNext(); ) {
          let element = _sync_for_iterator.current;
          if (i < end) {
            __dartListLikeSet(this, i, element);
          } else {
            __dartListAdd(this, element);
          }
          i = i + 1;
        }
      } else {
        let delta = insertLength - removeLength;
        let oldLength = this.length;
        let insertEnd_1 = start + insertLength;
        for (let i_1 = oldLength - delta; i_1 < oldLength; i_1 = i_1 + 1) {
          __dartListAdd(this, __dartListLikeGet(this, (i_1 > 0 ? i_1 : 0)));
        }
        if (insertEnd_1 < oldLength) {
          __dartListSetRange(this, insertEnd_1, oldLength, this, end);
        }
        __dartListSetRange(this, start, insertEnd_1, newContents);
      }
    }
  }
  indexOf(element, start = 0) {
    if (start < 0) {
      start = 0;
    }
    for (let i = start; i < this.length; i = i + 1) {
      if (__dartEquals(__dartListLikeGet(this, i), element)) {
        return i;
      }
    }
    return -1;
  }
  indexWhere(test, start = 0) {
    if (start < 0) {
      start = 0;
    }
    for (let i = start; i < this.length; i = i + 1) {
      if (test(__dartListLikeGet(this, i))) {
        return i;
      }
    }
    return -1;
  }
  lastIndexOf(element, start = null) {
    if ((start === null || start >= this.length)) {
      start = this.length - 1;
    }
    for (let i = start; i >= 0; i = i - 1) {
      if (__dartEquals(__dartListLikeGet(this, i), element)) {
        return i;
      }
    }
    return -1;
  }
  lastIndexWhere(test, start = null) {
    if ((start === null || start >= this.length)) {
      start = this.length - 1;
    }
    for (let i = start; i >= 0; i = i - 1) {
      if (test(__dartListLikeGet(this, i))) {
        return i;
      }
    }
    return -1;
  }
  insert(index, element) {
    __dartNullCheck(index);
    let length = this.length;
    __dartCheckValueInInterval(index, 0, length, "index", null);
    __dartListAdd(this, element);
    if (!(__dartEquals(index, length))) {
      __dartListSetRange(this, index + 1, length + 1, this, index);
      __dartListLikeSet(this, index, element);
    }
  }
  removeAt(index) {
    let result = __dartListLikeGet(this, index);
    __dartListRemoveRange(this, index, index + 1);
    return result;
  }
  insertAll(index, iterable) {
    __dartCheckValueInInterval(index, 0, this.length, "index", null);
    if (__dartEquals(index, this.length)) {
      __dartListAddAll(this, iterable);
      return;
    }
    if ((!(((iterable != null && typeof iterable !== "string") && typeof iterable.length === "number")) || Object.is(iterable, this))) {
      iterable = __dartListOf(iterable, true);
    }
    let insertionLength = __dartIterableToArray(iterable).length;
    if (__dartEquals(insertionLength, 0)) {
      return;
    }
    let oldLength = this.length;
    for (let i = oldLength - insertionLength; i < oldLength; i = i + 1) {
      __dartListAdd(this, __dartListLikeGet(this, (i > 0 ? i : 0)));
    }
    if (!(__dartEquals(__dartIterableToArray(iterable).length, insertionLength))) {
      this.length = this.length - insertionLength;
      throw __dartCoreError("ConcurrentModificationError", iterable);
    }
    let oldCopyStart = index + insertionLength;
    if (oldCopyStart < oldLength) {
      __dartListSetRange(this, oldCopyStart, oldLength, this, index);
    }
    __dartListSetAll(this, index, iterable);
  }
  setAll(index, iterable) {
    if ((Array.isArray(iterable) || (ArrayBuffer.isView(iterable) && !(iterable instanceof DataView)))) {
      __dartListSetRange(this, index, index + __dartIterableToArray(iterable).length, iterable);
    } else {
      let _sync_for_iterator = __dartIterator(iterable);
      for (; _sync_for_iterator.moveNext(); ) {
        let element = _sync_for_iterator.current;
        __dartListLikeSet(this, (() => {
          const v = index;
          return (() => {
            const v_1 = index = v + 1;
            return v;
          })();
        })(), element);
      }
    }
  }
  get reversed() {
    return __dartIterableToArray(this).reverse();
  }
  toString() {
    return `[${__dartIterableToArray(this).map((value) => __dartStr(value)).join(", ")}]`;
  }
  get length() {
    return this._tail_package_typed_data_src_typed_queue_dart - this._head_package_typed_data_src_typed_queue_dart & this._table_package_typed_data_src_typed_queue_dart.length - 1;
  }
  toList_1({ growable = true } = {}) {
    let list = (growable ? this._createBuffer_package_typed_data_src_typed_queue_dart(this.length) : this._createList_package_typed_data_src_typed_queue_dart(this.length));
    this._writeToList_package_typed_data_src_typed_queue_dart(list);
    return list;
  }
  cast_1() {
    if (this instanceof QueueList) {
      return __dartAs(this, (value) => value instanceof QueueList, "QueueList");
    }
    throw __dartCoreError("UnsupportedError", `${__dartStr(this)} cannot be cast to the desired type.`);
  }
  retype() {
    return this.cast_1();
  }
  addLast(value) {
    __dartListLikeSet(this._table_package_typed_data_src_typed_queue_dart, this._tail_package_typed_data_src_typed_queue_dart, value);
    this._tail_package_typed_data_src_typed_queue_dart = this._tail_package_typed_data_src_typed_queue_dart + 1 & this._table_package_typed_data_src_typed_queue_dart.length - 1;
    if (__dartEquals(this._head_package_typed_data_src_typed_queue_dart, this._tail_package_typed_data_src_typed_queue_dart)) {
      this._growAtCapacity_package_typed_data_src_typed_queue_dart();
    }
  }
  addFirst(value) {
    this._head_package_typed_data_src_typed_queue_dart = this._head_package_typed_data_src_typed_queue_dart - 1 & this._table_package_typed_data_src_typed_queue_dart.length - 1;
    __dartListLikeSet(this._table_package_typed_data_src_typed_queue_dart, this._head_package_typed_data_src_typed_queue_dart, value);
    if (__dartEquals(this._head_package_typed_data_src_typed_queue_dart, this._tail_package_typed_data_src_typed_queue_dart)) {
      this._growAtCapacity_package_typed_data_src_typed_queue_dart();
    }
  }
  removeFirst() {
    if (__dartEquals(this._head_package_typed_data_src_typed_queue_dart, this._tail_package_typed_data_src_typed_queue_dart)) {
      throw __dartCoreError("StateError", "No element");
    }
    let result = __dartListLikeGet(this._table_package_typed_data_src_typed_queue_dart, this._head_package_typed_data_src_typed_queue_dart);
    this._head_package_typed_data_src_typed_queue_dart = this._head_package_typed_data_src_typed_queue_dart + 1 & this._table_package_typed_data_src_typed_queue_dart.length - 1;
    return result;
  }
  removeLast_1() {
    if (__dartEquals(this._head_package_typed_data_src_typed_queue_dart, this._tail_package_typed_data_src_typed_queue_dart)) {
      throw __dartCoreError("StateError", "No element");
    }
    this._tail_package_typed_data_src_typed_queue_dart = this._tail_package_typed_data_src_typed_queue_dart - 1 & this._table_package_typed_data_src_typed_queue_dart.length - 1;
    return __dartListLikeGet(this._table_package_typed_data_src_typed_queue_dart, this._tail_package_typed_data_src_typed_queue_dart);
  }
  add_1(value) {
    return this.addLast(value);
  }
  set length(value) {
    __dartCheckNotNegative(value, "length", null);
    let delta = value - this.length;
    if (delta >= 0) {
      let needsToGrow = this._table_package_typed_data_src_typed_queue_dart.length <= value;
      if (needsToGrow) {
        this._growTo_package_typed_data_src_typed_queue_dart(value);
      }
      this._tail_package_typed_data_src_typed_queue_dart = this._tail_package_typed_data_src_typed_queue_dart + delta & this._table_package_typed_data_src_typed_queue_dart.length - 1;
      if (!(needsToGrow)) {
        this.fillRange_1(value - delta, value, this._defaultValue_package_typed_data_src_typed_queue_dart);
      }
    } else {
      this.removeRange_1(value, this.length);
    }
  }
  "[]"(index) {
    __dartCheckValidIndex(index, this, null, this.length, null);
    return __dartListLikeGet(this._table_package_typed_data_src_typed_queue_dart, this._head_package_typed_data_src_typed_queue_dart + index & this._table_package_typed_data_src_typed_queue_dart.length - 1);
  }
  "[]="(index, value) {
    __dartCheckValidIndex(index, this, null, null, null);
    __dartListLikeSet(this._table_package_typed_data_src_typed_queue_dart, this._head_package_typed_data_src_typed_queue_dart + index & this._table_package_typed_data_src_typed_queue_dart.length - 1, value);
  }
  removeRange_1(start, end) {
    let length = this.length;
    __dartCheckValidRange(start, end, length, null, null, null);
    if (__dartEquals(start, 0)) {
      this._head_package_typed_data_src_typed_queue_dart = this._head_package_typed_data_src_typed_queue_dart + end & this._table_package_typed_data_src_typed_queue_dart.length - 1;
      return;
    }
    let elementsAfter = length - end;
    if (__dartEquals(elementsAfter, 0)) {
      this._tail_package_typed_data_src_typed_queue_dart = this._head_package_typed_data_src_typed_queue_dart + start & this._table_package_typed_data_src_typed_queue_dart.length - 1;
      return;
    }
    let removedElements = end - start;
    if (start < elementsAfter) {
      this.setRange_1(removedElements, end, this);
      this._head_package_typed_data_src_typed_queue_dart = this._head_package_typed_data_src_typed_queue_dart + removedElements & this._table_package_typed_data_src_typed_queue_dart.length - 1;
    } else {
      this.setRange_1(start, length - removedElements, this, end);
      this._tail_package_typed_data_src_typed_queue_dart = this._tail_package_typed_data_src_typed_queue_dart - removedElements & this._table_package_typed_data_src_typed_queue_dart.length - 1;
    }
  }
  setRange_1(start, end, iterable, skipCount = 0) {
    __dartCheckValidRange(start, end, this.length, null, null, null);
    if (__dartEquals(start, end)) {
      return;
    }
    let targetStart = this._head_package_typed_data_src_typed_queue_dart + start & this._table_package_typed_data_src_typed_queue_dart.length - 1;
    let targetEnd = this._head_package_typed_data_src_typed_queue_dart + end & this._table_package_typed_data_src_typed_queue_dart.length - 1;
    let targetIsContiguous = targetStart < targetEnd;
    if (Object.is(iterable, this)) {
      let sourceStart = this._head_package_typed_data_src_typed_queue_dart + skipCount & this._table_package_typed_data_src_typed_queue_dart.length - 1;
      let sourceEnd = sourceStart + (end - start) & this._table_package_typed_data_src_typed_queue_dart.length - 1;
      if (__dartEquals(sourceStart, targetStart)) {
        return;
      }
      let sourceIsContiguous = sourceStart < sourceEnd;
      if ((targetIsContiguous && sourceIsContiguous)) {
        __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, targetStart, targetEnd, this._table_package_typed_data_src_typed_queue_dart, sourceStart);
      } else {
        if ((!(targetIsContiguous) && !(sourceIsContiguous))) {
          if (sourceStart > targetStart) {
            let startGap = sourceStart - targetStart;
            let firstEnd = this._table_package_typed_data_src_typed_queue_dart.length - startGap;
            __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, targetStart, firstEnd, this._table_package_typed_data_src_typed_queue_dart, sourceStart);
            __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, firstEnd, this._table_package_typed_data_src_typed_queue_dart.length, this._table_package_typed_data_src_typed_queue_dart);
            __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, 0, targetEnd, this._table_package_typed_data_src_typed_queue_dart, startGap);
          } else {
            if (sourceEnd < targetEnd) {
              let firstStart = targetEnd - sourceEnd;
              __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, firstStart, targetEnd, this._table_package_typed_data_src_typed_queue_dart);
              __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, 0, firstStart, this._table_package_typed_data_src_typed_queue_dart, this._table_package_typed_data_src_typed_queue_dart.length - firstStart);
              __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, targetStart, this._table_package_typed_data_src_typed_queue_dart.length, this._table_package_typed_data_src_typed_queue_dart, sourceStart);
            }
          }
        } else {
          if (sourceStart < targetEnd) {
            if (sourceIsContiguous) {
              __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, targetStart, this._table_package_typed_data_src_typed_queue_dart.length, this._table_package_typed_data_src_typed_queue_dart, sourceStart);
              __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, 0, targetEnd, this._table_package_typed_data_src_typed_queue_dart, sourceStart + (this._table_package_typed_data_src_typed_queue_dart.length - targetStart));
            } else {
              let firstEnd_1 = this._table_package_typed_data_src_typed_queue_dart.length - sourceStart;
              __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, targetStart, firstEnd_1, this._table_package_typed_data_src_typed_queue_dart, sourceStart);
              __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, firstEnd_1, targetEnd, this._table_package_typed_data_src_typed_queue_dart);
            }
          } else {
            if (sourceIsContiguous) {
              __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, 0, targetEnd, this._table_package_typed_data_src_typed_queue_dart, sourceStart + (this._table_package_typed_data_src_typed_queue_dart.length - targetStart));
              __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, targetStart, this._table_package_typed_data_src_typed_queue_dart.length, this._table_package_typed_data_src_typed_queue_dart, sourceStart);
            } else {
              let firstStart_1 = targetEnd - sourceEnd;
              __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, firstStart_1, targetEnd, this._table_package_typed_data_src_typed_queue_dart);
              __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, targetStart, firstStart_1, this._table_package_typed_data_src_typed_queue_dart, sourceStart);
            }
          }
        }
      }
    } else {
      if (targetIsContiguous) {
        __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, targetStart, targetEnd, iterable, skipCount);
      } else {
        if ((Array.isArray(iterable) || (ArrayBuffer.isView(iterable) && !(iterable instanceof DataView)))) {
          __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, targetStart, this._table_package_typed_data_src_typed_queue_dart.length, iterable, skipCount);
          __dartListSetRange(this._table_package_typed_data_src_typed_queue_dart, 0, targetEnd, iterable, skipCount + (this._table_package_typed_data_src_typed_queue_dart.length - targetStart));
        } else {
          super.setRange(start, end, iterable, skipCount);
        }
      }
    }
  }
  fillRange_1(start, end, value = null) {
    let startInTable = this._head_package_typed_data_src_typed_queue_dart + start & this._table_package_typed_data_src_typed_queue_dart.length - 1;
    let endInTable = this._head_package_typed_data_src_typed_queue_dart + end & this._table_package_typed_data_src_typed_queue_dart.length - 1;
    if (startInTable <= endInTable) {
      __dartListFillRange(this._table_package_typed_data_src_typed_queue_dart, startInTable, endInTable, value);
    } else {
      __dartListFillRange(this._table_package_typed_data_src_typed_queue_dart, startInTable, this._table_package_typed_data_src_typed_queue_dart.length, value);
      __dartListFillRange(this._table_package_typed_data_src_typed_queue_dart, 0, endInTable, value);
    }
  }
  sublist_1(start, end = null) {
    let length = this.length;
    let nonNullEnd = __dartCheckValidRange(start, end, length, null, null, null);
    let list = this._createList_package_typed_data_src_typed_queue_dart(nonNullEnd - start);
    this._writeToList_package_typed_data_src_typed_queue_dart(list, start, nonNullEnd);
    return list;
  }
  _writeToList_package_typed_data_src_typed_queue_dart(target, start = null, end = null) {
    (start === null ? start = 0 : null);
    (end === null ? end = this.length : null);
    let elementsToWrite = end - start;
    let startInTable = this._head_package_typed_data_src_typed_queue_dart + start & this._table_package_typed_data_src_typed_queue_dart.length - 1;
    let endInTable = this._head_package_typed_data_src_typed_queue_dart + end & this._table_package_typed_data_src_typed_queue_dart.length - 1;
    if (startInTable <= endInTable) {
      __dartListSetRange(target, 0, elementsToWrite, this._table_package_typed_data_src_typed_queue_dart, startInTable);
    } else {
      let firstPartSize = this._table_package_typed_data_src_typed_queue_dart.length - startInTable;
      __dartListSetRange(target, 0, firstPartSize, this._table_package_typed_data_src_typed_queue_dart, startInTable);
      __dartListSetRange(target, firstPartSize, firstPartSize + endInTable, this._table_package_typed_data_src_typed_queue_dart, 0);
    }
    return elementsToWrite;
  }
  _growAtCapacity_package_typed_data_src_typed_queue_dart() {
    let newTable = this._createList_package_typed_data_src_typed_queue_dart(this._table_package_typed_data_src_typed_queue_dart.length * 2);
    let partitionPoint = this._table_package_typed_data_src_typed_queue_dart.length - this._head_package_typed_data_src_typed_queue_dart;
    __dartListSetRange(newTable, 0, partitionPoint, this._table_package_typed_data_src_typed_queue_dart, this._head_package_typed_data_src_typed_queue_dart);
    if (!(__dartEquals(partitionPoint, this._table_package_typed_data_src_typed_queue_dart.length))) {
      __dartListSetRange(newTable, partitionPoint, this._table_package_typed_data_src_typed_queue_dart.length, this._table_package_typed_data_src_typed_queue_dart);
    }
    this._head_package_typed_data_src_typed_queue_dart = 0;
    this._tail_package_typed_data_src_typed_queue_dart = this._table_package_typed_data_src_typed_queue_dart.length;
    this._table_package_typed_data_src_typed_queue_dart = newTable;
  }
  _growTo_package_typed_data_src_typed_queue_dart(newElementCount) {
    newElementCount = newElementCount + (newElementCount >> 1);
    let newTable = this._createList_package_typed_data_src_typed_queue_dart(_nextPowerOf2(newElementCount));
    this._tail_package_typed_data_src_typed_queue_dart = this._writeToList_package_typed_data_src_typed_queue_dart(newTable);
    this._table_package_typed_data_src_typed_queue_dart = newTable;
    this._head_package_typed_data_src_typed_queue_dart = 0;
  }
  _createList_package_typed_data_src_typed_queue_dart(size) {
    throw new TypeError("Abstract member _TypedQueue._createList");
  }
  _createBuffer_package_typed_data_src_typed_queue_dart(size) {
    throw new TypeError("Abstract member _TypedQueue._createBuffer");
  }
  get _defaultValue_package_typed_data_src_typed_queue_dart() {
    throw new TypeError("Abstract member _TypedQueue._defaultValue");
  }
}

Object.defineProperty(_TypedQueue, Symbol.hasInstance, { value: function(value) {
  return value != null && (_TypedQueue.prototype.isPrototypeOf(value) || value[$_TypedQueue_interface] === true);
} });
const $_IntQueue_interface = Symbol("_IntQueue");
class _IntQueue extends _TypedQueue {
  constructor(queue) {
    super(queue);
  }
  get _defaultValue_package_typed_data_src_typed_queue_dart() {
    return 0;
  }
}

Object.defineProperty(_IntQueue, Symbol.hasInstance, { value: function(value) {
  return value != null && (_IntQueue.prototype.isPrototypeOf(value) || value[$_IntQueue_interface] === true);
} });
class Uint8Queue extends _IntQueue {
  constructor(initialCapacity = null) {
    super(new Uint8Array(_chooseRealInitialCapacity(initialCapacity)));
    Object.defineProperty(this, $QueueList_interface, { value: true });
  }
  static fromList(elements) {
    return (() => {
      const v = new Uint8Queue(elements.length);
      return (() => {
        v.addAll(elements);
        return v;
      })();
    })();
  }
  _createList_package_typed_data_src_typed_queue_dart(size) {
    return new Uint8Array(size);
  }
  _createBuffer_package_typed_data_src_typed_queue_dart(size) {
    return new Uint8Buffer(size);
  }
  get _table_package_collection_src_queue_list_dart() {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
  set _table_package_collection_src_queue_list_dart(value) {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
  get _head_package_collection_src_queue_list_dart() {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
  set _head_package_collection_src_queue_list_dart(value) {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
  get _tail_package_collection_src_queue_list_dart() {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
  set _tail_package_collection_src_queue_list_dart(value) {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
  _add_package_collection_src_queue_list_dart(element) {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
  _grow_package_collection_src_queue_list_dart() {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
  _writeToList_package_collection_src_queue_list_dart(target) {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
  _preGrow_package_collection_src_queue_list_dart(newElementCount) {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
}

function _chooseRealInitialCapacity(initialCapacity) {
  if ((initialCapacity === null || initialCapacity < 16)) {
    return 16;
  } else {
    if (!(_isPowerOf2(initialCapacity))) {
      return _nextPowerOf2(initialCapacity);
    } else {
      return initialCapacity;
    }
  }
}

function _isPowerOf2(number) {
  return __dartEquals(number & number - 1, 0);
}

function _nextPowerOf2(number) {
  number = (number << 1) - 1;
  for (; ; ) {
    let nextNumber = number & number - 1;
    if (__dartEquals(nextNumber, 0)) {
      return number;
    }
    number = nextNumber;
  }
}

export function main() {
  const buffer = (() => {
    const v = new Uint8Buffer();
    return (() => {
      v.add(1);
      v.addAll([2, 3, 4]);
      return v;
    })();
  })();
  buffer["[]="](1, 9);
  const bytes = Uint8Array.from(buffer);
  const queue = Uint8Queue.fromList([10, 11]);
  queue.add_1(12);
  const first = queue.removeFirst();
  queue.addFirst(8);
  const last = queue.removeLast_1();
  const floats = (() => {
    const v = new Float64Buffer();
    return (() => {
      v.add(1.5);
      v.add(2.25);
      return v;
    })();
  })();
  __dartPrint(`typed_data ${__dartStr(__dartIterableToArray(buffer).join(","))} ${__dartStr(first)} ${__dartStr(last)} ${__dartStr(queue.join("|"))} ${__dartStr(floats["[]"](0) + floats["[]"](1))} ${__dartStr(bytes.length)}`);
}

main();
