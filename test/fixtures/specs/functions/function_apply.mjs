function __dartStr(value) {
  if (value == null) return "null";
  if (Array.isArray(value)) {
    return "[" + value.map(__dartStr).join(", ") + "]";
  }
  if (value instanceof Set) {
    return "{" + Array.from(value).map(__dartStr).join(", ") + "}";
  }
  if (value instanceof Map) {
    return "{" + Array.from(value, ([key, entryValue]) => __dartStr(key) + ": " + __dartStr(entryValue)).join(", ") + "}";
  }
  if (typeof value === "object") {
    const toString = value.toString;
    if (typeof toString === "function" && toString !== Object.prototype.toString) {
      return String(toString.call(value));
    }
  }
  return String(value);
}
function __dartPrint(value) {
  console.log(__dartStr(value));
}
const __dartSymbolCache = new Map();
function __dartSymbol(key, name) {
  if (__dartSymbolCache.has(key)) return __dartSymbolCache.get(key);
  const value = Object.freeze({
    name,
    toString() { return "Symbol(" + JSON.stringify(name) + ")"; },
  });
  __dartSymbolCache.set(key, value);
  return value;
}
function __dartAs(value, test, typeName) {
  if (test(value)) return value;
  throw new TypeError("Type cast failed: expected " + typeName);
}
function __dartFunctionApply(fn, positionalArguments, namedArguments = null) {
  const args = Array.from(positionalArguments);
  if (namedArguments != null) {
    const options = {};
    let hasNamed = false;
    const entries = namedArguments instanceof Map ? namedArguments.entries() : Object.entries(namedArguments);
    for (const [key, value] of entries) {
      const name = typeof key === "string" ? key : (typeof key === "symbol" ? key.description : key?.name);
      if (typeof name !== "string") throw new TypeError("Function.apply named argument keys must be Symbols");
      options[name] = value;
      hasNamed = true;
    }
    if (hasNamed) args.push(options);
  }
  return fn(...args);
}

// Generated by dart2esm.

export function add(left, right) {
  return (left + right);
}

export function describe(name, { count = 1, loud = false } = {}) {
  const value = __dartStr(name) + ":" + __dartStr(count);
  return (loud ? value.toUpperCase() : value);
}

export function invokeDescribe(function_1, positional, named) {
  return __dartAs(__dartFunctionApply(function_1, positional, named), value => typeof value === "string", "String");
}

export function main() {
  const local = function(value, { add: add_1 = 0 } = {}) { return (value + add_1); };
  __dartPrint("positional " + __dartStr(__dartFunctionApply(add, [2, 3], null)));
  __dartPrint("named " + __dartStr(__dartFunctionApply(describe, ["ada"], new Map([[__dartSymbol("count", "count"), 3], [__dartSymbol("loud", "loud"), true]]))));
  __dartPrint("local " + __dartStr(__dartFunctionApply(local, [4], new Map([[__dartSymbol("add", "add"), 5]]))));
  __dartPrint("forward " + __dartStr(invokeDescribe(describe, ["dart"], new Map([[__dartSymbol("count", "count"), 2]]))));
}

main();
