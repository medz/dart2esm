// Generated by dart2esm.

function __dartCoreError(typeName, message) {
  const text = message == null ? "" : String(message);
  const display = text === "" ? typeName : typeName + ": " + text;
  const error = new Error(text);
  error.name = typeName;
  Object.defineProperty(error, "__dartCoreErrorType", { value: typeName });
  Object.defineProperty(error, "toString", { value() { return display; } });
  return error;
}
function __dartIsCoreError(value, typeName) {
  const actual = value == null ? null : value.__dartCoreErrorType;
  if (actual != null) {
    if (actual === typeName) return true;
    if (typeName === "Exception" && actual === "FormatException") return true;
    if (typeName === "RangeError" && actual === "IndexError") return true;
    if (typeName === "ArgumentError" && (actual === "RangeError" || actual === "IndexError")) return true;
    return typeName === "Error" && actual !== "Exception" && actual !== "FormatException";
  }
  if (typeName === "TypeError" && value instanceof TypeError) return true;
  return typeName === "Error" && value instanceof Error;
}

function __dartConstMap(entries) {
  const map = __dartMapFromEntries(entries);
  const throwConst = () => { throw new TypeError("Cannot modify const Map"); };
  Object.defineProperty(map, "set", { value: throwConst });
  Object.defineProperty(map, "delete", { value: throwConst });
  Object.defineProperty(map, "clear", { value: throwConst });
  return Object.freeze(map);
}

const __dartConstValues = new Map();
function __dartConst(key, create) {
  if (!__dartConstValues.has(key)) {
    __dartConstValues.set(key, create());
  }
  return __dartConstValues.get(key);
}

function __dartEquals(left, right) {
  if (left === right) return true;
  if (left == null || right == null) return false;
  if ((typeof left === "number" || left.__dartType === "double") && (typeof right === "number" || right.__dartType === "double")) return Number(left) === Number(right);
  if (__dartIsRecord(left) && __dartIsRecord(right)) {
    const leftShape = left[__dartRecordShape];
    const rightShape = right[__dartRecordShape];
    if (leftShape.length !== rightShape.length) return false;
    for (let i = 0; i < leftShape.length; i++) {
      const name = leftShape[i];
      if (name !== rightShape[i]) return false;
      if (!__dartEquals(left[name], right[name])) return false;
    }
    return true;
  }
  const equals = left["=="];
  return typeof equals === "function" ? equals.call(left, right) : false;
}

function __dartIterableToArray(iterable) {
  if (Array.isArray(iterable)) return Array.from(iterable);
  if (iterable != null && typeof iterable["[]"] === "function" && typeof iterable.length === "number") {
    return Array.from({ length: Number(iterable.length) }, (_, index) => iterable["[]"](index));
  }
  return Array.from(iterable);
}

function __dartIterator(iterable) {
  const values = __dartIterableToArray(iterable);
  let index = -1;
  return {
    current: undefined,
    moveNext() {
      index++;
      if (index < values.length) {
        this.current = values[index];
        return true;
      }
      this.current = undefined;
      return false;
    },
  };
}

function __dartLazyField(name, initialize, writable, publish = null) {
  let state = 0;
  let value;
  function get() {
    if (state === 2) return value;
    if (state === 1) {
      throw new Error("Cyclic initialization of field " + name);
    }
    if (initialize == null) {
      throw new Error("Late field " + name + " has not been initialized");
    }
    state = 1;
    try {
      value = initialize();
      if (publish) publish(value);
      state = 2;
      return value;
    } catch (error) {
      state = 0;
      throw error;
    }
  }
  function set(next) {
    if (writable === false || (writable === "once" && state === 2)) {
      throw new TypeError("Cannot assign to final field " + name);
    }
    value = next;
    if (publish) publish(value);
    state = 2;
    return next;
  }
  return { get, set };
}

function __dartListAdd(list, value) {
  if (Array.isArray(list)) {
    list.push(value);
  } else if (list != null && typeof list.add === "function") {
    list.add(value);
  } else {
    const index = list.length;
    list.length = index + 1;
    __dartListLikeSet(list, index, value);
  }
  return null;
}

function __dartListAddAll(list, values) {
  for (const value of __dartIterableToArray(values)) {
    __dartListAdd(list, value);
  }
  return null;
}

function __dartFixedList(values) {
  const list = __dartIterableToArray(values);
  Object.preventExtensions(list);
  return list;
}
function __dartListOf(values, growable = true) {
  const list = __dartIterableToArray(values);
  return growable ? list : __dartFixedList(list);
}
function __dartListFilled(length, fill, growable = false) {
  const list = Array(Number(length)).fill(fill);
  return growable ? list : __dartFixedList(list);
}
function __dartListGenerate(length, generator, growable = true) {
  const list = Array.from({ length: Number(length) }, (_, index) => generator(index));
  return growable ? list : __dartFixedList(list);
}
function __dartUnmodifiableList(values) {
  return Object.freeze(__dartIterableToArray(values));
}

function __dartListLikeGet(list, index) {
  if (Array.isArray(list) || ArrayBuffer.isView(list) || typeof list === "string") return list[index];
  const op = list == null ? null : list["[]"];
  return typeof op === "function" ? op.call(list, index) : list[index];
}
function __dartListLikeSet(list, index, value) {
  if (Array.isArray(list) || ArrayBuffer.isView(list)) {
    list[index] = value;
    return value;
  }
  const op = list == null ? null : list["[]="];
  if (typeof op === "function") return op.call(list, index, value);
  list[index] = value;
  return value;
}
function __dartListMixinFirst(list) {
  return __dartListLikeGet(list, 0);
}
function __dartListMixinLast(list) {
  return __dartListLikeGet(list, list.length - 1);
}
function __dartListMixinSingle(list) {
  if (list.length !== 1) throw __dartCoreError("StateError", "Too many elements");
  return __dartListLikeGet(list, 0);
}
function __dartListMixinInsert(list, index, value) {
  index = Number(index);
  const length = Number(list.length);
  list.length = length + 1;
  for (let i = length; i > index; i--) {
    __dartListLikeSet(list, i, __dartListLikeGet(list, i - 1));
  }
  __dartListLikeSet(list, index, value);
  return null;
}

function __dartListMutationRead(list, index) {
  return list != null && typeof list["[]"] === "function" ? list["[]"](index) : list[index];
}
function __dartListMutationWrite(list, index, value) {
  if (list != null && typeof list["[]="] === "function") {
    list["[]="](index, value);
  } else {
    list[index] = value;
  }
}
function __dartListMutationValues(values) {
  return __dartIterableToArray(values);
}
function __dartListShuffle(list, random = null) {
  for (let index = list.length - 1; index > 0; index--) {
    const nextInt = random == null ? Math.floor(Math.random() * (index + 1)) : Number(random.nextInt(index + 1));
    const current = __dartListMutationRead(list, index);
    __dartListMutationWrite(list, index, __dartListMutationRead(list, nextInt));
    __dartListMutationWrite(list, nextInt, current);
  }
  return null;
}
function __dartListRemoveAt(list, index) {
  index = Number(index);
  const value = __dartListMutationRead(list, index);
  __dartListRemoveRange(list, index, index + 1);
  return value;
}
function __dartListInsert(list, index, value) {
  index = Number(index);
  if (Array.isArray(list)) {
    list.splice(index, 0, value);
    return null;
  }
  const length = Number(list.length);
  list.length = length + 1;
  for (let cursor = length; cursor > index; cursor--) {
    __dartListMutationWrite(list, cursor, __dartListMutationRead(list, cursor - 1));
  }
  __dartListMutationWrite(list, index, value);
  return null;
}
function __dartListRemove(list, value) {
  for (let index = 0; index < list.length; index++) {
    if (__dartEquals(__dartListMutationRead(list, index), value)) {
      __dartListRemoveRange(list, index, index + 1);
      return true;
    }
  }
  return false;
}
function __dartListRemoveLast(list) {
  return __dartListRemoveAt(list, list.length - 1);
}
function __dartListInsertAll(list, index, values) {
  index = Number(index);
  const inserted = __dartListMutationValues(values);
  if (Array.isArray(list)) {
    list.splice(index, 0, ...inserted);
    return null;
  }
  const length = Number(list.length);
  list.length = length + inserted.length;
  for (let cursor = length - 1; cursor >= index; cursor--) {
    __dartListMutationWrite(list, cursor + inserted.length, __dartListMutationRead(list, cursor));
  }
  for (let cursor = 0; cursor < inserted.length; cursor++) {
    __dartListMutationWrite(list, index + cursor, inserted[cursor]);
  }
  return null;
}
function __dartListSetAll(list, index, values) {
  let cursor = Number(index);
  for (const value of __dartListMutationValues(values)) {
    __dartListMutationWrite(list, cursor++, value);
  }
  return null;
}
function __dartListFillRange(list, start, end, fill = null) {
  for (let index = Number(start); index < Number(end); index++) {
    __dartListMutationWrite(list, index, fill);
  }
  return null;
}
function __dartListReplaceRange(list, start, end, values) {
  start = Number(start);
  end = Number(end);
  const replacement = __dartListMutationValues(values);
  __dartListRemoveRange(list, start, end);
  __dartListInsertAll(list, start, replacement);
  return null;
}
function __dartListRemoveRange(list, start, end) {
  start = Number(start);
  end = Number(end);
  if (Array.isArray(list)) {
    list.splice(start, end - start);
    return null;
  }
  const length = Number(list.length);
  const count = end - start;
  for (let cursor = end; cursor < length; cursor++) {
    __dartListMutationWrite(list, cursor - count, __dartListMutationRead(list, cursor));
  }
  list.length = length - count;
  return null;
}
function __dartListRemoveWhere(list, test) {
  for (let index = list.length - 1; index >= 0; index--) {
    if (test(__dartListMutationRead(list, index))) {
      __dartListRemoveRange(list, index, index + 1);
    }
  }
  return null;
}
function __dartListRetainWhere(list, test) {
  for (let index = list.length - 1; index >= 0; index--) {
    if (!test(__dartListMutationRead(list, index))) {
      __dartListRemoveRange(list, index, index + 1);
    }
  }
  return null;
}
function __dartListAsMap(list) {
  const map = new Map();
  for (let index = 0; index < list.length; index++) map.set(index, __dartListMutationRead(list, index));
  return map;
}

function __dartMapAddAll(map, entries) {
  for (const [key, value] of entries) __dartMapSet(map, key, value);
  return null;
}

function __dartMapContainsKey(map, key) {
  if (!(map instanceof Map) && map != null && typeof map.containsKey === "function") return map.containsKey(key);
  return __dartMapKey(map, key) !== __dartMapMissingKey;
}

function __dartMapFromEntries(entries) {
  const map = new Map();
  Object.defineProperty(map, "__dartEqualityMap", { value: true });
  __dartMapAddAll(map, entries);
  return map;
}
function __dartMapFromIterable(iterable, key = null, value = null) {
  const map = new Map();
  Object.defineProperty(map, "__dartEqualityMap", { value: true });
  for (const element of iterable) {
    __dartMapSet(
      map,
      typeof key === "function" ? key(element) : element,
      typeof value === "function" ? value(element) : element,
    );
  }
  return map;
}
function __dartMapFromIterables(keys, values) {
  const keyList = Array.from(keys);
  const valueList = Array.from(values);
  if (keyList.length !== valueList.length) throw new Error("Iterables do not have same length");
  const map = new Map();
  Object.defineProperty(map, "__dartEqualityMap", { value: true });
  for (let index = 0; index < keyList.length; index++) {
    __dartMapSet(map, keyList[index], valueList[index]);
  }
  return map;
}

const __dartMapMissingKey = Symbol("dart.mapMissingKey");
function __dartMapKey(map, key) {
  if (typeof map.__dartSplayIsValidKey === "function" && !map.__dartSplayIsValidKey(key)) return __dartMapMissingKey;
  if (map.__dartSplayCompare !== undefined) {
    for (const candidate of map.keys()) {
      if (__dartCompare(candidate, key, map.__dartSplayCompare) === 0) return candidate;
    }
    return __dartMapMissingKey;
  }
  if (typeof map.__dartMapIsValidKey === "function" && !map.__dartMapIsValidKey(key)) return __dartMapMissingKey;
  if (typeof map.__dartMapEquals === "function") {
    for (const candidate of map.keys()) {
      if (map.__dartMapEquals(candidate, key)) return candidate;
    }
    return __dartMapMissingKey;
  }
  if (!map.__dartEqualityMap) return map.has(key) ? key : __dartMapMissingKey;
  for (const candidate of map.keys()) {
    if (__dartEquals(candidate, key)) return candidate;
  }
  return __dartMapMissingKey;
}
function __dartMapGet(map, key) {
  if (!(map instanceof Map) && map != null && typeof map["[]"] === "function") return map["[]"](key);
  const actualKey = __dartMapKey(map, key);
  return actualKey === __dartMapMissingKey ? null : map.get(actualKey);
}

function __dartMapAddEntries(map, entries) {
  for (const [key, value] of entries) __dartMapSet(map, key, value);
  return null;
}
function __dartMapContainsValue(map, value) {
  for (const candidate of map.values()) {
    if (__dartEquals(candidate, value)) return true;
  }
  return false;
}
function __dartMapPutIfAbsent(map, key, ifAbsent) {
  const actualKey = __dartMapKey(map, key);
  if (actualKey !== __dartMapMissingKey) return map.get(actualKey);
  const value = ifAbsent();
  __dartMapSet(map, key, value);
  return value;
}
function __dartMapUpdate(map, key, update, ifAbsent = null) {
  const actualKey = __dartMapKey(map, key);
  if (actualKey !== __dartMapMissingKey) {
    const value = update(map.get(actualKey));
    map.set(actualKey, value);
    return value;
  }
  if (typeof ifAbsent === "function") {
    const value = ifAbsent();
    __dartMapSet(map, key, value);
    return value;
  }
  throw new Error("Key not found");
}
function __dartMapForEach(map, action) {
  for (const [key, value] of map) action(key, value);
  return null;
}
function __dartMapMap(map, transform) {
  const result = new Map();
  Object.defineProperty(result, "__dartEqualityMap", { value: true });
  for (const [key, value] of map) {
    const entry = transform(key, value);
    __dartMapSet(result, entry[0], entry[1]);
  }
  return result;
}
function __dartMapRemove(map, key) {
  const actualKey = __dartMapKey(map, key);
  if (actualKey === __dartMapMissingKey) return null;
  const value = map.get(actualKey);
  map.delete(actualKey);
  return value;
}
function __dartMapUpdateAll(map, update) {
  for (const [key, value] of Array.from(map)) {
    map.set(key, update(key, value));
  }
  return null;
}
function __dartMapRemoveWhere(map, test) {
  for (const [key, value] of Array.from(map)) {
    if (test(key, value)) map.delete(key);
  }
  return null;
}
function __dartMapClear(map) {
  map.clear();
  return null;
}

function __dartMapSet(map, key, value) {
  const actualKey = __dartMapKey(map, key);
  map.set(actualKey === __dartMapMissingKey ? key : actualKey, value);
  if (map.__dartSplayCompare !== undefined) __dartSplaySortMap(map);
  return value;
}

function __dartNullCheck(value) {
  if (value == null) throw new TypeError("Null check operator used on a null value");
  return value;
}

function __dartPatternRegExp(pattern, global = false) {
  if (pattern != null && typeof pattern.__dartRegExpMake === "function") return pattern.__dartRegExpMake(global);
  if (pattern instanceof RegExp) {
    let flags = pattern.flags;
    flags = global ? (flags.includes("g") ? flags : flags + "g") : flags.replace(/g/g, "");
    return new RegExp(pattern.source, flags);
  }
  return null;
}
function __dartPatternAllMatches(pattern, input, start = 0) {
  if (pattern != null && typeof pattern !== "string" && !(pattern instanceof RegExp) && typeof pattern.allMatches === "function") return pattern.allMatches(input, start);
  const text = String(input);
  const regexp = __dartPatternRegExp(pattern, true);
  if (regexp != null) {
    const matches = [];
    regexp.lastIndex = start;
    let match;
    while ((match = regexp.exec(text)) !== null) {
      matches.push(__dartRegExpMatch(match, 0, text, pattern));
      if (match[0] === "") regexp.lastIndex++;
    }
    return matches;
  }
  return __dartStringAllMatches(pattern, text, start);
}
function __dartPatternMatchAsPrefix(pattern, input, start = 0) {
  if (pattern != null && typeof pattern !== "string" && !(pattern instanceof RegExp) && typeof pattern.matchAsPrefix === "function") return pattern.matchAsPrefix(input, start);
  const text = String(input);
  const regexp = __dartPatternRegExp(pattern, false);
  if (regexp != null) {
    const match = regexp.exec(text.slice(start));
    return match == null || match.index !== 0 ? null : __dartRegExpMatch(match, start, text, pattern);
  }
  return __dartStringMatchAsPrefix(pattern, text, start);
}
function __dartStringContains(source, pattern, start = 0) {
  const text = String(source);
  const regexp = __dartPatternRegExp(pattern, false);
  if (regexp != null) return regexp.test(text.slice(start));
  return text.includes(String(pattern), start);
}
function __dartStringStartsWith(source, pattern, start = 0) {
  const text = String(source);
  const regexp = __dartPatternRegExp(pattern, false);
  if (regexp != null) {
    const match = regexp.exec(text.slice(start));
    return match != null && match.index === 0;
  }
  return text.startsWith(String(pattern), start);
}
function __dartStringIndexOf(source, pattern, start = 0) {
  const text = String(source);
  const regexp = __dartPatternRegExp(pattern, false);
  if (regexp != null) {
    const match = regexp.exec(text.slice(start));
    return match == null ? -1 : start + match.index;
  }
  return text.indexOf(String(pattern), start);
}
function __dartStringLastIndexOf(source, pattern, start = null) {
  const text = String(source);
  const limit = start == null ? text.length : start;
  const regexp = __dartPatternRegExp(pattern, false);
  if (regexp != null) {
    for (let index = Math.min(limit, text.length); index >= 0; index--) {
      const match = regexp.exec(text.slice(index));
      if (match != null && match.index === 0) return index;
    }
    return -1;
  }
  return text.lastIndexOf(String(pattern), limit);
}
function __dartStringSplit(source, pattern) {
  const text = String(source);
  const regexp = __dartPatternRegExp(pattern, false);
  return regexp == null ? text.split(String(pattern)) : text.split(regexp);
}
function __dartStringReplaceAll(source, pattern, replacement) {
  const text = String(source);
  const replacementText = String(replacement);
  const regexp = __dartPatternRegExp(pattern, true);
  return regexp == null ? text.split(String(pattern)).join(replacementText) : text.replace(regexp, () => replacementText);
}
function __dartStringReplaceAllMapped(source, pattern, replace) {
  const text = String(source);
  const regexp = __dartPatternRegExp(pattern, true);
  if (regexp != null) return text.replace(regexp, (...args) => String(replace(__dartJsRegExpReplacementMatch(args))));
  return __dartStringReplaceStringMapped(text, String(pattern), replace, true, 0);
}
function __dartStringReplaceFirstMapped(source, pattern, replace, startIndex = 0) {
  const text = String(source);
  const regexp = __dartPatternRegExp(pattern, false);
  if (regexp != null) {
    const tail = text.slice(startIndex);
    const match = regexp.exec(tail);
    if (match == null) return text;
    const dartMatch = __dartRegExpMatch(match, startIndex);
    return text.slice(0, dartMatch.start) + String(replace(dartMatch)) + text.slice(dartMatch.end);
  }
  return __dartStringReplaceStringMapped(text, String(pattern), replace, false, startIndex);
}
function __dartStringReplaceStringMapped(text, needle, replace, all, startIndex) {
  if (needle === "") return text;
  let result = "";
  let cursor = 0;
  let index = text.indexOf(needle, startIndex);
  let replaced = false;
  while (index >= 0) {
    result += text.slice(cursor, index);
    result += String(replace(__dartStringMatch(text, index, needle)));
    cursor = index + needle.length;
    replaced = true;
    if (!all) break;
    index = text.indexOf(needle, cursor);
  }
  return replaced ? result + text.slice(cursor) : text;
}
function __dartStringSplitMapJoin(source, pattern, onMatch = null, onNonMatch = null) {
  const text = String(source);
  const matchMapper = typeof onMatch === "function" ? onMatch : (match) => match.group(0);
  const nonMatchMapper = typeof onNonMatch === "function" ? onNonMatch : (part) => part;
  const matches = __dartStringPatternMatches(text, pattern);
  if (matches.length === 0) return String(nonMatchMapper(text));
  let result = "";
  let cursor = 0;
  for (const match of matches) {
    result += String(nonMatchMapper(text.slice(cursor, match.start)));
    result += String(matchMapper(match));
    cursor = match.end;
  }
  result += String(nonMatchMapper(text.slice(cursor)));
  return result;
}
function __dartStringPatternMatches(text, pattern) {
  const regexp = __dartPatternRegExp(pattern, true);
  if (regexp != null) {
    const matches = [];
    let match;
    while ((match = regexp.exec(text)) !== null) {
      matches.push(__dartRegExpMatch(match));
      if (match[0] === "") regexp.lastIndex++;
    }
    return matches;
  }
  const needle = String(pattern);
  if (needle === "") return [];
  const matches = [];
  let index = text.indexOf(needle);
  while (index >= 0) {
    matches.push(__dartStringMatch(text, index, needle));
    index = text.indexOf(needle, index + needle.length);
  }
  return matches;
}
function __dartStringAllMatches(pattern, input, start = 0) {
  const text = String(input);
  const needle = String(pattern);
  const matches = [];
  if (needle === "") return matches;
  let index = text.indexOf(needle, start);
  while (index >= 0) {
    matches.push(__dartStringMatch(text, index, needle));
    index = text.indexOf(needle, index + needle.length);
  }
  return matches;
}
function __dartStringMatchAsPrefix(pattern, input, start = 0) {
  const text = String(input);
  const needle = String(pattern);
  return text.startsWith(needle, start) ? __dartStringMatch(text, start, needle) : null;
}
function __dartStringMatch(input, start, value) {
  return {
    input,
    start,
    end: start + value.length,
    get groupCount() { return 0; },
    group(index) { return index === 0 ? value : null; },
    groups(indices) { return Array.from(indices, (index) => this.group(index)); },
    namedGroup() { return null; },
    get groupNames() { return new Set(); },
    0: value,
  };
}
function __dartRegExpMatch(match, offset = 0, input = null, pattern = null) {
  const namedGroups = match.groups ?? {};
  const result = {
    start: offset + match.index,
    end: offset + match.index + match[0].length,
    get input() { return input; },
    get pattern() { return pattern; },
    get groupCount() { return match.length - 1; },
    group(index) { return index >= 0 && index < match.length ? (match[index] ?? null) : null; },
    groups(indices) { return Array.from(indices, (index) => this.group(index)); },
    namedGroup(name) { return Object.prototype.hasOwnProperty.call(namedGroups, name) ? (namedGroups[name] ?? null) : null; },
    get groupNames() { return new Set(Object.keys(namedGroups)); },
  };
  for (let i = 0; i < match.length; i++) {
    result[i] = match[i] ?? null;
  }
  return result;
}
function __dartJsRegExpReplacementMatch(args) {
  const hasNamedGroups = args.length > 0 && args[args.length - 1] != null && typeof args[args.length - 1] === "object";
  const input = args[args.length - (hasNamedGroups ? 2 : 1)];
  const offset = args[args.length - (hasNamedGroups ? 3 : 2)];
  const match = Array.prototype.slice.call(args, 0, args.length - (hasNamedGroups ? 3 : 2));
  match.index = offset;
  match.input = input;
  if (hasNamedGroups) match.groups = args[args.length - 1];
  return __dartRegExpMatch(match);
}
function __dartStringReplaceFirstPattern(source, pattern, replacement, startIndex = 0) {
  const text = String(source);
  const replacementText = String(replacement);
  const regexp = __dartPatternRegExp(pattern, false);
  if (regexp != null) {
    const tail = text.slice(startIndex);
    const match = regexp.exec(tail);
    if (match == null) return text;
    const index = startIndex + match.index;
    return text.slice(0, index) + replacementText + text.slice(index + match[0].length);
  }
  return __dartStringReplaceFirst(source, pattern, replacement, startIndex);
}

function __dartPrint(value) {
  console.log(__dartStr(value));
}

const __dartRecordShape = Symbol("dart.recordShape");

function __dartIsRecord(value) {
  return value != null && typeof value === "object" && Array.isArray(value[__dartRecordShape]);
}

function __dartRegExp(pattern, options = {}) {
  const source = String(pattern);
  const caseSensitive = options.caseSensitive !== false;
  const multiLine = options.multiLine === true;
  const unicode = options.unicode === true;
  const dotAll = options.dotAll === true;
  function make(global = false) {
    let flags = global ? "g" : "";
    if (!caseSensitive) flags += "i";
    if (multiLine) flags += "m";
    if (unicode) flags += "u";
    if (dotAll) flags += "s";
    return new RegExp(source, flags);
  }
  function displayFlags() {
    return (caseSensitive ? "" : "i") + (multiLine ? "m" : "") + (dotAll ? "s" : "") + (unicode ? "u" : "");
  }
  return {
    __dartRegExpMake: make,
    pattern: source,
    isCaseSensitive: caseSensitive,
    isMultiLine: multiLine,
    isUnicode: unicode,
    isDotAll: dotAll,
    hasMatch(input) { return make(false).test(String(input)); },
    firstMatch(input) {
      const text = String(input);
      const match = make(false).exec(text);
      return match == null ? null : __dartRegExpMatch(match, 0, text, this);
    },
    stringMatch(input) {
      const match = this.firstMatch(input);
      return match == null ? null : match.group(0);
    },
    matchAsPrefix(input, start = 0) {
      const sourceText = String(input);
      const text = sourceText.slice(start);
      const match = make(false).exec(text);
      return match == null || match.index !== 0 ? null : __dartRegExpMatch(match, start, sourceText, this);
    },
    allMatches(input, start = 0) {
      const text = String(input);
      const regexp = make(true);
      regexp.lastIndex = start;
      const matches = [];
      let match;
      while ((match = regexp.exec(text)) !== null) {
        matches.push(__dartRegExpMatch(match, 0, text, this));
        if (match[0] === "") regexp.lastIndex++;
      }
      return matches;
    },
    toString() { return "RegExp: pattern=" + source + " flags=" + displayFlags(); },
  };
}

function __dartSetContains(set, needle) {
  if (typeof set.__dartSplayIsValidKey === "function" && !set.__dartSplayIsValidKey(needle)) return false;
  if (set.__dartSplayCompare !== undefined) {
    for (const value of set) {
      if (__dartCompare(value, needle, set.__dartSplayCompare) === 0) return true;
    }
    return false;
  }
  if (!set.__dartEqualitySet) return set.has(needle);
  for (const value of set) {
    if (__dartEquals(value, needle)) return true;
  }
  return false;
}
function __dartSetAdd(set, value) {
  if (__dartSetContains(set, value)) return false;
  set.add(value);
  if (set.__dartSplayCompare !== undefined) __dartSplaySortSet(set);
  return true;
}
function __dartSetFrom(values) {
  const set = new Set();
  Object.defineProperty(set, "__dartEqualitySet", { value: true });
  for (const value of values) __dartSetAdd(set, value);
  return set;
}
function __dartSetAddAll(set, values) {
  for (const value of values) __dartSetAdd(set, value);
  return null;
}

function __dartStringBuffer(initial = "") {
  let value = initial == null ? "" : __dartStr(initial);
  return {
    write(next) {
      value += __dartStr(next);
      return null;
    },
    writeln(next = "") {
      value += __dartStr(next) + "\n";
      return null;
    },
    writeAll(values, separator = "") {
      const parts = __dartIterableToArray(values).map((item) => __dartStr(item));
      value += parts.join(__dartStr(separator));
      return null;
    },
    writeCharCode(charCode) {
      value += String.fromCodePoint(Number(charCode));
      return null;
    },
    clear() {
      value = "";
      return null;
    },
    toString() {
      return value;
    },
    get length() {
      return value.length;
    },
    get isEmpty() {
      return value.length === 0;
    },
    get isNotEmpty() {
      return value.length !== 0;
    },
  };
}

function __dartStringCodeUnits(source) {
  const text = String(source);
  return Array.from({ length: text.length }, (_, index) => text.charCodeAt(index));
}
function __dartStringReplaceFirst(source, pattern, replacement, startIndex = 0) {
  const text = String(source);
  const needle = String(pattern);
  const index = text.indexOf(needle, Number(startIndex));
  if (index < 0) return text;
  return text.slice(0, index) + String(replacement) + text.slice(index + needle.length);
}
function __dartStringReplaceRange(source, start, end, replacement) {
  const text = String(source);
  const actualEnd = end == null ? text.length : Number(end);
  return text.slice(0, Number(start)) + String(replacement) + text.slice(actualEnd);
}

function __dartStr(value) {
  if (value == null) return "null";
  if (Array.isArray(value)) {
    return "[" + value.map(__dartStr).join(", ") + "]";
  }
  if (value instanceof Set) {
    return "{" + Array.from(value).map(__dartStr).join(", ") + "}";
  }
  if (value instanceof Map) {
    return "{" + Array.from(value, ([key, entryValue]) => __dartStr(key) + ": " + __dartStr(entryValue)).join(", ") + "}";
  }
  if (typeof value === "object") {
    const toString = value.toString;
    if (typeof toString === "function" && toString !== Object.prototype.toString) {
      return String(toString.call(value));
    }
  }
  return String(value);
}

function __dartAs(value, test, typeName) {
  if (test(value)) {
    return value;
  }
  throw new TypeError("Type cast failed: expected " + typeName);
}

function __dartUnmodifiableListView(source) {
  const list = Array.isArray(source) ? source : Array.from(source);
  const readonly = new Set(["copyWithin", "fill", "pop", "push", "reverse", "shift", "sort", "splice", "unshift"]);
  return new Proxy(list, {
    get(target, property, receiver) {
      if (readonly.has(property)) return () => { throw new TypeError("Unsupported operation: Cannot modify an unmodifiable list"); };
      return Reflect.get(target, property, receiver);
    },
    set() { throw new TypeError("Unsupported operation: Cannot modify an unmodifiable list"); },
    deleteProperty() { throw new TypeError("Unsupported operation: Cannot modify an unmodifiable list"); },
    defineProperty() { throw new TypeError("Unsupported operation: Cannot modify an unmodifiable list"); },
  });
}
function __dartUnmodifiableMapView(source) {
  const mapLike = source != null && typeof source === "object" && (source instanceof Map || typeof source["[]"] === "function");
  const map = mapLike ? source : new Map(source);
  const readonly = new Set(["set", "delete", "clear", "[]=", "addAll", "addEntries", "remove", "removeWhere", "update", "updateAll", "putIfAbsent"]);
  return new Proxy(map, {
    get(target, property) {
      if (readonly.has(property)) return () => { throw new TypeError("Unsupported operation: Cannot modify an unmodifiable map"); };
      const descriptor = Reflect.getOwnPropertyDescriptor(target, property);
      if (descriptor != null && "value" in descriptor) return descriptor.value;
      const value = Reflect.get(target, property, target);
      return typeof value === "function" ? value.bind(target) : value;
    },
    set() { throw new TypeError("Unsupported operation: Cannot modify an unmodifiable map"); },
    deleteProperty() { throw new TypeError("Unsupported operation: Cannot modify an unmodifiable map"); },
    defineProperty() { throw new TypeError("Unsupported operation: Cannot modify an unmodifiable map"); },
  });
}

class ArgResults {
  constructor() {
    throw new TypeError("Class ArgResults has no unnamed constructor");
  }
  static __package_args_src_arg_results_dart(_parser, _parsed, name, command, rest, $arguments) {
    const $self = Object.create(this.prototype);
    Object.defineProperty($self, "_parser_package_args_src_arg_results_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_parsed_package_args_src_arg_results_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "name", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "command", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "rest", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "$arguments", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_parser_package_args_src_arg_results_dart", { value: _parser, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_parsed_package_args_src_arg_results_dart", { value: _parsed, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "name", { value: name, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "command", { value: command, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "rest", { value: __dartUnmodifiableListView(rest), writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "$arguments", { value: __dartUnmodifiableListView($arguments), writable: true, enumerable: true, configurable: true });
    return $self;
  }
  "[]"(name) {
    if (!(__dartMapContainsKey(this._parser_package_args_src_arg_results_dart.options, name))) {
      throw __dartCoreError("ArgumentError", `Could not find an option named "--${__dartStr(name)}".`);
    }
    const option = __dartNullCheck(__dartMapGet(this._parser_package_args_src_arg_results_dart.options, name));
    if ((option.mandatory && !(__dartMapContainsKey(this._parsed_package_args_src_arg_results_dart, name)))) {
      throw __dartCoreError("ArgumentError", `Option ${__dartStr(name)} is mandatory.`);
    }
    return option.valueOrDefault(__dartMapGet(this._parsed_package_args_src_arg_results_dart, name));
  }
  flag(name) {
    const option = __dartMapGet(this._parser_package_args_src_arg_results_dart.options, name);
    if (option === null) {
      throw __dartCoreError("ArgumentError", `Could not find a flag named "--${__dartStr(name)}".`);
    }
    if (!(option.isFlag)) {
      throw __dartCoreError("ArgumentError", `"${__dartStr(name)}" is not a flag.`);
    }
    return __dartAs(option.valueOrDefault(__dartMapGet(this._parsed_package_args_src_arg_results_dart, name)), (value) => typeof value === "boolean", "bool");
  }
  option(name) {
    const option = __dartMapGet(this._parser_package_args_src_arg_results_dart.options, name);
    if (option === null) {
      throw __dartCoreError("ArgumentError", `Could not find an option named "--${__dartStr(name)}".`);
    }
    if (!(option.isSingle)) {
      throw __dartCoreError("ArgumentError", `"${__dartStr(name)}" is a multi-option.`);
    }
    if ((option.mandatory && !(__dartMapContainsKey(this._parsed_package_args_src_arg_results_dart, name)))) {
      throw __dartCoreError("ArgumentError", `Option ${__dartStr(name)} is mandatory.`);
    }
    return __dartAs(option.valueOrDefault(__dartMapGet(this._parsed_package_args_src_arg_results_dart, name)), (value) => (value === null || typeof value === "string"), "String");
  }
  multiOption(name) {
    let option = __dartMapGet(this._parser_package_args_src_arg_results_dart.options, name);
    if (option === null) {
      throw __dartCoreError("ArgumentError", `Could not find an option named "--${__dartStr(name)}".`);
    }
    if (!(option.isMultiple)) {
      throw __dartCoreError("ArgumentError", `"${__dartStr(name)}" is not a multi-option.`);
    }
    return __dartAs(option.valueOrDefault(__dartMapGet(this._parsed_package_args_src_arg_results_dart, name)), (value) => (Array.isArray(value) || (ArrayBuffer.isView(value) && !(value instanceof DataView))), "List");
  }
  get options() {
    let result = __dartSetFrom(__dartIterableToArray(this._parsed_package_args_src_arg_results_dart.keys()));
    __dartMapForEach(this._parser_package_args_src_arg_results_dart.options, (name, option) => {
      if (!(option.defaultsTo === null)) {
        __dartSetAdd(result, name);
      }
    });
    return result;
  }
  wasParsed(name) {
    if (!(__dartMapContainsKey(this._parser_package_args_src_arg_results_dart.options, name))) {
      throw __dartCoreError("ArgumentError", `Could not find an option named "--${__dartStr(name)}".`);
    }
    return __dartMapContainsKey(this._parsed_package_args_src_arg_results_dart, name);
  }
}

class Option {
  constructor() {
    throw new TypeError("Class Option has no unnamed constructor");
  }
  static __package_args_src_option_dart(name, abbr, help, valueHelp, allowed, allowedHelp, defaultsTo, callback, type, { negatable = null, splitCommas = null, mandatory = false, hide = false, hideNegatedUsage = null, aliases = __dartConst("[\"list\",\"InterfaceType(String)\"]", () => Object.freeze([])) } = {}) {
    const $self = Object.create(this.prototype);
    Object.defineProperty($self, "name", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "abbr", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "help", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "valueHelp", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "allowed", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "allowedHelp", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "defaultsTo", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "negatable", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "hideNegatedUsage", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "callback", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "type", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "splitCommas", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "mandatory", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "hide", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "aliases", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "name", { value: name, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "abbr", { value: abbr, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "help", { value: help, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "valueHelp", { value: valueHelp, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "defaultsTo", { value: defaultsTo, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "callback", { value: callback, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "type", { value: type, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "negatable", { value: negatable, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "mandatory", { value: mandatory, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "hide", { value: hide, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "hideNegatedUsage", { value: hideNegatedUsage, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "aliases", { value: aliases, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "allowed", { value: (allowed === null ? null : __dartUnmodifiableList(allowed)), writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "allowedHelp", { value: (allowedHelp === null ? null : __dartMapFromEntries(allowedHelp)), writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "splitCommas", { value: (splitCommas ?? __dartEquals(type, __dartConst("[\"InstanceConstant\",\"InstanceConstant(const OptionType{OptionType.name: \\\"OptionType.multiple\\\"})\"]", () => Object.freeze(Object.assign(Object.create(OptionType.prototype), { name: "OptionType.multiple" }))))), writable: true, enumerable: true, configurable: true });
    if ($self.name.length === 0) {
      throw __dartCoreError("ArgumentError", "Name cannot be empty.");
    } else {
      if ($self.name.startsWith("-")) {
        throw __dartCoreError("ArgumentError", `Name ${__dartStr($self.name)} cannot start with "-".`);
      }
    }
    if (Option._invalidChars_package_args_src_option_dart.hasMatch($self.name)) {
      throw __dartCoreError("ArgumentError", `Name "${__dartStr($self.name)}" contains invalid characters.`);
    }
    let abbr_1 = $self.abbr;
    if (!(abbr_1 === null)) {
      if (!(__dartEquals(abbr_1.length, 1))) {
        throw __dartCoreError("ArgumentError", "Abbreviation must be null or have length 1.");
      } else {
        if (__dartEquals(abbr_1, "-")) {
          throw __dartCoreError("ArgumentError", "Abbreviation cannot be \"-\".");
        }
      }
      if (Option._invalidChars_package_args_src_option_dart.hasMatch(abbr_1)) {
        throw __dartCoreError("ArgumentError", "Abbreviation is an invalid character.");
      }
    }
    return $self;
  }
  get isFlag() {
    return __dartEquals(this.type, __dartConst("[\"InstanceConstant\",\"InstanceConstant(const OptionType{OptionType.name: \\\"OptionType.flag\\\"})\"]", () => Object.freeze(Object.assign(Object.create(OptionType.prototype), { name: "OptionType.flag" }))));
  }
  get isSingle() {
    return __dartEquals(this.type, __dartConst("[\"InstanceConstant\",\"InstanceConstant(const OptionType{OptionType.name: \\\"OptionType.single\\\"})\"]", () => Object.freeze(Object.assign(Object.create(OptionType.prototype), { name: "OptionType.single" }))));
  }
  get isMultiple() {
    return __dartEquals(this.type, __dartConst("[\"InstanceConstant\",\"InstanceConstant(const OptionType{OptionType.name: \\\"OptionType.multiple\\\"})\"]", () => Object.freeze(Object.assign(Object.create(OptionType.prototype), { name: "OptionType.multiple" }))));
  }
  valueOrDefault(value) {
    if (!(value === null)) {
      return value;
    }
    if (this.isMultiple) {
      return (this.defaultsTo ?? Array(0).fill(null));
    }
    return this.defaultsTo;
  }
  getOrDefault(value) {
    return this.valueOrDefault(value);
  }
}

const $Option__invalidChars = __dartLazyField("Option._invalidChars", () => __dartRegExp("[ \\t\\r\\n\"'\\\\/]", { caseSensitive: true, multiLine: false, unicode: false, dotAll: false }), "once");
Object.defineProperty(Option, "_invalidChars_package_args_src_option_dart", { get: function() {
  return $Option__invalidChars.get();
}, set: function(value) {
  $Option__invalidChars.set(value);
}, enumerable: true });
class OptionType {
  constructor() {
    throw new TypeError("Class OptionType has no unnamed constructor");
  }
  static __package_args_src_option_dart(name) {
    const $self = Object.create(this.prototype);
    Object.defineProperty($self, "name", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "name", { value: name, writable: true, enumerable: true, configurable: true });
    return $self;
  }
}

const $OptionType_flag = __dartLazyField("OptionType.flag", () => __dartConst("[\"InstanceConstant\",\"InstanceConstant(const OptionType{OptionType.name: \\\"OptionType.flag\\\"})\"]", () => Object.freeze(Object.assign(Object.create(OptionType.prototype), { name: "OptionType.flag" }))), false);
Object.defineProperty(OptionType, "flag", { get: function() {
  return $OptionType_flag.get();
}, set: function(value) {
  $OptionType_flag.set(value);
}, enumerable: true });
const $OptionType_single = __dartLazyField("OptionType.single", () => __dartConst("[\"InstanceConstant\",\"InstanceConstant(const OptionType{OptionType.name: \\\"OptionType.single\\\"})\"]", () => Object.freeze(Object.assign(Object.create(OptionType.prototype), { name: "OptionType.single" }))), false);
Object.defineProperty(OptionType, "single", { get: function() {
  return $OptionType_single.get();
}, set: function(value) {
  $OptionType_single.set(value);
}, enumerable: true });
const $OptionType_multiple = __dartLazyField("OptionType.multiple", () => __dartConst("[\"InstanceConstant\",\"InstanceConstant(const OptionType{OptionType.name: \\\"OptionType.multiple\\\"})\"]", () => Object.freeze(Object.assign(Object.create(OptionType.prototype), { name: "OptionType.multiple" }))), false);
Object.defineProperty(OptionType, "multiple", { get: function() {
  return $OptionType_multiple.get();
}, set: function(value) {
  $OptionType_multiple.set(value);
}, enumerable: true });
class ArgParserException {
  constructor(message, commands = null, argumentName = null, source = null, offset = null) {
    Object.defineProperty(this, "__dartCoreErrorType", { value: "FormatException", writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "name", { value: "FormatException", writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "message", { value: message, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "source", { value: source, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "offset", { value: offset, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "toString", { value: function() {
      return (this.message === "" ? this.name : `${this.name}: ${this.message}`);
    }, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "commands", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "argumentName", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "argumentName", { value: argumentName, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "commands", { value: (commands === null ? __dartConst("[\"list\",\"InterfaceType(String)\"]", () => Object.freeze([])) : __dartUnmodifiableList(commands)), writable: true, enumerable: true, configurable: true });
  }
}

class Parser {
  constructor(_commandName, _grammar, _args, _parent = null, rest = null) {
    Object.defineProperty(this, "_commandName_package_args_src_parser_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_parent_package_args_src_parser_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_grammar_package_args_src_parser_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_args_package_args_src_parser_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_rest_package_args_src_parser_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_results_package_args_src_parser_dart", { value: __dartMapFromEntries([]), writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_commandName_package_args_src_parser_dart", { value: _commandName, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_grammar_package_args_src_parser_dart", { value: _grammar, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_args_package_args_src_parser_dart", { value: _args, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_parent_package_args_src_parser_dart", { value: _parent, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_rest_package_args_src_parser_dart", { value: (() => {
      const v = Array(0).fill(null);
      const v_1 = rest;
      if (!(v_1 === null)) {
        __dartListAddAll(v, v_1);
      }
      return v;
    })(), writable: true, enumerable: true, configurable: true });
  }
  get _current_package_args_src_parser_dart() {
    return Array.from(this._args_package_args_src_parser_dart)[0];
  }
  parse() {
    let $arguments = __dartListOf(this._args_package_args_src_parser_dart, true);
    if (this._grammar_package_args_src_parser_dart.allowsAnything) {
      return newArgResults(this._grammar_package_args_src_parser_dart, __dartConst("[\"map\",\"InterfaceType(String)\",\"DynamicType(dynamic)\"]", () => __dartConstMap([])), this._commandName_package_args_src_parser_dart, null, $arguments, $arguments);
    }
    let commandResults = null;
    label: {
      while (__dartIterableToArray(this._args_package_args_src_parser_dart).length > 0) {
        label_1: {
          if (__dartEquals(this._current_package_args_src_parser_dart, "--")) {
            this._args_package_args_src_parser_dart.shift();
            break label;
          }
          let command = __dartMapGet(this._grammar_package_args_src_parser_dart.commands, this._current_package_args_src_parser_dart);
          if (!(command === null)) {
            this._validate_package_args_src_parser_dart(__dartIterableToArray(this._rest_package_args_src_parser_dart).length === 0, "Cannot specify arguments before a command.", this._current_package_args_src_parser_dart);
            let commandName = this._args_package_args_src_parser_dart.shift();
            let commandParser = new Parser(commandName, command, this._args_package_args_src_parser_dart, this, this._rest_package_args_src_parser_dart);
            try {
              commandResults = commandParser.parse();
            } catch ($error) {
              if ($error instanceof ArgParserException) {
                const error = $error;
                throw new ArgParserException(error.message, (() => {
                  const v = [commandName];
                  __dartListAddAll(v, error.commands);
                  return v;
                })(), error.argumentName, error.source, error.offset);
              } else {
                throw $error;
              }
            }
            this._rest_package_args_src_parser_dart.length = 0;
            break label;
          }
          if (this._parseSoloOption_package_args_src_parser_dart()) {
            break label_1;
          }
          if (this._parseAbbreviation_package_args_src_parser_dart(this)) {
            break label_1;
          }
          if (this._parseLongOption_package_args_src_parser_dart()) {
            break label_1;
          }
          if (!(this._grammar_package_args_src_parser_dart.allowTrailingOptions)) {
            break label;
          }
          __dartListAdd(this._rest_package_args_src_parser_dart, this._args_package_args_src_parser_dart.shift());
        }
      }
    }
    __dartMapForEach(this._grammar_package_args_src_parser_dart.options, (name, option) => {
      let parsedOption = __dartMapGet(this._results_package_args_src_parser_dart, name);
      let callback = option.callback;
      if (callback === null) {
        return;
      }
      if ((option.mandatory && parsedOption === null)) {
        throw new ArgParserException(`Option ${__dartStr(name)} is mandatory.`, null, name);
      }
      callback(option.valueOrDefault(parsedOption));
    });
    __dartListAddAll(this._rest_package_args_src_parser_dart, this._args_package_args_src_parser_dart);
    this._args_package_args_src_parser_dart.length = 0;
    return newArgResults(this._grammar_package_args_src_parser_dart, this._results_package_args_src_parser_dart, this._commandName_package_args_src_parser_dart, commandResults, this._rest_package_args_src_parser_dart, $arguments);
  }
  _readNextArgAsValue_package_args_src_parser_dart(option, arg) {
    this._validate_package_args_src_parser_dart(__dartIterableToArray(this._args_package_args_src_parser_dart).length > 0, `Missing argument for "${__dartStr(arg)}".`, arg);
    this._setOption_package_args_src_parser_dart(this._results_package_args_src_parser_dart, option, this._current_package_args_src_parser_dart, arg);
    this._args_package_args_src_parser_dart.shift();
  }
  _parseSoloOption_package_args_src_parser_dart() {
    if (!(__dartEquals(this._current_package_args_src_parser_dart.length, 2))) {
      return false;
    }
    if (!(this._current_package_args_src_parser_dart.startsWith("-"))) {
      return false;
    }
    let opt = this._current_package_args_src_parser_dart[1];
    if (!(_isLetterOrDigit(opt.charCodeAt(0)))) {
      return false;
    }
    return this._handleSoloOption_package_args_src_parser_dart(opt);
  }
  _handleSoloOption_package_args_src_parser_dart(opt) {
    let option = this._grammar_package_args_src_parser_dart.findByAbbreviation(opt);
    if (option === null) {
      this._validate_package_args_src_parser_dart(!(this._parent_package_args_src_parser_dart === null), `Could not find an option or flag "-${__dartStr(opt)}".`, `-${__dartStr(opt)}`);
      return __dartNullCheck(this._parent_package_args_src_parser_dart)._handleSoloOption_package_args_src_parser_dart(opt);
    }
    this._args_package_args_src_parser_dart.shift();
    if (option.isFlag) {
      this._setFlag_package_args_src_parser_dart(this._results_package_args_src_parser_dart, option, true);
    } else {
      this._readNextArgAsValue_package_args_src_parser_dart(option, `-${__dartStr(opt)}`);
    }
    return true;
  }
  _parseAbbreviation_package_args_src_parser_dart(innermostCommand) {
    if (this._current_package_args_src_parser_dart.length < 2) {
      return false;
    }
    if (!(this._current_package_args_src_parser_dart.startsWith("-"))) {
      return false;
    }
    let index = 1;
    while ((index < this._current_package_args_src_parser_dart.length && _isLetterOrDigit(this._current_package_args_src_parser_dart.charCodeAt(index)))) {
      index = index + 1;
    }
    if (__dartEquals(index, 1)) {
      return false;
    }
    let lettersAndDigits = this._current_package_args_src_parser_dart.substring(1, index);
    let rest = this._current_package_args_src_parser_dart.substring(index);
    if ((rest.includes("\n") || rest.includes("\r"))) {
      return false;
    }
    return this._handleAbbreviation_package_args_src_parser_dart(lettersAndDigits, rest, innermostCommand);
  }
  _handleAbbreviation_package_args_src_parser_dart(lettersAndDigits, rest, innermostCommand) {
    let c = lettersAndDigits.substring(0, 1);
    let first = this._grammar_package_args_src_parser_dart.findByAbbreviation(c);
    if (first === null) {
      this._validate_package_args_src_parser_dart(!(this._parent_package_args_src_parser_dart === null), `Could not find an option with short name "-${__dartStr(c)}".`, `-${__dartStr(c)}`);
      return __dartNullCheck(this._parent_package_args_src_parser_dart)._handleAbbreviation_package_args_src_parser_dart(lettersAndDigits, rest, innermostCommand);
    } else {
      if (!(first.isFlag)) {
        let value = `${__dartStr(lettersAndDigits.substring(1))}${__dartStr(rest)}`;
        this._setOption_package_args_src_parser_dart(this._results_package_args_src_parser_dart, first, value, `-${__dartStr(c)}`);
      } else {
        this._validate_package_args_src_parser_dart(__dartEquals(rest, ""), `Option "-${__dartStr(c)}" is a flag and cannot handle value "${__dartStr(lettersAndDigits.substring(1))}${__dartStr(rest)}".`, `-${__dartStr(c)}`);
        for (let i = 0; i < lettersAndDigits.length; i = i + 1) {
          let c_1 = lettersAndDigits.substring(i, i + 1);
          innermostCommand._parseShortFlag_package_args_src_parser_dart(c_1);
        }
      }
    }
    this._args_package_args_src_parser_dart.shift();
    return true;
  }
  _parseShortFlag_package_args_src_parser_dart(c) {
    let option = this._grammar_package_args_src_parser_dart.findByAbbreviation(c);
    if (option === null) {
      this._validate_package_args_src_parser_dart(!(this._parent_package_args_src_parser_dart === null), `Could not find an option with short name "-${__dartStr(c)}".`, `-${__dartStr(c)}`);
      __dartNullCheck(this._parent_package_args_src_parser_dart)._parseShortFlag_package_args_src_parser_dart(c);
      return;
    }
    this._validate_package_args_src_parser_dart(option.isFlag, `Option "-${__dartStr(c)}" must be a flag to be in a collapsed "-".`, `-${__dartStr(c)}`);
    this._setFlag_package_args_src_parser_dart(this._results_package_args_src_parser_dart, option, true);
  }
  _parseLongOption_package_args_src_parser_dart() {
    if (!(this._current_package_args_src_parser_dart.startsWith("--"))) {
      return false;
    }
    let index = this._current_package_args_src_parser_dart.indexOf("=");
    let name = (__dartEquals(index, -1) ? this._current_package_args_src_parser_dart.substring(2) : this._current_package_args_src_parser_dart.substring(2, index));
    for (let i = 0; !(__dartEquals(i, name.length)); i = i + 1) {
      if (!(_isLetterDigitHyphenOrUnderscore(name.charCodeAt(i)))) {
        return false;
      }
    }
    let value = (__dartEquals(index, -1) ? null : this._current_package_args_src_parser_dart.substring(index + 1));
    if ((!(value === null) && (value.includes("\n") || value.includes("\r")))) {
      return false;
    }
    return this._handleLongOption_package_args_src_parser_dart(name, value);
  }
  _handleLongOption_package_args_src_parser_dart(name, value) {
    let option = this._grammar_package_args_src_parser_dart.findByNameOrAlias(name);
    if (!(option === null)) {
      this._args_package_args_src_parser_dart.shift();
      if (option.isFlag) {
        this._validate_package_args_src_parser_dart(value === null, `Flag option "--${__dartStr(name)}" should not be given a value.`, `--${__dartStr(name)}`);
        this._setFlag_package_args_src_parser_dart(this._results_package_args_src_parser_dart, option, true);
      } else {
        if (!(value === null)) {
          this._setOption_package_args_src_parser_dart(this._results_package_args_src_parser_dart, option, value, `--${__dartStr(name)}`);
        } else {
          this._readNextArgAsValue_package_args_src_parser_dart(option, `--${__dartStr(name)}`);
        }
      }
    } else {
      if (name.startsWith("no-")) {
        let positiveName = name.substring("no-".length);
        option = this._grammar_package_args_src_parser_dart.findByNameOrAlias(positiveName);
        if (option === null) {
          this._validate_package_args_src_parser_dart(!(this._parent_package_args_src_parser_dart === null), `Could not find an option named "--${__dartStr(name)}".`, `--${__dartStr(name)}`);
          return __dartNullCheck(this._parent_package_args_src_parser_dart)._handleLongOption_package_args_src_parser_dart(name, value);
        }
        this._args_package_args_src_parser_dart.shift();
        this._validate_package_args_src_parser_dart(option.isFlag, `Cannot negate non-flag option "--${__dartStr(name)}".`, `--${__dartStr(name)}`);
        this._validate_package_args_src_parser_dart(__dartNullCheck(option.negatable), `Cannot negate option "--${__dartStr(name)}".`, `--${__dartStr(name)}`);
        this._setFlag_package_args_src_parser_dart(this._results_package_args_src_parser_dart, option, false);
      } else {
        this._validate_package_args_src_parser_dart(!(this._parent_package_args_src_parser_dart === null), `Could not find an option named "--${__dartStr(name)}".`, `--${__dartStr(name)}`);
        return __dartNullCheck(this._parent_package_args_src_parser_dart)._handleLongOption_package_args_src_parser_dart(name, value);
      }
    }
    return true;
  }
  _validate_package_args_src_parser_dart(condition, message, args = null, source = null, offset = null) {
    if (!(condition)) {
      throw new ArgParserException(message, null, args, source, offset);
    }
  }
  _setOption_package_args_src_parser_dart(results, option, value, arg) {
    if (!(option.isMultiple)) {
      this._validateAllowed_package_args_src_parser_dart(option, value, arg);
      __dartMapSet(results, option.name, value);
      return;
    }
    let list = __dartAs(__dartMapPutIfAbsent(results, option.name, () => {
      return Array(0).fill(null);
    }), (value) => (Array.isArray(value) || (ArrayBuffer.isView(value) && !(value instanceof DataView))), "List");
    if (option.splitCommas) {
      let _sync_for_iterator = __dartIterator(value.split(","));
      for (; _sync_for_iterator.moveNext(); ) {
        let element = _sync_for_iterator.current;
        this._validateAllowed_package_args_src_parser_dart(option, element, arg);
        __dartListAdd(list, element);
      }
    } else {
      this._validateAllowed_package_args_src_parser_dart(option, value, arg);
      __dartListAdd(list, value);
    }
  }
  _setFlag_package_args_src_parser_dart(results, option, value) {
    __dartMapSet(results, option.name, value);
  }
  _validateAllowed_package_args_src_parser_dart(option, value, arg) {
    if (option.allowed === null) {
      return;
    }
    this._validate_package_args_src_parser_dart(__dartNullCheck(option.allowed).includes(value), `"${__dartStr(value)}" is not an allowed value for option "${__dartStr(arg)}".`, arg);
  }
}

class _Usage {
  constructor(_optionsAndSeparators, lineLength) {
    const $_columnWidths_package_args_src_usage_dart = __dartLazyField("_Usage._columnWidths", () => this._calculateColumnWidths_package_args_src_usage_dart(), "once");
    Object.defineProperty(this, "_columnWidths_package_args_src_usage_dart", { get: function() {
      return $_columnWidths_package_args_src_usage_dart.get();
    }, set: function(value) {
      $_columnWidths_package_args_src_usage_dart.set(value);
    }, enumerable: true });
    Object.defineProperty(this, "_optionsAndSeparators_package_args_src_usage_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_buffer_package_args_src_usage_dart", { value: __dartStringBuffer(""), writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_currentColumn_package_args_src_usage_dart", { value: 0, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_newlinesNeeded_package_args_src_usage_dart", { value: 0, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "lineLength", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "_optionsAndSeparators_package_args_src_usage_dart", { value: _optionsAndSeparators, writable: true, enumerable: true, configurable: true });
    Object.defineProperty(this, "lineLength", { value: lineLength, writable: true, enumerable: true, configurable: true });
  }
  generate() {
    let _sync_for_iterator = __dartIterator(this._optionsAndSeparators_package_args_src_usage_dart);
    for (; _sync_for_iterator.moveNext(); ) {
      let optionOrSeparator = _sync_for_iterator.current;
      label: {
        if (typeof optionOrSeparator === "string") {
          this._writeSeparator_package_args_src_usage_dart(optionOrSeparator);
          break label;
        }
        let option = __dartAs(optionOrSeparator, (value) => value instanceof Option, "Option");
        if (option.hide) {
          break label;
        }
        this._writeOption_package_args_src_usage_dart(option);
      }
    }
    return this._buffer_package_args_src_usage_dart.toString();
  }
  _writeSeparator_package_args_src_usage_dart(separator) {
    if (this._buffer_package_args_src_usage_dart.isNotEmpty) {
      this._buffer_package_args_src_usage_dart.write("\n\n");
    }
    this._buffer_package_args_src_usage_dart.write(separator);
    this._newlinesNeeded_package_args_src_usage_dart = 1;
  }
  _writeOption_package_args_src_usage_dart(option) {
    this._write_package_args_src_usage_dart(0, this._abbreviation_package_args_src_usage_dart(option));
    this._write_package_args_src_usage_dart(1, `${__dartStr(this._longOption_package_args_src_usage_dart(option))}${__dartStr(this._mandatoryOption_package_args_src_usage_dart(option))}`);
    const _0_0 = option.help;
    let help = null;
    if (!(_0_0 === null)) {
      help = _0_0;
      this._write_package_args_src_usage_dart(2, help);
    }
    const _1_0 = option.allowedHelp;
    let allowedHelp = null;
    if (!(_1_0 === null)) {
      allowedHelp = _1_0;
      this._newline_package_args_src_usage_dart();
      let _sync_for_iterator = __dartIterator(__dartIterableToArray(allowedHelp.entries()));
      for (; _sync_for_iterator.moveNext(); ) {
        const v = _sync_for_iterator.current;
        let name = null;
        let content = null;
        const _2_0 = v;
        name = _2_0[0];
        content = _2_0[1];
        this._write_package_args_src_usage_dart(1, this._allowedTitle_package_args_src_usage_dart(option, name));
        this._write_package_args_src_usage_dart(2, content);
      }
      this._newline_package_args_src_usage_dart();
    } else {
      if (!(option.allowed === null)) {
        this._write_package_args_src_usage_dart(2, this._buildAllowedList_package_args_src_usage_dart(option));
      } else {
        if (option.isFlag) {
          if (__dartEquals(option.defaultsTo, true)) {
            this._write_package_args_src_usage_dart(2, "(defaults to on)");
          }
        } else {
          if (option.isMultiple) {
            if ((!(option.defaultsTo === null) && __dartIterableToArray(__dartAs(option.defaultsTo, (value) => (((value != null && typeof value !== "string") && !(value instanceof Map)) && typeof value[Symbol.iterator] === "function"), "Iterable")).length > 0)) {
              let defaults = __dartIterableToArray(Array.from(__dartAs(option.defaultsTo, (value) => (Array.isArray(value) || (ArrayBuffer.isView(value) && !(value instanceof DataView))), "List"), (value) => {
                return `"${__dartStr(value)}"`;
              })).join(", ");
              this._write_package_args_src_usage_dart(2, `(defaults to ${__dartStr(defaults)})`);
            }
          } else {
            if (!(option.defaultsTo === null)) {
              this._write_package_args_src_usage_dart(2, `(defaults to "${__dartStr(option.defaultsTo)}")`);
            }
          }
        }
      }
    }
  }
  _abbreviation_package_args_src_usage_dart(option) {
    return (option.abbr === null ? "" : `-${__dartStr(option.abbr)}, `);
  }
  _longOption_package_args_src_usage_dart(option) {
    let result = null;
    if ((__dartNullCheck(option.negatable) && !(__dartNullCheck(option.hideNegatedUsage)))) {
      result = `--[no-]${__dartStr(option.name)}`;
    } else {
      result = `--${__dartStr(option.name)}`;
    }
    if (!(option.valueHelp === null)) {
      result = result + `=<${__dartStr(option.valueHelp)}>`;
    }
    return result;
  }
  _mandatoryOption_package_args_src_usage_dart(option) {
    return (option.mandatory ? " (mandatory)" : "");
  }
  _allowedTitle_package_args_src_usage_dart(option, allowed) {
    let isDefault = ((Array.isArray(option.defaultsTo) || (ArrayBuffer.isView(option.defaultsTo) && !(option.defaultsTo instanceof DataView))) ? __dartAs(option.defaultsTo, (value) => (Array.isArray(value) || (ArrayBuffer.isView(value) && !(value instanceof DataView))), "List").includes(allowed) : __dartEquals(option.defaultsTo, allowed));
    return `      [${__dartStr(allowed)}]${__dartStr((isDefault ? " (default)" : ""))}`;
  }
  _calculateColumnWidths_package_args_src_usage_dart() {
    let abbr = 0;
    let title = 0;
    let _sync_for_iterator = __dartIterator(this._optionsAndSeparators_package_args_src_usage_dart);
    for (; _sync_for_iterator.moveNext(); ) {
      let option = _sync_for_iterator.current;
      label: {
        if (!(option instanceof Option)) {
          break label;
        }
        if (option.hide) {
          break label;
        }
        abbr = Math.max(abbr, this._abbreviation_package_args_src_usage_dart(option).length);
        title = Math.max(title, this._longOption_package_args_src_usage_dart(option).length + this._mandatoryOption_package_args_src_usage_dart(option).length);
        if (!(option.allowedHelp === null)) {
          let _sync_for_iterator_1 = __dartIterator(__dartIterableToArray(__dartNullCheck(option.allowedHelp).keys()));
          for (; _sync_for_iterator_1.moveNext(); ) {
            let allowed = _sync_for_iterator_1.current;
            title = Math.max(title, this._allowedTitle_package_args_src_usage_dart(option, allowed).length);
          }
        }
      }
    }
    title = title + 4;
    return [abbr, title];
  }
  _newline_package_args_src_usage_dart() {
    this._newlinesNeeded_package_args_src_usage_dart = this._newlinesNeeded_package_args_src_usage_dart + 1;
    this._currentColumn_package_args_src_usage_dart = 0;
  }
  _write_package_args_src_usage_dart(column, text) {
    let lines = text.split("\n");
    if ((__dartEquals(column, this._columnWidths_package_args_src_usage_dart.length) && !(this.lineLength === null))) {
      let start = __dartIterableToArray(__dartIterableToArray(this._columnWidths_package_args_src_usage_dart).slice(0, column)).reduce((start, width) => {
        return start + width;
      });
      lines = (() => {
        const v = Array(0).fill(null);
        let _sync_for_iterator = __dartIterator(lines);
        for (; _sync_for_iterator.moveNext(); ) {
          let line = _sync_for_iterator.current;
          __dartListAddAll(v, wrapTextAsLines(line, { start: start, length: this.lineLength }));
        }
        return v;
      })();
    }
    while ((__dartIterableToArray(lines).length > 0 && __dartEquals(Array.from(lines)[0].trim(), ""))) {
      __dartListRemoveAt(lines, 0);
    }
    while ((__dartIterableToArray(lines).length > 0 && __dartEquals(Array.from(lines).at(-1).trim(), ""))) {
      __dartListRemoveLast(lines);
    }
    let _sync_for_iterator = __dartIterator(lines);
    for (; _sync_for_iterator.moveNext(); ) {
      let line = _sync_for_iterator.current;
      this._writeLine_package_args_src_usage_dart(column, line);
    }
  }
  _writeLine_package_args_src_usage_dart(column, text) {
    while (this._newlinesNeeded_package_args_src_usage_dart > 0) {
      this._buffer_package_args_src_usage_dart.write("\n");
      this._newlinesNeeded_package_args_src_usage_dart = this._newlinesNeeded_package_args_src_usage_dart - 1;
    }
    while (!(__dartEquals(this._currentColumn_package_args_src_usage_dart, column))) {
      if (this._currentColumn_package_args_src_usage_dart < 3 - 1) {
        this._buffer_package_args_src_usage_dart.write(" " * __dartListLikeGet(this._columnWidths_package_args_src_usage_dart, this._currentColumn_package_args_src_usage_dart));
      } else {
        this._buffer_package_args_src_usage_dart.write("\n");
      }
      this._currentColumn_package_args_src_usage_dart = (this._currentColumn_package_args_src_usage_dart + 1) % 3;
    }
    if (column < this._columnWidths_package_args_src_usage_dart.length) {
      this._buffer_package_args_src_usage_dart.write(text.padEnd(__dartListLikeGet(this._columnWidths_package_args_src_usage_dart, column), " "));
    } else {
      this._buffer_package_args_src_usage_dart.write(text);
    }
    this._currentColumn_package_args_src_usage_dart = (this._currentColumn_package_args_src_usage_dart + 1) % 3;
    if (__dartEquals(column, 3 - 1)) {
      this._newlinesNeeded_package_args_src_usage_dart = this._newlinesNeeded_package_args_src_usage_dart + 1;
    }
  }
  _buildAllowedList_package_args_src_usage_dart(option) {
    let isDefault = ((Array.isArray(option.defaultsTo) || (ArrayBuffer.isView(option.defaultsTo) && !(option.defaultsTo instanceof DataView))) ? (function() {
      const $receiver = __dartAs(option.defaultsTo, (value) => (Array.isArray(value) || (ArrayBuffer.isView(value) && !(value instanceof DataView))), "List");
      return function(element) {
        return $receiver.includes(element);
      };
    })() : (value) => {
      return __dartEquals(value, option.defaultsTo);
    });
    let allowedBuffer = __dartStringBuffer("");
    allowedBuffer.write("[");
    let first = true;
    let _sync_for_iterator = __dartIterator(__dartNullCheck(option.allowed));
    for (; _sync_for_iterator.moveNext(); ) {
      let allowed = _sync_for_iterator.current;
      if (!(first)) {
        allowedBuffer.write(", ");
      }
      allowedBuffer.write(allowed);
      if (isDefault(allowed)) {
        allowedBuffer.write(" (default)");
      }
      first = false;
    }
    allowedBuffer.write("]");
    return allowedBuffer.toString();
  }
}

const $_Usage__columnCount = __dartLazyField("_Usage._columnCount", () => 3, false);
Object.defineProperty(_Usage, "_columnCount_package_args_src_usage_dart", { get: function() {
  return $_Usage__columnCount.get();
}, set: function(value) {
  $_Usage__columnCount.set(value);
}, enumerable: true });
const $ArgParser_interface = Symbol("ArgParser");
class ArgParser {
  constructor({ allowTrailingOptions = true, usageLineLength = null } = {}) {
    return ArgParser.__package_args_src_arg_parser_dart(__dartMapFromEntries([]), __dartMapFromEntries([]), __dartMapFromEntries([]), { allowTrailingOptions: allowTrailingOptions, usageLineLength: usageLineLength });
  }
  static __package_args_src_arg_parser_dart(options, commands, _aliases, { allowTrailingOptions = true, usageLineLength = null } = {}) {
    const $self = Object.create(this.prototype);
    Object.defineProperty($self, "_options_package_args_src_arg_parser_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_commands_package_args_src_arg_parser_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_aliases_package_args_src_arg_parser_dart", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "options", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "commands", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_optionsAndSeparators_package_args_src_arg_parser_dart", { value: Array(0).fill(null), writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "allowTrailingOptions", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "usageLineLength", { value: null, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_aliases_package_args_src_arg_parser_dart", { value: _aliases, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "allowTrailingOptions", { value: allowTrailingOptions, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "usageLineLength", { value: usageLineLength, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_options_package_args_src_arg_parser_dart", { value: options, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "options", { value: __dartUnmodifiableMapView(options), writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "_commands_package_args_src_arg_parser_dart", { value: commands, writable: true, enumerable: true, configurable: true });
    Object.defineProperty($self, "commands", { value: __dartUnmodifiableMapView(commands), writable: true, enumerable: true, configurable: true });
    return $self;
  }
  static allowAnything() {
    return new AllowAnythingParser();
  }
  get allowsAnything() {
    return false;
  }
  addCommand(name, parser = null) {
    if (__dartMapContainsKey(this._commands_package_args_src_arg_parser_dart, name)) {
      throw __dartCoreError("ArgumentError", `Duplicate command "${__dartStr(name)}".`);
    }
    (parser === null ? parser = new ArgParser() : null);
    __dartMapSet(this._commands_package_args_src_arg_parser_dart, name, parser);
    return parser;
  }
  addFlag(name, { abbr = null, help = null, defaultsTo = false, negatable = true, callback = null, hide = false, hideNegatedUsage = false, aliases = __dartConst("[\"list\",\"InterfaceType(String)\"]", () => Object.freeze([])) } = {}) {
    this._addOption_package_args_src_arg_parser_dart(name, abbr, help, null, null, null, defaultsTo, (callback === null ? null : (value) => {
      return callback(value);
    }), __dartConst("[\"InstanceConstant\",\"InstanceConstant(const OptionType{OptionType.name: \\\"OptionType.flag\\\"})\"]", () => Object.freeze(Object.assign(Object.create(OptionType.prototype), { name: "OptionType.flag" }))), { negatable: negatable, hide: hide, hideNegatedUsage: hideNegatedUsage, aliases: aliases });
  }
  addOption(name, { abbr = null, help = null, valueHelp = null, allowed = null, allowedHelp = null, defaultsTo = null, callback = null, mandatory = false, hide = false, aliases = __dartConst("[\"list\",\"InterfaceType(String)\"]", () => Object.freeze([])) } = {}) {
    this._addOption_package_args_src_arg_parser_dart(name, abbr, help, valueHelp, allowed, allowedHelp, defaultsTo, callback, __dartConst("[\"InstanceConstant\",\"InstanceConstant(const OptionType{OptionType.name: \\\"OptionType.single\\\"})\"]", () => Object.freeze(Object.assign(Object.create(OptionType.prototype), { name: "OptionType.single" }))), { mandatory: mandatory, hide: hide, aliases: aliases });
  }
  addMultiOption(name, { abbr = null, help = null, valueHelp = null, allowed = null, allowedHelp = null, defaultsTo = null, callback = null, splitCommas = true, hide = false, aliases = __dartConst("[\"list\",\"InterfaceType(String)\"]", () => Object.freeze([])) } = {}) {
    this._addOption_package_args_src_arg_parser_dart(name, abbr, help, valueHelp, allowed, allowedHelp, ((() => {
      const v = defaultsTo;
      return (v === null ? null : __dartListOf(v, true));
    })() ?? Array(0).fill(null)), (callback === null ? null : (value) => {
      return callback(value);
    }), __dartConst("[\"InstanceConstant\",\"InstanceConstant(const OptionType{OptionType.name: \\\"OptionType.multiple\\\"})\"]", () => Object.freeze(Object.assign(Object.create(OptionType.prototype), { name: "OptionType.multiple" }))), { splitCommas: splitCommas, hide: hide, aliases: aliases });
  }
  _addOption_package_args_src_arg_parser_dart(name, abbr, help, valueHelp, allowed, allowedHelp, defaultsTo, callback, type, { negatable = false, splitCommas = null, mandatory = false, hide = false, hideNegatedUsage = false, aliases = __dartConst("[\"list\",\"InterfaceType(String)\"]", () => Object.freeze([])) } = {}) {
    let allNames = (() => {
      const v = [name];
      __dartListAddAll(v, aliases);
      return v;
    })();
    if (__dartIterableToArray(allNames).some((name) => {
      return !(this.findByNameOrAlias(name) === null);
    })) {
      throw __dartCoreError("ArgumentError", `Duplicate option or alias "${__dartStr(name)}".`);
    }
    if (!(abbr === null)) {
      let existing = this.findByAbbreviation(abbr);
      if (!(existing === null)) {
        throw __dartCoreError("ArgumentError", `Abbreviation "${__dartStr(abbr)}" is already used by "${__dartStr(existing.name)}".`);
      }
    }
    if ((mandatory && !(defaultsTo === null))) {
      throw __dartCoreError("ArgumentError", `The option ${__dartStr(name)} cannot be mandatory and have a default value.`);
    }
    if ((!(negatable) && hideNegatedUsage)) {
      throw __dartCoreError("ArgumentError", `The option ${__dartStr(name)} cannot have \`hideNegatedUsage\` without being negatable.`);
    }
    let option = newOption(name, abbr, help, valueHelp, allowed, allowedHelp, defaultsTo, callback, type, { negatable: negatable, splitCommas: splitCommas, mandatory: mandatory, hide: hide, hideNegatedUsage: hideNegatedUsage, aliases: aliases });
    __dartMapSet(this._options_package_args_src_arg_parser_dart, name, option);
    __dartListAdd(this._optionsAndSeparators_package_args_src_arg_parser_dart, option);
    let _sync_for_iterator = __dartIterator(aliases);
    for (; _sync_for_iterator.moveNext(); ) {
      let alias = _sync_for_iterator.current;
      __dartMapSet(this._aliases_package_args_src_arg_parser_dart, alias, name);
    }
  }
  addSeparator(text) {
    __dartListAdd(this._optionsAndSeparators_package_args_src_arg_parser_dart, text);
  }
  parse(args) {
    return new Parser(null, this, __dartIterableToArray(args)).parse();
  }
  get usage() {
    return generateUsage(this._optionsAndSeparators_package_args_src_arg_parser_dart, { lineLength: this.usageLineLength });
  }
  defaultFor(option) {
    let value = this.findByNameOrAlias(option);
    if (value === null) {
      throw __dartCoreError("ArgumentError", `No option named ${__dartStr(option)}`);
    }
    return value.defaultsTo;
  }
  getDefault(option) {
    return this.defaultFor(option);
  }
  findByAbbreviation(abbr) {
    let _sync_for_iterator = __dartIterator(__dartIterableToArray(this.options.values()));
    for (; _sync_for_iterator.moveNext(); ) {
      let option = _sync_for_iterator.current;
      if (__dartEquals(option.abbr, abbr)) {
        return option;
      }
    }
    return null;
  }
  findByNameOrAlias(name) {
    return __dartMapGet(this.options, (__dartMapGet(this._aliases_package_args_src_arg_parser_dart, name) ?? name));
  }
}

Object.defineProperty(ArgParser, Symbol.hasInstance, { value: function(value) {
  return value != null && (ArgParser.prototype.isPrototypeOf(value) || value[$ArgParser_interface] === true);
} });
class AllowAnythingParser {
  constructor() {
    Object.defineProperty(this, $ArgParser_interface, { value: true });
  }
  get options() {
    return __dartConst("[\"map\",\"InterfaceType(String)\",\"InterfaceType(Option)\"]", () => __dartConstMap([]));
  }
  get commands() {
    return __dartConst("[\"map\",\"InterfaceType(String)\",\"InterfaceType(ArgParser)\"]", () => __dartConstMap([]));
  }
  get allowTrailingOptions() {
    return false;
  }
  get allowsAnything() {
    return true;
  }
  get usageLineLength() {
    return null;
  }
  addCommand(name, parser = null) {
    throw __dartCoreError("UnsupportedError", "ArgParser.allowAnything().addCommands() isn't supported.");
  }
  addFlag(name, { abbr = null, help = null, defaultsTo = false, negatable = true, callback = null, hide = false, hideNegatedUsage = false, aliases = __dartConst("[\"list\",\"InterfaceType(String)\"]", () => Object.freeze([])) } = {}) {
    throw __dartCoreError("UnsupportedError", "ArgParser.allowAnything().addFlag() isn't supported.");
  }
  addOption(name, { abbr = null, help = null, valueHelp = null, allowed = null, allowedHelp = null, defaultsTo = null, callback = null, allowMultiple = false, splitCommas = null, mandatory = false, hide = false, aliases = __dartConst("[\"list\",\"InterfaceType(String)\"]", () => Object.freeze([])) } = {}) {
    throw __dartCoreError("UnsupportedError", "ArgParser.allowAnything().addOption() isn't supported.");
  }
  addMultiOption(name, { abbr = null, help = null, valueHelp = null, allowed = null, allowedHelp = null, defaultsTo = null, callback = null, splitCommas = true, hide = false, aliases = __dartConst("[\"list\",\"InterfaceType(String)\"]", () => Object.freeze([])) } = {}) {
    throw __dartCoreError("UnsupportedError", "ArgParser.allowAnything().addMultiOption() isn't supported.");
  }
  addSeparator(text) {
    throw __dartCoreError("UnsupportedError", "ArgParser.allowAnything().addSeparator() isn't supported.");
  }
  parse(args) {
    return new Parser(null, this, __dartIterableToArray(args)).parse();
  }
  get usage() {
    return "";
  }
  defaultFor(option) {
    throw __dartCoreError("ArgumentError", `No option named ${__dartStr(option)}`);
  }
  getDefault(option) {
    throw __dartCoreError("ArgumentError", `No option named ${__dartStr(option)}`);
  }
  findByAbbreviation(abbr) {
    return null;
  }
  findByNameOrAlias(name) {
    return null;
  }
  get _options_package_args_src_arg_parser_dart() {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
  get _commands_package_args_src_arg_parser_dart() {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
  get _aliases_package_args_src_arg_parser_dart() {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
  get _optionsAndSeparators_package_args_src_arg_parser_dart() {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
  _addOption_package_args_src_arg_parser_dart(name, abbr, help, valueHelp, allowed, allowedHelp, defaultsTo, callback, type, { negatable = false, splitCommas = null, mandatory = false, hide = false, hideNegatedUsage = false, aliases = __dartConst("[\"list\",\"InterfaceType(String)\"]", () => Object.freeze([])) } = {}) {
    return (() => {
      throw __dartCoreError("NoSuchMethodError", this);
    })();
  }
}

function newArgResults(parser, parsed, name, command, rest, $arguments) {
  return ArgResults.__package_args_src_arg_results_dart(parser, parsed, name, command, rest, $arguments);
}

function newOption(name, abbr, help, valueHelp, allowed, allowedHelp, defaultsTo, callback, type, { negatable = null, splitCommas = null, mandatory = false, hide = false, hideNegatedUsage = false, aliases = __dartConst("[\"list\",\"InterfaceType(String)\"]", () => Object.freeze([])) } = {}) {
  return Option.__package_args_src_option_dart(name, abbr, help, valueHelp, allowed, allowedHelp, defaultsTo, callback, type, { negatable: negatable, splitCommas: splitCommas, mandatory: mandatory, hide: hide, hideNegatedUsage: hideNegatedUsage, aliases: aliases });
}

function _isLetterOrDigit(codeUnit) {
  return (((codeUnit >= 65 && codeUnit <= 90) || (codeUnit >= 97 && codeUnit <= 122)) || (codeUnit >= 48 && codeUnit <= 57));
}

function _isLetterDigitHyphenOrUnderscore(codeUnit) {
  return ((_isLetterOrDigit(codeUnit) || __dartEquals(codeUnit, 45)) || __dartEquals(codeUnit, 95));
}

function wrapTextAsLines(text, { start = 0, length = null } = {}) {
  const isWhitespace = (text, index) => {
    let rune = text.charCodeAt(index);
    return ((((((((((((rune >= 9 && rune <= 13) || __dartEquals(rune, 32)) || __dartEquals(rune, 133)) || __dartEquals(rune, 5760)) || __dartEquals(rune, 6158)) || (rune >= 8192 && rune <= 8202)) || __dartEquals(rune, 8232)) || __dartEquals(rune, 8233)) || __dartEquals(rune, 8239)) || __dartEquals(rune, 8287)) || __dartEquals(rune, 12288)) || __dartEquals(rune, 65279));
  };
  if (length === null) {
    return text.split("\n");
  }
  let result = Array(0).fill(null);
  let effectiveLength = Math.max(length - start, 10);
  let _sync_for_iterator = __dartIterator(text.split("\n"));
  for (; _sync_for_iterator.moveNext(); ) {
    let line = _sync_for_iterator.current;
    label: {
      line = line.trim();
      if (line.length <= effectiveLength) {
        __dartListAdd(result, line);
        break label;
      }
      let currentLineStart = 0;
      let lastWhitespace = null;
      for (let i = 0; i < line.length; i = i + 1) {
        if (isWhitespace(line, i)) {
          lastWhitespace = i;
        }
        if (i - currentLineStart >= effectiveLength) {
          if (!(lastWhitespace === null)) {
            i = lastWhitespace;
          }
          __dartListAdd(result, line.substring(currentLineStart, i).trim());
          while ((isWhitespace(line, i) && i < line.length)) {
            i = i + 1;
          }
          currentLineStart = i;
          lastWhitespace = null;
        }
      }
      __dartListAdd(result, line.substring(currentLineStart).trim());
    }
  }
  return result;
}

function generateUsage(optionsAndSeparators, { lineLength = null } = {}) {
  return new _Usage(optionsAndSeparators, lineLength).generate();
}

export function main() {
  const command = (() => {
    const v = new ArgParser();
    return (() => {
      v.addFlag("watch", { defaultsTo: true });
      v.addOption("host", { defaultsTo: "localhost" });
      return v;
    })();
  })();
  const parser = (() => {
    const v = new ArgParser({ allowTrailingOptions: false });
    return (() => {
      v.addFlag("verbose", { abbr: "v", negatable: false });
      v.addOption("mode", { allowed: ["debug", "release"], defaultsTo: "debug" });
      v.addMultiOption("define", { abbr: "D" });
      v.addCommand("serve", command);
      return v;
    })();
  })();
  const results = parser.parse(["--mode", "release", "-v", "-Dfoo=bar", "-D", "answer=42", "serve", "--no-watch", "--host", "0.0.0.0", "web"]);
  const subcommand = __dartNullCheck(results.command);
  __dartPrint(`args ${__dartStr(results["[]"]("mode"))} ${__dartStr(results["[]"]("verbose"))} ${__dartStr(__dartIterableToArray(__dartAs(results["[]"]("define"), (value) => (Array.isArray(value) || (ArrayBuffer.isView(value) && !(value instanceof DataView))), "List")).join("|"))} ${__dartStr(subcommand.name)} ${__dartStr(subcommand["[]"]("watch"))} ${__dartStr(subcommand["[]"]("host"))} ${__dartStr(__dartIterableToArray(subcommand.rest).join(","))} ${__dartStr(results.wasParsed("mode"))}`);
  try {
    parser.parse(["--mode", "profile"]);
  } catch ($error) {
    if (__dartIsCoreError($error, "FormatException")) {
      const error = $error;
      __dartPrint(`args-error ${__dartStr(Array.from(error.message.split("\n"))[0])}`);
    } else {
      throw $error;
    }
  }
  __dartPrint(`usage ${__dartStr(parser.usage.includes("--mode"))} ${__dartStr(__dartIterableToArray(__dartIterableToArray(parser.commands.keys())).join(","))}`);
}

main();
