// Generated by dart2esm.

function __dartPrint(value) {
  console.log(__dartStr(value));
}

function __dartStr(value) {
  if (value == null) return "null";
  if (Array.isArray(value)) {
    return "[" + value.map(__dartStr).join(", ") + "]";
  }
  if (value instanceof Set) {
    return "{" + Array.from(value).map(__dartStr).join(", ") + "}";
  }
  if (value instanceof Map) {
    return "{" + Array.from(value, ([key, entryValue]) => __dartStr(key) + ": " + __dartStr(entryValue)).join(", ") + "}";
  }
  if (typeof value === "object") {
    const toString = value.toString;
    if (typeof toString === "function" && toString !== Object.prototype.toString) {
      return String(toString.call(value));
    }
  }
  return String(value);
}

export class Point {
  constructor(x, y) {
    this.x = null;
    this.y = null;
    this.label = null;
    this.x = x;
    this.y = y;
    this.label = "point";
  }
  static origin() {
    const $self = Object.create(this.prototype);
    $self.x = null;
    $self.y = null;
    $self.label = null;
    $self.x = 0;
    $self.y = 0;
    $self.label = "origin";
    $self.label = `${__dartStr($self.label)}!`;
    return $self;
  }
  static square(size) {
    const $self = Object.create(this.prototype);
    $self.x = null;
    $self.y = null;
    $self.label = null;
    $self.x = size;
    $self.y = size;
    $self.label = "square";
    return $self;
  }
  describe() {
    return `${__dartStr(this.label)}:${__dartStr(this.x)},${__dartStr(this.y)}`;
  }
}

export class Token {
  constructor() {
    throw new TypeError("Class Token has no unnamed constructor");
  }
  static named(value) {
    const $self = Object.create(this.prototype);
    $self.value = null;
    $self.value = value;
    return $self;
  }
  describe() {
    return `token ${__dartStr(this.value)}`;
  }
}

export function main() {
  const origin = Point.origin();
  const square = Point.square(4);
  const regular = new Point(2, 3);
  const token = Token.named("alpha");
  __dartPrint(origin.describe());
  __dartPrint(square.describe());
  __dartPrint(regular.describe());
  __dartPrint(token.describe());
}

main();
