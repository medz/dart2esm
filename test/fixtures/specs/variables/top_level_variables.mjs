// Generated by dart2esm.

function __dartPrint(value) {
  console.log(value);
}

export let initCount = 0;
export let readFirst = init("readFirst", 10);
export let assignFirst = init("assignFirst", 20);
export const finalValue = init("finalValue", 30);
export const constValue = 40;
let _computedBacking = 5;
export function init(name, value) {
  __dartPrint(`init ${name}`);
  initCount = initCount + 1;
  return value;
}

function _computedValue() {
  __dartPrint("get computed");
  return _computedBacking + 1;
}

function _computedValue_1(value) {
  __dartPrint(`set computed ${value}`);
  _computedBacking = value;
}

export function main() {
  const firstRead = readFirst;
  const firstAssign = assignFirst;
  const firstFinal = finalValue;
  const firstComputed = _computedValue();
  assignFirst = 99;
  _computedValue_1(40);
  __dartPrint(`initial ${firstAssign}`);
  __dartPrint(`assigned ${assignFirst}`);
  __dartPrint(`read ${firstRead}`);
  __dartPrint(`read again ${readFirst}`);
  __dartPrint(`final ${firstFinal}`);
  __dartPrint("const 40");
  __dartPrint(`computed ${firstComputed} ${_computedValue()} backing ${_computedBacking}`);
  __dartPrint(`count ${initCount}`);
}

main();
