// Generated by dart2esm.

function __dartPrint(value) {
  console.log(value);
}

const __dartRecordShape = Symbol("dart.recordShape");

function __dartIsRecord(value) {
  return value != null && typeof value === "object" && Array.isArray(value[__dartRecordShape]);
}

function __dartRecord(positional, named) {
  const record = {};
  const shape = [];
  for (let i = 0; i < positional.length; i++) {
    const name = "$" + (i + 1);
    shape.push(name);
    Object.defineProperty(record, name, { value: positional[i], enumerable: true });
  }
  for (const name of Object.keys(named).sort()) {
    shape.push(name);
    Object.defineProperty(record, name, { value: named[name], enumerable: true });
  }
  Object.defineProperty(record, __dartRecordShape, { value: Object.freeze(shape) });
  Object.defineProperty(record, "toString", {
    value() {
      return "(" + shape.map((name) => {
        const value = String(record[name]);
        return name.startsWith("$") ? value : name + ": " + value;
      }).join(", ") + ")";
    },
  });
  return Object.freeze(record);
}

export function flags(value) {
  return `${value != null} ${(((value != null && typeof value !== "string") && !(value instanceof Map)) && typeof value[Symbol.iterator] === "function")} ${(Array.isArray(value) || (ArrayBuffer.isView(value) && !(value instanceof DataView)))} ${value instanceof Set} ${value instanceof Map} ${typeof value === "function"} ${__dartIsRecord(value)} ${(value === null || typeof value === "string")} ${!(typeof value === "string")}`;
}

export function recordFlags(value) {
  return `${(((((__dartIsRecord(value) && value[__dartRecordShape].length === 2) && value[__dartRecordShape][0] === "$1") && value[__dartRecordShape][1] === "label") && typeof value.$1 === "number") && typeof value.label === "string")} ${(((((__dartIsRecord(value) && value[__dartRecordShape].length === 2) && value[__dartRecordShape][0] === "$1") && value[__dartRecordShape][1] === "label") && typeof value.$1 === "string") && typeof value.label === "string")} ${(((((__dartIsRecord(value) && value[__dartRecordShape].length === 2) && value[__dartRecordShape][0] === "$1") && value[__dartRecordShape][1] === "other") && typeof value.$1 === "number") && typeof value.other === "string")} ${(((((__dartIsRecord(value) && value[__dartRecordShape].length === 2) && value[__dartRecordShape][0] === "$1") && value[__dartRecordShape][1] === "$2") && typeof value.$1 === "number") && typeof value.$2 === "number")} ${(value === null || __dartIsRecord(value))}`;
}

export function main() {
  __dartPrint(flags([1, 2]));
  __dartPrint(flags((function() { const v = new Set(); v.add(1); v.add(2); return v; })()));
  __dartPrint(flags(new Map([["a", 1]])));
  __dartPrint(flags(function() { return 1; }));
  __dartPrint(flags(__dartRecord([1], { label: "one" })));
  __dartPrint(flags("x"));
  __dartPrint(flags(null));
  __dartPrint(recordFlags(__dartRecord([1], { label: "one" })));
  __dartPrint(recordFlags(null));
  __dartPrint(recordFlags([1]));
}

main();
