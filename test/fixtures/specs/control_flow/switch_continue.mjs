function __dartStr(value) {
  if (value == null) return "null";
  if (Array.isArray(value)) {
    return "[" + value.map(__dartStr).join(", ") + "]";
  }
  if (value instanceof Set) {
    return "{" + Array.from(value).map(__dartStr).join(", ") + "}";
  }
  if (value instanceof Map) {
    return "{" + Array.from(value, ([key, entryValue]) => __dartStr(key) + ": " + __dartStr(entryValue)).join(", ") + "}";
  }
  return String(value);
}
function __dartPrint(value) {
  console.log(__dartStr(value));
}

// Generated by dart2esm.

export function route(value) {
  let log = "";
  L:
  {
    const $switchValue = value;
    let $switchTarget = -1;
    switch ($switchValue) {
      case 0:
        $switchTarget = 0;
        break;
      case 1:
        $switchTarget = 1;
        break;
      case 2:
      case 3:
        $switchTarget = 2;
        break;
      default:
        $switchTarget = 3;
        break;
    }
    $switchLoop: while ($switchTarget !== -1) {
      switch ($switchTarget) {
        case 0:
          $switchTarget = -1;
          {
            log = __dartStr(log) + "zero>";
            $switchTarget = 1;
            continue $switchLoop;
          }
          break;
        case 1:
          $switchTarget = -1;
          {
            log = __dartStr(log) + "one>";
            break L;
          }
          break;
        case 2:
          $switchTarget = -1;
          {
            log = __dartStr(log) + "group>";
            break L;
          }
          break;
        case 3:
          $switchTarget = -1;
          {
            log = __dartStr(log) + "default>";
          }
          break;
      }
    }
  }
  return log;
}

export function loopAndSwitch(value) {
  let log = "";
  for (let i = 0; (i < 3); i = (i + 1)) {
    L:
    {
      L_1:
      {
        const $switchValue = (value + i);
        let $switchTarget = -1;
        switch ($switchValue) {
          case 1:
            $switchTarget = 0;
            break;
          case 2:
            $switchTarget = 1;
            break;
          case 3:
            $switchTarget = 2;
            break;
          default:
            $switchTarget = 3;
            break;
        }
        $switchLoop: while ($switchTarget !== -1) {
          switch ($switchTarget) {
            case 0:
              $switchTarget = -1;
              {
                log = __dartStr(log) + "skip>";
                break L;
              }
              break;
            case 1:
              $switchTarget = -1;
              {
                log = __dartStr(log) + "jump>";
                $switchTarget = 2;
                continue $switchLoop;
              }
              break;
            case 2:
              $switchTarget = -1;
              {
                log = __dartStr(log) + "done>";
                break L_1;
              }
              break;
            case 3:
              $switchTarget = -1;
              {
                log = __dartStr(log) + "other>";
              }
              break;
          }
        }
      }
      log = __dartStr(log) + "after>";
    }
  }
  return log;
}

export function main() {
  __dartPrint(route(0));
  __dartPrint(route(1));
  __dartPrint(route(2));
  __dartPrint(route(3));
  __dartPrint(route(4));
  __dartPrint(loopAndSwitch(0));
  __dartPrint(loopAndSwitch(1));
}

main();
