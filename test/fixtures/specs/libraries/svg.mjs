// Generated by dart2esm.

function __dartPrint(value) {
  console.log(__dartStr(value));
}

function __dartStr(value) {
  if (value == null) return "null";
  if (Array.isArray(value)) {
    return "[" + value.map(__dartStr).join(", ") + "]";
  }
  if (value instanceof Set) {
    return "{" + Array.from(value).map(__dartStr).join(", ") + "}";
  }
  if (value instanceof Map) {
    return "{" + Array.from(value, ([key, entryValue]) => __dartStr(key) + ": " + __dartStr(entryValue)).join(", ") + "}";
  }
  if (typeof value === "object") {
    const toString = value.toString;
    if (typeof toString === "function" && toString !== Object.prototype.toString) {
      return String(toString.call(value));
    }
  }
  return String(value);
}

function __dartAs(value, test, typeName) {
  if (test(value)) {
    return value;
  }
  throw new TypeError("Type cast failed: expected " + typeName);
}

export function hide(value) {
  return value;
}

export function main() {
  const element = globalThis.document.createElementNS("http://www.w3.org/2000/svg", "svg");
  element.id = "root";
  element.setAttribute("viewBox", "0 0 10 10");
  const title = globalThis.document.createElementNS("http://www.w3.org/2000/svg", "title");
  title.textContent = "Dart";
  element.appendChild(title);
  const maybeTitle = hide(title);
  const castTitle = __dartAs(maybeTitle, (value) => (value === null || (typeof globalThis["SVGElement"] === "function" && value instanceof globalThis["SVGElement"])), "SvgElement");
  __dartPrint(`svg ${__dartStr(element.id)} ${__dartStr(element.getAttribute("viewBox"))} ${__dartStr(element.children.length)} ${__dartStr(castTitle?.textContent)} ${__dartStr((typeof globalThis["SVGElement"] === "function" && maybeTitle instanceof globalThis["SVGElement"]))}`);
}

main();
