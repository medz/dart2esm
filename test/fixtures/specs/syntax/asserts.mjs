// Generated by dart2esm.

function __dartPrint(value) {
  console.log(value);
}

export function checked(value) {
  let touched = false;
  return (touched ? value : -1);
}

export function failed() {
  let messageEvaluated = false;
  try {
    return "not reached";
  } catch ($error) {
    if ($error != null) {
      const error = $error;
      return `caught:${String(error).includes("boom")}:${messageEvaluated}`;
    } else {
      throw $error;
    }
  }
}

export function main() {
  __dartPrint(`${checked(3)} ${failed()}`);
}

main();
