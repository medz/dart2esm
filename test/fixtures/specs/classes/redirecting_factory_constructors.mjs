function __dartStr(value) {
  if (value == null) return "null";
  if (Array.isArray(value)) {
    return "[" + value.map(__dartStr).join(", ") + "]";
  }
  if (value instanceof Set) {
    return "{" + Array.from(value).map(__dartStr).join(", ") + "}";
  }
  if (value instanceof Map) {
    return "{" + Array.from(value, ([key, entryValue]) => __dartStr(key) + ": " + __dartStr(entryValue)).join(", ") + "}";
  }
  return String(value);
}
function __dartPrint(value) {
  console.log(__dartStr(value));
}
function __dartAs(value, test, typeName) {
  if (test(value)) return value;
  throw new TypeError("Type cast failed: expected " + typeName);
}

// Generated by dart2esm.

export class Shape {
  constructor(size) {
    if (new.target === Shape) {
      return new Square(size);
    }
  }
  static named(size) {
    return Square.named(size);
  }
  static alias(size) {
    return Shape.named(size);
  }
  static newAlias(size) {
    return new Square(size);
  }
  static withDefault(size = 8) {
    return Square.withDefault(size);
  }
  static options({ size = 9, label = "options" } = {}) {
    return Square.options({ size: size, label: label });
  }
}

export class Square extends Shape {
  constructor(size) {
    super();
    this.size = size;
    this.label = "square";
  }
  static named(size) {
    return $Square_named(Square, size);
  }
  static withDefault(size = 8) {
    return $Square_withDefault(Square, size);
  }
  static options({ size = 9, label = "options" } = {}) {
    return $Square_options(Square, { size: size, label: label });
  }
  describe() {
    return __dartStr(this.label) + ":" + __dartStr(this.size);
  }
}

function $Square_named($newTarget, size) {
  const $self = Object.create($newTarget.prototype);
  $self.size = size;
  $self.label = "named";
  return $self;
}

function $Square_withDefault($newTarget, size = 8) {
  const $self = Object.create($newTarget.prototype);
  $self.size = size;
  $self.label = "default";
  return $self;
}

function $Square_options($newTarget, { size = 9, label = "options" } = {}) {
  const $self = Object.create($newTarget.prototype);
  $self.size = size;
  $self.label = label;
  return $self;
}

export class Widget {
  constructor() {
    if (new.target === Widget) {
      throw new TypeError("Class Widget has no unnamed constructor");
    }
  }
  static d() {
    return new Button();
  }
  static named(label) {
    return Button.named(label);
  }
}

export class Button extends Widget {
  constructor() {
    super();
    this.label = "button";
  }
  static named(label) {
    return $Button_named(Button, label);
  }
  describe() {
    return "button:" + __dartStr(this.label);
  }
}

function $Button_named($newTarget, label) {
  const $self = Object.create($newTarget.prototype);
  $self.label = label;
  return $self;
}

export function main() {
  const unnamed = __dartAs(new Square(2), value => value instanceof Square, "Square");
  const named = __dartAs(Square.named(3), value => value instanceof Square, "Square");
  const alias = __dartAs(Square.named(4), value => value instanceof Square, "Square");
  const newAlias = __dartAs(new Square(5), value => value instanceof Square, "Square");
  const withDefault = __dartAs(Square.withDefault(), value => value instanceof Square, "Square");
  const optionsDefault = __dartAs(Square.options(), value => value instanceof Square, "Square");
  const optionsCustom = __dartAs(Square.options({ size: 6, label: "custom" }), value => value instanceof Square, "Square");
  const widget = __dartAs(new Button(), value => value instanceof Button, "Button");
  const namedWidget = __dartAs(Button.named("primary"), value => value instanceof Button, "Button");
  __dartPrint(unnamed.describe());
  __dartPrint(named.describe());
  __dartPrint(alias.describe());
  __dartPrint(newAlias.describe());
  __dartPrint(withDefault.describe());
  __dartPrint(optionsDefault.describe());
  __dartPrint(optionsCustom.describe());
  __dartPrint(widget.describe());
  __dartPrint(namedWidget.describe());
}

main();
